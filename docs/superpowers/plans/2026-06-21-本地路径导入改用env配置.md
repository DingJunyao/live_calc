# 「从本地路径导入」改用 .env 配置 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把数据维护中心「从本地路径导入」从页面上手填目录路径，改为直接复用 `.env` 的 `DATA_LOCAL_PATH`，与启动时导入共用同一来源。

**Architecture:** 后端 `trigger_local_import` 移除 `local_path` 查询参数，改读 `settings.data_local_path`；新增一个只读 `GET /data/local-path-config` 给前端展示。前端删掉 `v-text-field`，改为只读展示当前配置路径，未配置时禁用「开始导入」按钮。service 层 `import_from_local_dir` 签名不变，路径来源由 API 层注入。

**Tech Stack:** FastAPI + Pydantic Settings（后端）；Vue 3 `<script setup>` + Vuetify 3 + Axios（前端）。

**项目规范约束（来自 CLAUDE.md，优先级最高）：**
- 不自动执行 `git commit` / 分支操作——本计划**不含任何 git commit 步骤**。每个任务的收尾验证改为「后端语法检查通过 / 前端构建通过」。
- 后端修改须确保无语法错误；前端修改须确保构建通过。
- 不要在对话中启动服务（用户已起自动重载的前后端服务），验证靠语法检查 + 构建 + 手动核对。
- 不涉及数据库表结构变更，无需 alembic / SQL 脚本。

---

## File Structure

| 文件 | 责任 | 改动 |
|---|---|---|
| `backend/app/api/import_api.py` | 导入相关 API 路由 | 改造 `trigger_local_import`；新增 `get_local_path_config`；加 `settings` 导入 |
| `frontend/src/views/admin/DataMaintenanceView.vue` | 数据维护中心页面 | 删 `v-text-field` 与 `localPath` ref；加只读展示与配置拉取；改 `importFromLocalPath` |

`backend/app/config.py`（`data_local_path` 字段）、`backend/app/main.py`（启动导入）、`backend/app/services/importer/api_service.py`（`import_from_local_dir`）、`frontend/src/composables/useImportTask.ts`（`startTask` 通用签名）—— **均不改**。

---

## Task 1: 后端 — 导入端点改读 .env + 新增配置查询端点

**Files:**
- Modify: `backend/app/api/import_api.py`（顶部导入区 L14-19 附近、`trigger_local_import` L100-122）

- [ ] **Step 1: 在顶部导入区加入 `settings`**

在 `backend/app/api/import_api.py` 现有 `from app.core.database ...` 这一行的**前面**，按 isort 字母序（app.config 早于 app.core）插入一行。

把这段：

```python
from app.core.database import get_db, SessionLocal
```

改为：

```python
from app.config import settings
from app.core.database import get_db, SessionLocal
```

- [ ] **Step 2: 改造 `trigger_local_import`，移除 `local_path` 参数、改读 settings**

把 `backend/app/api/import_api.py` 中 L100-122 的整个 `trigger_local_import` 函数：

```python
@router.post("/data/import-from-local")
def trigger_local_import(
    local_path: str,
    db: Session = Depends(get_db),
    current_user=Depends(get_current_user),
):
    """从本地目录导入（仅管理员）。"""
    if not getattr(current_user, "is_admin", False):
        raise HTTPException(403, detail="仅管理员可操作")

    if not os.path.isdir(local_path):
        raise HTTPException(400, detail=f"目录不存在: {local_path}")

    task_id = start_background_import(
        db,
        "local_import",
        current_user.id,
        import_from_local_dir,
        db,
        local_path,
        current_user.id,
    )
    return {"task_id": task_id}
```

替换为：

```python
@router.post("/data/import-from-local")
def trigger_local_import(
    db: Session = Depends(get_db),
    current_user=Depends(get_current_user),
):
    """从本地目录导入（仅管理员）。

    导入目录取自 .env 的 DATA_LOCAL_PATH（settings.data_local_path），
    与启动时的首次初始化导入共用同一来源，不在页面上手填。
    """
    if not getattr(current_user, "is_admin", False):
        raise HTTPException(403, detail="仅管理员可操作")

    local_path = settings.data_local_path
    if not local_path:
        raise HTTPException(
            400,
            detail="未在 .env 配置 DATA_LOCAL_PATH，请在 backend/.env 设置",
        )
    if not os.path.isdir(local_path):
        raise HTTPException(400, detail=f"目录不存在: {local_path}")

    task_id = start_background_import(
        db,
        "local_import",
        current_user.id,
        import_from_local_dir,
        db,
        local_path,
        current_user.id,
    )
    return {"task_id": task_id}
```

- [ ] **Step 3: 新增只读配置查询端点 `GET /data/local-path-config`**

紧接 `trigger_local_import` 函数之后（原 L122 `return {"task_id": task_id}` 的下一行、`# ---- 任务状态查询 ----` 注释块之前），插入新端点：

```python
@router.get("/data/local-path-config")
def get_local_path_config(
    current_user=Depends(get_current_user),
):
    """返回 .env 中 DATA_LOCAL_PATH 的配置情况（仅管理员）。

    供前端只读展示当前会导入哪个目录，不在此处校验目录是否存在
    （目录存在性校验交给导入端点负责）。
    """
    if not getattr(current_user, "is_admin", False):
        raise HTTPException(403, detail="仅管理员可操作")

    path = settings.data_local_path
    return {"configured": bool(path), "path": path}
```

- [ ] **Step 4: 后端语法检查**

在项目根目录执行（用根目录 `.venv` 的解释器，POSIX 写法，Bash 工具可跑）：

```bash
.venv/Scripts/python.exe -m py_compile backend/app/api/import_api.py
```

Expected: 无任何输出，退出码 0（表示语法 OK）。

- [ ] **Step 5: 手动核对（自动重载服务已起）**

用浏览器或 curl（带管理员 token）核对两个端点行为：

- `GET /api/v1/import/data/local-path-config` → 返回 `{"configured": true, "path": "D:/code/HowToCook_json"}`（按当前 .env）。
- 临时把 `.env` 的 `DATA_LOCAL_PATH` 注释掉重启后，`POST /api/v1/import/data/import-from-local` 应返回 400「未在 .env 配置 DATA_LOCAL_PATH…」。（核对完记得改回。）

> 若不便临时改 .env，至少核对 GET 端点返回值与 `.env` 一致即可。

---

## Task 2: 前端 — 删输入框、加只读展示、配置拉取、按钮联动

**Files:**
- Modify: `frontend/src/views/admin/DataMaintenanceView.vue`（template L58-104 卡片、script L495 `localPath`、L531 `onMounted`、L638-647 `importFromLocalPath`）

- [ ] **Step 1: 替换卡片内的输入框为只读展示区**

把 `frontend/src/views/admin/DataMaintenanceView.vue` 中 L66-87 的 `<v-card-text class="pt-6"> ... </v-card-text>` 整段：

```vue
          <v-card-text class="pt-6">
            <p class="text-body-2 mb-4">
              从服务器上的本地目录导入数据。目录结构可以是
              HowToCook_json 格式或系统导出格式（自动检测）。
            </p>
            <v-text-field
              v-model="localPath"
              label="本地目录绝对路径"
              variant="outlined"
              placeholder="/data/recipes/out"
              prepend-icon="mdi-folder-path"
              hint="输入服务器上的数据目录路径"
              persistent-hint
              :disabled="submitting.local"
            />
            <v-alert type="info" variant="tonal" class="mt-4" density="compact">
              <div class="text-caption">
                支持格式：HowToCook_json（ingredients.json + 菜谱 JSON）/<br />
                系统导出格式（manifest.json）
              </div>
            </v-alert>
          </v-card-text>
```

替换为：

```vue
          <v-card-text class="pt-6">
            <p class="text-body-2 mb-4">
              从服务器上的本地目录导入数据。目录路径取自配置文件
              <code>backend/.env</code> 的 <code>DATA_LOCAL_PATH</code>，
              与启动时导入共用同一来源（自动检测 HowToCook_json / 系统导出格式）。
            </p>
            <v-alert
              :type="localPathConfig.configured ? 'info' : 'warning'"
              variant="tonal"
              density="compact"
              class="mb-4"
            >
              <div v-if="!localPathConfig.loaded" class="text-caption">
                正在读取配置…
              </div>
              <div v-else-if="localPathConfig.configured" class="text-caption">
                将导入：<code>{{ localPathConfig.path }}</code>
              </div>
              <div v-else class="text-caption">
                未配置 <code>DATA_LOCAL_PATH</code>，请在
                <code>backend/.env</code> 设置后重启服务。
              </div>
            </v-alert>
            <v-alert type="info" variant="tonal" density="compact">
              <div class="text-caption">
                支持格式：HowToCook_json（ingredients.json + 菜谱 JSON）/<br />
                系统导出格式（manifest.json）
              </div>
            </v-alert>
          </v-card-text>
```

- [ ] **Step 2: 改「开始导入」按钮的 `:disabled` 绑定**

把同卡片内按钮（原 L91-101）的这一行：

```vue
              :disabled="!localPath.trim()"
```

改为：

```vue
              :disabled="!localPathConfig.loaded || !localPathConfig.configured"
```

（`:loading="submitting.local"` 与 `@click="importFromLocalPath"` 保持不变。）

- [ ] **Step 3: 把 `localPath` ref 换成 `localPathConfig` 响应式对象**

把 script 中 L495 的：

```ts
const localPath = ref('')
```

替换为（`reactive` 已在文件顶部 `from 'vue'` 导入，无需新增导入）：

```ts
// 本地导入路径配置（从后端 GET /import/data/local-path-config 读取，只读展示）
const localPathConfig = reactive({
  loaded: false,
  configured: false,
  path: '',
})
```

- [ ] **Step 4: 在 `onMounted` 中拉取配置**

在 `onMounted` 回调内（L531 起），`fetchTasks(10)` 那一行之后、`// 加载翻译配置` 注释之前，插入一个不阻塞后续的配置拉取块。即把：

```ts
onMounted(async () => {
  // 加载近期任务列表，恢复对运行中任务的轮询
  fetchTasks(10)

  // 加载翻译配置
```

改为：

```ts
onMounted(async () => {
  // 加载近期任务列表，恢复对运行中任务的轮询
  fetchTasks(10)

  // 加载本地导入路径配置（只读展示用，失败降级为「未配置」）
  api
    .get('/import/data/local-path-config')
    .then((cfg: any) => {
      localPathConfig.configured = !!cfg?.configured
      localPathConfig.path = cfg?.path || ''
    })
    .catch(() => {
      localPathConfig.configured = false
    })
    .finally(() => {
      localPathConfig.loaded = true
    })

  // 加载翻译配置
```

（`api` 已在文件顶部 `import api from '@/api/client'` 引入，无需新增导入。）

- [ ] **Step 5: 改 `importFromLocalPath` 不再传路径参数**

把 L638-647 的：

```ts
async function importFromLocalPath() {
  submitting.local = true
  const taskId = await startTask('/import/data/import-from-local', {
    params: { local_path: localPath.value.trim() },
  })
  if (!taskId) {
    errorMessage.value = '本地导入任务启动失败，请检查路径和后端状态'
  }
  submitting.local = false
}
```

替换为：

```ts
async function importFromLocalPath() {
  submitting.local = true
  const taskId = await startTask('/import/data/import-from-local')
  if (!taskId) {
    errorMessage.value = '本地导入任务启动失败，请检查后端状态与 DATA_LOCAL_PATH 配置'
  }
  submitting.local = false
}
```

（`startTask` 第二参 `axiosConfig` 有默认值 `{}`，不传即 POST 空体，无需改 `useImportTask.ts`。）

- [ ] **Step 6: 前端构建检查**

在 `frontend` 目录执行：

```bash
npm run build
```

Expected: 构建成功，无 TypeScript / 模板编译错误（退出码 0）。

- [ ] **Step 7: 手动核对（自动重载服务已起）**

浏览器打开数据维护中心页（管理员账号登录），核对「从本地路径导入」卡片：

- 输入框已消失，显示只读「将导入：`D:/code/HowToCook_json`」（按当前 .env），按钮可用。
- 点击「开始导入」，任务进入下方列表并轮询（与改造前行为一致，只是不再需要手填路径）。
- （可选）临时清空 `.env` 的 `DATA_LOCAL_PATH` 重启后刷新页面，应显示「未配置…」且按钮禁用。

---

## Self-Review（计划自检，供执行者参考）

- **Spec 覆盖**：后端两处（改 trigger + 新增 GET）→ Task 1 Step 2/3；前端删框/只读展示/按钮联动/配置拉取/函数去参 → Task 2 Step 1-5；错误处理（未配置 400 / 目录不存在 400 / 拉取失败降级）→ Task 1 Step 2 + Task 2 Step 4；service 与启动逻辑不动 → File Structure 表已声明。✓
- **类型/命名一致性**：后端 `settings.data_local_path`（来自 config.py L71，已存在）；前端 `localPathConfig.{loaded,configured,path}` 在 Step 3 定义、Step 1/2/4 消费，命名统一；GET 返回 `{configured, path}` 与前端读取字段一致。✓
- **无占位符**：所有 step 均含完整代码与精确行号锚点。✓
- **未改动项确认**：`useImportTask.ts` 的 `startTask(endpoint, axiosConfig={})` 默认参数使其无参调用合法；`inferTaskType` 仍能由 `import-from-local` 推断 `local_import`，任务列表展示不受影响。✓
