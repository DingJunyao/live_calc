# 存储配置后台化与个人信息编辑重组·实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: 用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 按任务逐个实现。步骤用 `- [ ]` 复选框跟踪。

**Goal:** 收尾 2026-07-20 当日「用户体系扩展与图片存储抽象」三阶段——把头像/地区入口收进「用户信息编辑」对话框、修复行政区划 seed（lifespan 移出条件块 + 数据维护中心更新模块）、图片存储配置 DB 化（后台向导式 local↔S3 切换 + 双向异步迁移）。

**Architecture:** 两个独立子任务可并行。子任务一：前端 ProfileView 对话框重组（单文件）+ 后端 lifespan 一行挪位 + 两管理端点 + DataMaintenanceView 一张卡片。子任务二：`StorageConfiguration` 表（对齐 MapConfiguration）+ factory 改读 effective 配置（DB→.env BOOTSTRAP_→默认，三层逐字段）+ 四管理 API + 异步双向迁移复用 `ImportTask` + 后台向导页。

**Tech Stack:** Python / FastAPI / SQLAlchemy / Alembic / boto3 / pytest；Vue 3 / Vuetify 3 / TypeScript / Vite。

**执行规范（项目约定，必读）：**
- 后端在 `backend/`，用项目根 `.venv`（`d:\code\live_calc\.venv`，非 `backend/.venv`）；`uv` 管依赖。跑测试用 `.venv/Scripts/python.exe -m pytest ...`。
- 表结构变更走三件套：alembic 迁移 + SQLite/MySQL/PostgreSQL 三引擎 SQL（PostGIS 无关不发）+ 开发库直接补（项目不走 alembic upgrade，`create_all` 建表后手动补 ALTER/INSERT）。开发库路径见 `backend/.env` 的 `DATABASE_URL`（当前 `backend/data/livecalc.db`）。
- 后端 TDD：先写失败测试 → 跑确认失败 → 实现 → 跑确认通过。前端无单测框架，验证靠 `npm run build` + 手动/edge-devtools 核验。
- 每步 `py_compile`（后端）/ `npm run build`（前端）必过。
- 不要自行启动服务（开发者已起自动重载的前后端）。
- **不要自行 git 提交**（本项目 [CLAUDE.md](../../../CLAUDE.md) 规矩）。每个 Task 末尾的 **Checkpoint** 是逻辑分组点 + 建议提交信息，执行时等用户确认后再提交。
- 设计依据：[spec](../specs/2026-07-20-存储配置后台化与头像重组-design.md)。

---

## 文件结构

### 新建
| 文件 | 职责 |
|---|---|
| `backend/app/models/storage_configuration.py` | `StorageConfiguration` 模型（单行结构化配置表） |
| `backend/alembic/versions/20260720_0002_storage_configurations.py` | 建表 alembic 迁移 |
| `backend/scripts/sql/20260720_storage_configurations_sqlite.sql` / `_mysql.sql` / `_postgres.sql` | 三引擎建表 SQL |
| `backend/app/services/storage/effective.py` | `load_effective_storage_config()` 三层合并 + `StorageEffectiveConfig` dataclass |
| `backend/app/api/storage_config.py` | 存储配置四管理 API（GET/PUT/test/migrate） |
| `backend/app/services/storage/migrate.py` | 双向迁移核心（`migrate_to_s3` / `migrate_from_s3`）+ `_run_migrate_task` |
| `backend/scripts/migrate_s3_to_local.py` | 反向迁移 CLI 兜底 |
| `frontend/src/views/admin/StorageConfigView.vue` | 后台「图片存储」向导页 |
| `backend/tests/services/storage/test_effective.py` | 三层优先级单测 |
| `backend/tests/services/storage/test_storage_config_api.py` | 四 API 单测 |
| `backend/tests/services/storage/test_migrate.py` | 双向迁移单测 |
| `backend/tests/services/test_region_admin.py` | 行政区划管理端点单测 |

### 修改
| 文件 | 改动 |
|---|---|
| `backend/app/main.py` | lifespan `ensure_administrative_regions` 移出 `first_run_init_recipes` 条件块；注册 storage_config 路由 |
| `backend/app/api/regions.py` | 加 `GET /admin/regions/seed-status` + `POST /admin/regions/seed` |
| `backend/app/config.py` | 8 个存储字段加 `bootstrap_` 前缀 |
| `backend/.env.example` | 存储变量改 `BOOTSTRAP_` 前缀 + 注释 |
| `backend/app/services/storage/__init__.py` | 导出 `load_effective_storage_config` |
| `backend/app/services/storage/factory.py` | `_build` 改读 effective 配置 |
| `backend/scripts/migrate_local_to_s3.py` | 读 `settings.bootstrap_s3_*`；`_migrate_all` 加 `progress_callback` |
| `frontend/src/views/profile/ProfileView.vue` | 删顶部头像卡 + 删独立地区对话框 + 头像/地区搬进 accountDialog + 昵称去图标 + 第一级文案 |
| `frontend/src/views/admin/DataMaintenanceView.vue` | 新增「行政区划」卡片 + 任务列表 `storage_migrate` 分支 |
| `frontend/src/router/index.ts` | 加 `/admin/storage` 路由 |
| `frontend/src/components/layout/AppLayout.vue`（或后台导航所在文件） | 加「图片存储」导航项 |

---

# 子任务一·个人信息编辑重组 + 地区 seed

## Task 1: 后端·地区 seed 修复（lifespan 移出条件块）

**Files:**
- Modify: `backend/app/main.py:225-263`
- Test: `backend/tests/services/test_region_seed_lifespan.py`

**Context:** spec §3.2。`ensure_administrative_regions(db)` 当前在 `first_run_init_recipes` 的 `else` 块内（[main.py:259-263](../../../backend/app/main.py#L259-L263)），已有菜谱的库（非首次）整个 else 跳过、seed 永不执行 → `administrative_regions` 表空 → 前端级联 No data。行政区划是纯地理基础数据，不依赖菜谱，须无条件 ensure。`ensure_administrative_regions` 本身幂等（count>0 跳过），见 [region_seed.py:322](../../../backend/app/services/region_seed.py#L322)。

- [ ] **Step 1: 写失败测试**

```python
# backend/tests/services/test_region_seed_lifespan.py
"""验证 ensure_administrative_regions 不被 first_run_init_recipes 条件卡住。

用静态分析断言 main.py 里 ensure_administrative_regions 的调用缩进
与 first_run 的 if/else 同级（无条件执行），而非嵌在 else 内。
"""
from pathlib import Path

MAIN_PY = Path(__file__).resolve().parents[2] / "app" / "main.py"


def _indent(line: str) -> int:
    return len(line) - len(line.lstrip(" "))


def test_ensure_administrative_regions_not_nested_in_first_run_else():
    src = MAIN_PY.read_text(encoding="utf-8")
    lines = src.splitlines()

    # 定位 first_run_init_recipes 的 if 与 else
    if_line = next(i for i, ln in enumerate(lines) if "settings.first_run_init_recipes" in ln and "if " in ln)
    # ensure_administrative_regions 调用行
    ensure_line = next(i for i, ln in enumerate(lines) if "ensure_administrative_regions(db)" in ln)

    assert ensure_line > if_line, "ensure 调用应在 first_run 判断之后"
    # ensure 所在块缩进须 ≤ if 缩进（同级或更外层），不能在 else 内（更深）
    assert _indent(lines[ensure_line]) <= _indent(lines[if_line]), (
        "ensure_administrative_regions 须在 first_run if/else 之外无条件执行，"
        "当前嵌在条件块内——非首次启动库会跳过 seed"
    )
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/Scripts/python.exe -m pytest backend/tests/services/test_region_seed_lifespan.py -v`
Expected: FAIL（ensure 当前缩进 16 > if 缩进 8）

- [ ] **Step 3: 实现——把 ensure 块移出条件块**

在 [main.py:225-263](../../../backend/app/main.py#L225-L263) 区域，把第 257-263 行的 `ensure_administrative_regions` try/except 块从 `else:` 内挪到 `if/else` 之外（与第 225 行 `if not settings.first_run_init_recipes:` 同级缩进，即 8 空格），放在整个 first_run `if/else` 块之后。`ensure_allergen_groups`（251-255）保持原位不动（依赖原料，留 else 内合理）。

挪后形如：
```python
        if not settings.first_run_init_recipes:
            logger.info("FIRST_RUN_INIT_RECIPES=false，跳过启动导入")
        else:
            imported_count = db.query(Recipe).filter(Recipe.source == "json_repo").count()
            if imported_count > 0:
                logger.info(f"初始数据已导入（{imported_count} 条菜谱），跳过启动导入")
            else:
                # ... 本地/远程导入 ...

            # 确保过敏原黑名单分组存在（仅在启动导入时执行...）
            try:
                from app.services.allergen_seed import ensure_allergen_groups
                ensure_allergen_groups(db)
            except Exception as e:
                logger.warning(f"过敏原分组初始化失败: {e}")

        # 确保行政区划数据存在（纯地理基础数据，与菜谱无关，无条件执行；
        # 幂等 count>0 跳过，失败不阻断启动）
        try:
            from app.services.region_seed import ensure_administrative_regions
            ensure_administrative_regions(db)
        except Exception as e:
            logger.warning(f"行政区划数据初始化失败: {e}")
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/Scripts/python.exe -m pytest backend/tests/services/test_region_seed_lifespan.py -v`
Expected: PASS

- [ ] **Step 5: py_compile + 当前开发库补数据**

Run: `.venv/Scripts/python.exe -m py_compile backend/app/main.py`
然后手动触发一次 seed 验证（当前库表空）:
```bash
.venv/Scripts/python.exe -c "from app.core.database import SessionLocal; from app.services.region_seed import upsert_administrative_regions; db=SessionLocal(); print(upsert_administrative_regions(db)); db.close()"
```
Expected: `{created: ~3700, skipped: 0, errors: 0}`（联网拉数据；失败检查网络/缓存）

- [ ] **Step 6: Checkpoint**

建议提交：`fix(region): ensure_administrative_regions 移出 first_run 条件块（修复非首次库 No data）`

---

## Task 2: 后端·行政区划管理端点（GET seed-status + POST seed）

**Files:**
- Modify: `backend/app/api/regions.py`（末尾追加）
- Test: `backend/tests/services/test_region_admin.py`

**Context:** spec §3.3。对齐 [黑名单分组 `seed-allergens` 端点](../../../backend/app/api/blacklist_groups.py) 范式。service 层 [`upsert_administrative_regions`](../../../backend/app/services/region_seed.py#L252) / [`need_region_seed`](../../../backend/app/services/region_seed.py#L343) 已就绪。

- [ ] **Step 1: 写失败测试**

```python
# backend/tests/services/test_region_admin.py
from fastapi.testclient import TestClient
from app.main import app


def test_seed_status_empty(monkeypatch):
    """空库时 needed=True，各级 count=0。"""
    from app.services import region_seed
    monkeypatch.setattr(region_seed, "need_region_seed", lambda db: True)
    # 用独立 app 避免污染
    client = TestClient(app)
    # 需管理员 token——测试里直接调内部函数验证逻辑
    from app.api import regions as regions_api
    # 直接验证端点函数逻辑（绕过鉴权用 db 注入）
    from app.core.database import SessionLocal
    db = SessionLocal()
    try:
        # 模拟 status 构造逻辑
        from app.models.administrative_region import AdministrativeRegion
        counts = {lv: db.query(AdministrativeRegion).filter(
            AdministrativeRegion.level == lv,
            AdministrativeRegion.is_active == True,
        ).count() for lv in range(4)}
        assert counts is not None
    finally:
        db.close()


def test_seed_endpoint_calls_upsert(monkeypatch):
    """POST /admin/regions/seed 调 upsert_administrative_regions，返 created/skipped/errors。"""
    called = {}
    from app.services import region_seed
    def fake_upsert(db):
        called["yes"] = True
        return {"created": 10, "skipped": 2, "errors": 0}
    monkeypatch.setattr(region_seed, "upsert_administrative_regions", fake_upsert)
    # 端点鉴权用 get_current_admin_user——测试用 monkeypatch 绕过
    from app.core import security
    monkeypatch.setattr(security, "get_current_admin_user", lambda: type("U", (), {"id": 1, "is_admin": True})())
    client = TestClient(app)
    r = client.post("/api/v1/admin/regions/seed")
    assert r.status_code == 200
    assert r.json()["created"] == 10
    assert called.get("yes") is True
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/Scripts/python.exe -m pytest backend/tests/services/test_region_admin.py -v`
Expected: FAIL（端点不存在，404）

- [ ] **Step 3: 实现——在 regions.py 末尾加端点**

```python
# backend/app/api/regions.py 末尾追加
from app.core.security import get_current_admin_user
from app.models.user import User
from app.services.region_seed import upsert_administrative_regions, need_region_seed


@router.get("/admin/regions/seed-status")
def region_seed_status(
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_admin_user),
):
    """行政区划数据状态：各级数量 + 是否需要 seed。仅管理员。"""
    counts = {}
    for lv in range(4):
        counts[lv] = (
            db.query(AdministrativeRegion)
            .filter(
                AdministrativeRegion.level == lv,
                AdministrativeRegion.is_active == True,
            )
            .count()
        )
    return {
        "counts": counts,
        "needed": need_region_seed(db),
        "total": sum(counts.values()),
    }


@router.post("/admin/regions/seed")
def region_seed_run(
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_admin_user),
):
    """手动 upsert 行政区划数据（更新/补缺失）。仅管理员。同步执行。"""
    result = upsert_administrative_regions(db)
    return result
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/Scripts/python.exe -m pytest backend/tests/services/test_region_admin.py -v`
Expected: PASS

- [ ] **Step 5: py_compile**

Run: `.venv/Scripts/python.exe -m py_compile backend/app/api/regions.py`

- [ ] **Step 6: Checkpoint**

建议提交：`feat(region): GET /admin/regions/seed-status + POST /admin/regions/seed（数据维护中心更新入口）`

---

## Task 3: 前端·ProfileView 对话框重组（头像 + 地区 + 昵称图标 + 文案）

**Files:**
- Modify: `frontend/src/views/profile/ProfileView.vue`（顶部卡 53-88 / accountDialog 242-301 / regionDialog 509-589 / 副标题 98 / 昵称框 262 / 国家 label 527 / 脚本逻辑）

**Context:** spec §3.1。前端单文件，后端零改动。头像复用 `triggerAvatarUpload`/`uploadAvatar`/`avatarSrc`/`avatarUploading`/`avatarFileInputRef`（逻辑零改动）；地区复用 `regionSelections`/`regionItems`/`regionLoading`/`loadRegionLevel`/`onRegionChange`/`regionPathLabel`/`resetRegionDialog`/`saveRegion`。

- [ ] **Step 1: 删顶部独立头像卡**

删除 [ProfileView.vue:53-88](../../../frontend/src/views/profile/ProfileView.vue#L53-L88) 整个「头像上传」`<v-card>` 块。

- [ ] **Step 2: accountDialog 顶部加头像区**

在 [accountDialog 的 `<v-card-text>` 内、`<div class="text-caption">基本信息</div>` 之前](../../../frontend/src/views/profile/ProfileView.vue#L250) 插入头像区（复用现有 v-avatar + 隐藏 v-file-input 结构，从顶部卡搬来）：

```vue
<div class="d-flex flex-column align-center mb-3">
  <v-avatar size="80" class="cursor-pointer mb-1" @click="triggerAvatarUpload">
    <template v-if="userStore.user?.avatar">
      <v-img :src="avatarSrc" cover />
    </template>
    <v-icon v-else size="48">mdi-account</v-icon>
  </v-avatar>
  <v-file-input
    ref="avatarFileInputRef"
    accept="image/jpeg,image/png,image/gif,image/webp"
    hide-input hide-details density="compact"
    class="d-none"
    @update:model-value="uploadAvatar"
  />
  <div class="text-caption text-medium-emphasis cursor-pointer" @click="triggerAvatarUpload">
    <template v-if="avatarUploading">
      <v-progress-circular indeterminate size="16" width="2" class="mr-1" />上传中…
    </template>
    <template v-else>点击更换头像</template>
  </div>
</div>
```

- [ ] **Step 3: 昵称框去 prepend-icon**

[第 262 行](../../../frontend/src/views/profile/ProfileView.vue#L262) 删 `prepend-icon="mdi-account-edit"`（与用户名框对称）。

- [ ] **Step 4: accountDialog 加地区级联**

在 [accountDialog 手机号字段之后、修改密码按钮之前](../../../frontend/src/views/profile/ProfileView.vue#L284) 插入地区级联（把 regionDialog 的 4 个 v-select 搬来，第一级 label 改「国家/地区」）：

```vue
<div class="text-caption text-medium-emphasis mb-2 mt-2">所在地区</div>
<div class="d-flex flex-wrap ga-2 mb-2">
  <v-select
    v-model="regionSelections[0]"
    :items="regionItems[0]" item-title="name" item-value="id"
    label="国家/地区" variant="outlined" density="compact"
    class="region-select flex-grow-1" :loading="regionLoading[0]"
    hide-details="auto" clearable
    @update:model-value="onRegionChange(0)"
  />
  <v-select
    v-if="regionSelections[0] && regionItems[1].length >= 0"
    v-model="regionSelections[1]"
    :items="regionItems[1]" item-title="name" item-value="id"
    label="省/州" variant="outlined" density="compact"
    class="region-select flex-grow-1" :loading="regionLoading[1]"
    hide-details="auto" clearable
    @update:model-value="onRegionChange(1)"
  />
  <v-select
    v-if="regionSelections[1] && regionItems[2].length >= 0"
    v-model="regionSelections[2]"
    :items="regionItems[2]" item-title="name" item-value="id"
    label="城市" variant="outlined" density="compact"
    class="region-select flex-grow-1" :loading="regionLoading[2]"
    hide-details="auto" clearable
    @update:model-value="onRegionChange(2)"
  />
  <v-select
    v-if="regionSelections[2] && regionItems[3].length >= 0"
    v-model="regionSelections[3]"
    :items="regionItems[3]" item-title="name" item-value="id"
    label="区/县" variant="outlined" density="compact"
    class="region-select flex-grow-1" :loading="regionLoading[3]"
    hide-details="auto" clearable
    @update:model-value="onRegionChange(3)"
  />
</div>
```

- [ ] **Step 5: 删独立 regionDialog + 设置列表「地区」项**

删除 [regionDialog 整块 509-589](../../../frontend/src/views/profile/ProfileView.vue#L509-L589)。删除设置列表里的「地区」`<v-list-item>`（[第 182 行附近](../../../frontend/src/views/profile/ProfileView.vue#L182)，含 `regionPathLabel`）。

- [ ] **Step 6: 副标题补字**

[第 98 行](../../../frontend/src/views/profile/ProfileView.vue#L98) 副标题「用户名、昵称、邮箱、密码」→「头像、用户名、昵称、邮箱、地区、密码」。

- [ ] **Step 7: 脚本逻辑——openAccountDialog 初始化地区**

`openAccountDialog`（[915 行](../../../frontend/src/views/profile/ProfileView.vue#L915)）打开对话框时初始化地区：加载第一级 + 回填用户当前 region。在 `accountDialog.value = true` 前加：
```ts
// 加载第一级（若未加载）
if (regionItems.value[0].length === 0) {
  await loadRegionLevel(0)
}
// 回填用户当前 region（若有 region_id，解析祖先链回填各级）
const u = userStore.user as any
if (u?.region_id) {
  await restoreRegionSelections(u.region_id)  // 见下
}
```
若 `restoreRegionSelections` 不存在，新增：调 `GET /regions/{id}` 拿 ancestors 链，逐级回填 `regionSelections` + `loadRegionLevel` 子级。

- [ ] **Step 8: 脚本逻辑——saveAccount 保存地区**

`saveAccount`（[933 行](../../../frontend/src/views/profile/ProfileView.vue#L933)）在 PUT `/auth/me/account` 成功后，若地区有变化，调现有 `saveRegion()` 逻辑（保存最深一级 region_id）。具体：在 `await api.put('/auth/me/account', payload)` 之后加：
```ts
// 保存地区（若选择了最深一级）
const deepestRegionId = [...regionSelections.value].reverse().find(v => v !== null)
if (deepestRegionId !== undefined) {
  await saveRegionInternal(deepestRegionId)  // 抽取 saveRegion 的核心保存逻辑
}
```
`saveRegion` 现有逻辑（保存 region_id 到用户）保留，抽核心为 `saveRegionInternal(regionId)` 复用。

- [ ] **Step 9: build 通过**

Run（在 `frontend/`）: `npm run build`
Expected: 构建成功（precache entries 数变化不大）

- [ ] **Step 10: 手动核验**

浏览器开 `/profile`：① 顶部无独立头像卡 ② 点「用户信息编辑」对话框：顶部头像可点换、昵称框无图标、含地区级联、第一级 label「国家/地区」③ 选地区各级有数据（Task 1/2 已 seed）④ 保存头像/昵称/地区均生效。

- [ ] **Step 11: Checkpoint**

建议提交：`refactor(frontend): ProfileView 头像/地区收进用户信息编辑对话框 + 昵称去图标 + 国家/地区文案`

---

## Task 4: 前端·DataMaintenanceView 行政区划模块

**Files:**
- Modify: `frontend/src/views/admin/DataMaintenanceView.vue`

**Context:** spec §3.3。对齐同文件 USDA 数据管理卡片结构（`<v-card>` + 标题 + 统计 + 按钮）。后端端点 Task 2 已就绪。

- [ ] **Step 1: 加 API 调用封装**

在 DataMaintenanceView 脚本区加：
```ts
const regionStatus = ref<{ counts: Record<number, number>; needed: boolean; total: number } | null>(null)
const regionSeeding = ref(false)

async function loadRegionStatus() {
  try {
    const data = await api.get('/admin/regions/seed-status')
    regionStatus.value = data
  } catch (e: any) { /* 忽略，非致命 */ }
}

async function seedRegions() {
  regionSeeding.value = true
  try {
    const res = await api.post('/admin/regions/seed')
    notify(`行政区划更新完成：新建 ${res.created}，跳过 ${res.skipped}`, 'success')
    await loadRegionStatus()
  } catch (e: any) {
    notify('更新失败：' + (e?.userMessage || e?.message || '未知错误'), 'error')
  } finally {
    regionSeeding.value = false
  }
}

onMounted(() => { loadRegionStatus() /* 与现有 onMounted 合并 */ })
```

- [ ] **Step 2: 加「行政区划」卡片**

在合适位置（如 USDA 卡片之后）加卡片，对齐 USDA 卡片结构：
```vue
<!-- 行政区划 -->
<v-card class="mb-4" elevation="1">
  <v-card-title class="d-flex align-center py-4">
    <v-icon class="mr-2" color="blue">mdi-map-marker-multiple</v-icon>
    行政区划
  </v-card-title>
  <v-card-text>
    <v-alert v-if="regionStatus?.needed" type="warning" variant="tonal" density="compact" class="mb-3">
      行政区划数据缺失，用户级联选择器将无法使用
    </v-alert>
    <div class="d-flex flex-wrap ga-3 mb-3 text-body-2">
      <v-chip size="small">国家/地区：{{ regionStatus?.counts?.[0] ?? '--' }}</v-chip>
      <v-chip size="small">省/州：{{ regionStatus?.counts?.[1] ?? '--' }}</v-chip>
      <v-chip size="small">城市：{{ regionStatus?.counts?.[2] ?? '--' }}</v-chip>
      <v-chip size="small">区/县：{{ regionStatus?.counts?.[3] ?? '--' }}</v-chip>
      <v-chip size="small" color="primary">总计：{{ regionStatus?.total ?? '--' }}</v-chip>
    </div>
    <v-btn color="primary" :loading="regionSeeding" prepend-icon="mdi-database-refresh" @click="seedRegions">
      {{ regionStatus?.needed ? '导入行政区划数据' : '更新行政区划数据' }}
    </v-btn>
  </v-card-text>
</v-card>
```

- [ ] **Step 3: build 通过**

Run: `npm run build`（在 `frontend/`）

- [ ] **Step 4: 手动核验**

浏览器开后台「数据维护中心」：见「行政区划」卡片，统计显示各级数量，点「更新」loading 后数量刷新。

- [ ] **Step 5: Checkpoint**

建议提交：`feat(frontend): 数据维护中心加行政区划模块（状态 + 更新按钮）`

---

# 子任务二·图片存储配置后台化

## Task 5: 后端·StorageConfiguration 模型 + 迁移三件套

**Files:**
- Create: `backend/app/models/storage_configuration.py`
- Create: `backend/alembic/versions/20260720_0002_storage_configurations.py`
- Create: `backend/scripts/sql/20260720_storage_configurations_sqlite.sql` / `_mysql.sql` / `_postgres.sql`
- Modify: 开发库 `backend/data/livecalc.db`（手动建表）

**Context:** spec §4.2。对齐 [`MapConfiguration`](../../../backend/app/models/map_config.py) 范式。单行结构化表，首次为空（全回落 .env）。

- [ ] **Step 1: 写模型**

```python
# backend/app/models/storage_configuration.py
import sqlalchemy as sa
from sqlalchemy import Column, Integer, String, DateTime
from datetime import datetime
from app.core.database import Base


class StorageConfiguration(Base):
    """图片存储配置（单行）。DB 字段非空则覆盖 .env BOOTSTRAP_*。"""
    __tablename__ = "storage_configurations"
    id = Column(Integer, primary_key=True, index=True)
    backend = Column(String(10), nullable=False, default="local")        # local | s3
    storage_base_url = Column(String(255), nullable=True)
    s3_endpoint = Column(String(255), nullable=True)
    s3_access_key = Column(String(255), nullable=True)
    s3_secret_key = Column(String(255), nullable=True)
    s3_bucket = Column(String(255), nullable=True)
    s3_region = Column(String(64), nullable=True)
    s3_url_style = Column(String(10), nullable=False, default="path")    # path | virtual
    created_at = Column(DateTime, default=datetime.utcnow)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
```

- [ ] **Step 2: 注册模型**

在 `backend/app/models/__init__.py` 加 `from app.models.storage_configuration import StorageConfiguration` + 加入 `__all__`（对齐现有模式，确保 `create_all` 能建表）。

- [ ] **Step 3: alembic 迁移**

```python
# backend/alembic/versions/20260720_0002_storage_configurations.py
"""add storage_configurations table"""
from alembic import op
import sqlalchemy as sa

revision = "20260720_0002"
down_revision = None  # 按项目最新 revision 链接，执行时核对
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.create_table(
        "storage_configurations",
        sa.Column("id", sa.Integer(), primary_key=True, autoincrement=True),
        sa.Column("backend", sa.String(10), nullable=False, server_default="local"),
        sa.Column("storage_base_url", sa.String(255), nullable=True),
        sa.Column("s3_endpoint", sa.String(255), nullable=True),
        sa.Column("s3_access_key", sa.String(255), nullable=True),
        sa.Column("s3_secret_key", sa.String(255), nullable=True),
        sa.Column("s3_bucket", sa.String(255), nullable=True),
        sa.Column("s3_region", sa.String(64), nullable=True),
        sa.Column("s3_url_style", sa.String(10), nullable=False, server_default="path"),
        sa.Column("created_at", sa.DateTime(), nullable=True),
        sa.Column("updated_at", sa.DateTime(), nullable=True),
    )


def downgrade() -> None:
    op.drop_table("storage_configurations")
```

- [ ] **Step 4: 三引擎 SQL**

`backend/scripts/sql/20260720_storage_configurations_sqlite.sql`:
```sql
CREATE TABLE IF NOT EXISTS storage_configurations (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  backend TEXT NOT NULL DEFAULT 'local',
  storage_base_url TEXT,
  s3_endpoint TEXT,
  s3_access_key TEXT,
  s3_secret_key TEXT,
  s3_bucket TEXT,
  s3_region TEXT,
  s3_url_style TEXT NOT NULL DEFAULT 'path',
  created_at DATETIME,
  updated_at DATETIME
);
```
MySQL 版：`id INT AUTO_INCREMENT PRIMARY KEY`、`VARCHAR(n)`、`DATETIME`。Postgres 版：`SERIAL`/`TEXT`/`TIMESTAMP`。三份各自引擎语法。

- [ ] **Step 5: 开发库直补**

备份 `backend/data/livecalc.db` → `.bak_20260720_storage`，跑 sqlite SQL 建表（或重启后端让 `create_all` 建——Step 2 注册后 create_all 自动建）。

- [ ] **Step 6: py_compile + 验证建表**

Run: `.venv/Scripts/python.exe -m py_compile backend/app/models/storage_configuration.py backend/app/models/__init__.py`
验证表存在:
```bash
.venv/Scripts/python.exe -c "import sqlite3; c=sqlite3.connect('backend/data/livecalc.db'); print([r for r in c.execute(\"SELECT name FROM sqlite_master WHERE name='storage_configurations'\")])"
```
Expected: `[('storage_configurations',)]`

- [ ] **Step 7: Checkpoint**

建议提交：`feat(storage): StorageConfiguration 模型 + 迁移三件套`

---

## Task 6: 后端·config.py BOOTSTRAP_ 改名 + .env.example

**Files:**
- Modify: `backend/app/config.py:60-72`
- Modify: `backend/.env.example:49-62`

**Context:** spec §4.3。8 个存储字段加 `bootstrap_` 前缀，明确「初始化引导用」。factory 改造后（Task 7）这些字段仅在 `load_effective_storage_config()` 内作 fallback 引用一次。

- [ ] **Step 1: config.py 字段改名**

[config.py:60-72](../../../backend/app/config.py#L60-L72) 8 个字段加 `bootstrap_` 前缀：
```python
    # 图片存储（仅初始化 / 首次启动 / DB 未配置时生效；后台「图片存储」页配置后优先级更高）
    bootstrap_storage_backend: Literal["local", "s3"] = "local"
    bootstrap_storage_base_url: str = ""
    bootstrap_s3_endpoint: str = ""
    bootstrap_s3_access_key: str = ""
    bootstrap_s3_secret_key: str = ""
    bootstrap_s3_bucket: str = ""
    bootstrap_s3_region: str = ""
    bootstrap_s3_url_style: Literal["path", "virtual"] = "path"
```

- [ ] **Step 2: .env.example 改名 + 注释**

[.env.example:49-62](../../../backend/.env.example#L49-L62) 8 个变量加 `BOOTSTRAP_` 前缀，开头注释强调：
```env
# 图片存储（仅初始化 / 首次启动 / DB 未配置时生效；后台「图片存储」页配置后优先级更高）
# BOOTSTRAP_STORAGE_BACKEND: local=本地磁盘（默认）, s3=S3 兼容对象存储（OSS/COS/MinIO 等）
BOOTSTRAP_STORAGE_BACKEND=local
BOOTSTRAP_STORAGE_BASE_URL=
BOOTSTRAP_S3_ENDPOINT=
BOOTSTRAP_S3_ACCESS_KEY=
BOOTSTRAP_S3_SECRET_KEY=
BOOTSTRAP_S3_BUCKET=
BOOTSTRAP_S3_REGION=
BOOTSTRAP_S3_URL_STYLE=path
```

- [ ] **Step 3: migrate_local_to_s3.py 读新名**

[scripts/migrate_local_to_s3.py:102-123](../../../backend/scripts/migrate_local_to_s3.py#L102-L123) 把 `settings.s3_*` 改 `settings.bootstrap_s3_*`，`settings.s3_url_style` 改 `settings.bootstrap_s3_url_style`，`missing` 列表里的 `S3_*` 改 `BOOTSTRAP_S3_*`。

- [ ] **Step 4: py_compile + grep 残留**

Run: `.venv/Scripts/python.exe -m py_compile backend/app/config.py backend/scripts/migrate_local_to_s3.py`
Grep 确认无残留旧名（除注释）:
```bash
grep -rn "settings\.s3_\|settings\.storage_backend\|settings\.storage_base_url" backend/app backend/scripts --include="*.py"
```
Expected: 空（factory.py 的引用在 Task 7 一并改）

- [ ] **Step 5: 更新开发机 .env（手动）**

提醒开发者：本机 `backend/.env` 里若有 `STORAGE_*`/`S3_*`，手动加 `BOOTSTRAP_` 前缀（默认 local 不受影响）。

- [ ] **Step 6: Checkpoint**

建议提交：`refactor(storage): .env 存储变量加 BOOTSTRAP_ 前缀（明确初始化语义）`

---

## Task 7: 后端·factory 三层优先级改造（load_effective_storage_config）

**Files:**
- Create: `backend/app/services/storage/effective.py`
- Modify: `backend/app/services/storage/__init__.py`
- Modify: `backend/app/services/storage/factory.py`
- Test: `backend/tests/services/storage/test_effective.py`

**Context:** spec §4.1、§4.4。DB → .env(BOOTSTRAP_*) → 代码默认，逐字段判定 source。保留 `@lru_cache` 单例 + boto3 懒加载。

- [ ] **Step 1: 写失败测试**

```python
# backend/tests/services/storage/test_effective.py
from dataclasses import dataclass
from unittest.mock import patch, MagicMock


def test_effective_db_overrides_env(monkeypatch):
    """DB 字段非空时覆盖 .env。"""
    from app.services.storage.effective import load_effective_storage_config
    # mock DB 返 s3 配置
    fake_row = MagicMock(
        backend="s3", storage_base_url=None,
        s3_endpoint="https://db.example.com", s3_access_key="db-ak",
        s3_secret_key="db-sk", s3_bucket="db-bucket", s3_region="db-r",
        s3_url_style="virtual",
    )
    with patch("app.services.storage.effective._query_db_row", return_value=fake_row):
        cfg = load_effective_storage_config()
    assert cfg.backend == "s3"
    assert cfg.s3_endpoint == "https://db.example.com"
    assert cfg.sources["s3_endpoint"] == "db"


def test_effective_falls_back_to_env(monkeypatch):
    """DB 字段为空时回落 .env。"""
    from app.services.storage.effective import load_effective_storage_config
    fake_row = MagicMock(backend=None, storage_base_url=None, s3_endpoint=None,
                         s3_access_key=None, s3_secret_key=None, s3_bucket=None,
                         s3_region=None, s3_url_style=None)
    with patch("app.services.storage.effective._query_db_row", return_value=fake_row), \
         patch("app.services.storage.effective.settings") as s:
        s.bootstrap_storage_backend = "s3"
        s.bootstrap_s3_endpoint = "https://env.example.com"
        s.bootstrap_storage_base_url = ""
        s.bootstrap_s3_access_key = ""; s.bootstrap_s3_secret_key = ""
        s.bootstrap_s3_bucket = ""; s.bootstrap_s3_region = ""
        s.bootstrap_s3_url_style = "path"
        cfg = load_effective_storage_config()
    assert cfg.backend == "s3"
    assert cfg.s3_endpoint == "https://env.example.com"
    assert cfg.sources["s3_endpoint"] == "env"


def test_effective_default_when_all_empty(monkeypatch):
    """DB 与 .env 都空时用代码默认。"""
    from app.services.storage.effective import load_effective_storage_config
    fake_row = MagicMock(backend=None, storage_base_url=None, s3_endpoint=None,
                         s3_access_key=None, s3_secret_key=None, s3_bucket=None,
                         s3_region=None, s3_url_style=None)
    with patch("app.services.storage.effective._query_db_row", return_value=fake_row), \
         patch("app.services.storage.effective.settings") as s:
        s.bootstrap_storage_backend = "local"; s.bootstrap_storage_base_url = ""
        s.bootstrap_s3_endpoint = ""; s.bootstrap_s3_access_key = ""
        s.bootstrap_s3_secret_key = ""; s.bootstrap_s3_bucket = ""
        s.bootstrap_s3_region = ""; s.bootstrap_s3_url_style = "path"
        cfg = load_effective_storage_config()
    assert cfg.backend == "local"
    assert cfg.sources["backend"] == "default"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/Scripts/python.exe -m pytest backend/tests/services/storage/test_effective.py -v`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现 effective.py**

```python
# backend/app/services/storage/effective.py
"""存储配置三层合并：DB → .env(BOOTSTRAP_*) → 代码默认，逐字段判定 source。"""
from dataclasses import dataclass, field
from typing import Optional
from app.config import settings

# 代码默认值
_DEFAULTS = {
    "backend": "local",
    "storage_base_url": "",
    "s3_endpoint": "", "s3_access_key": "", "s3_secret_key": "",
    "s3_bucket": "", "s3_region": "", "s3_url_style": "path",
}

# DB 字段名 → settings 字段名
_ENV_MAP = {
    "backend": "bootstrap_storage_backend",
    "storage_base_url": "bootstrap_storage_base_url",
    "s3_endpoint": "bootstrap_s3_endpoint",
    "s3_access_key": "bootstrap_s3_access_key",
    "s3_secret_key": "bootstrap_s3_secret_key",
    "s3_bucket": "bootstrap_s3_bucket",
    "s3_region": "bootstrap_s3_region",
    "s3_url_style": "bootstrap_s3_url_style",
}


@dataclass
class StorageEffectiveConfig:
    backend: str
    storage_base_url: Optional[str]
    s3_endpoint: Optional[str]
    s3_access_key: Optional[str]
    s3_secret_key: Optional[str]
    s3_bucket: Optional[str]
    s3_region: Optional[str]
    s3_url_style: str
    sources: dict = field(default_factory=dict)   # 字段名 → db/env/default


def _query_db_row():
    """读 storage_configurations 单行；无表/异常返 None（不阻断启动）。"""
    try:
        from app.core.database import SessionLocal
        from app.models.storage_configuration import StorageConfiguration
        db = SessionLocal()
        try:
            return db.query(StorageConfiguration).first()
        finally:
            db.close()
    except Exception:
        return None


def load_effective_storage_config() -> StorageEffectiveConfig:
    """逐字段三层合并 DB→.env→默认，返回带 source 标注的配置。"""
    row = _query_db_row()
    resolved = {}
    sources = {}
    for fname, env_name in _ENV_MAP.items():
        db_val = getattr(row, fname, None) if row else None
        if db_val not in (None, ""):
            resolved[fname] = db_val
            sources[fname] = "db"
            continue
        env_val = getattr(settings, env_name, None)
        if env_val not in (None, ""):
            resolved[fname] = env_val
            sources[fname] = "env"
            continue
        resolved[fname] = _DEFAULTS[fname]
        sources[fname] = "default"
    return StorageEffectiveConfig(
        backend=resolved["backend"],
        storage_base_url=resolved["storage_base_url"],
        s3_endpoint=resolved["s3_endpoint"],
        s3_access_key=resolved["s3_access_key"],
        s3_secret_key=resolved["s3_secret_key"],
        s3_bucket=resolved["s3_bucket"],
        s3_region=resolved["s3_region"],
        s3_url_style=resolved["s3_url_style"],
        sources=sources,
    )
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/Scripts/python.exe -m pytest backend/tests/services/storage/test_effective.py -v`
Expected: PASS

- [ ] **Step 5: factory 改读 effective**

```python
# backend/app/services/storage/factory.py（替换 _build）
from functools import lru_cache
from pathlib import Path
from app.services.storage.base import StorageBackend
from app.services.storage.local import LocalBackend
from app.services.storage.effective import load_effective_storage_config

_STATIC_ROOT = Path(__file__).resolve().parents[3] / "static"


@lru_cache(maxsize=1)
def _build() -> StorageBackend:
    cfg = load_effective_storage_config()
    if cfg.backend == "s3":
        from app.services.storage.s3 import S3Backend   # 懒加载 boto3
        return S3Backend(
            endpoint=cfg.s3_endpoint, bucket=cfg.s3_bucket,
            access_key=cfg.s3_access_key, secret_key=cfg.s3_secret_key,
            region=cfg.s3_region, url_style=cfg.s3_url_style,
        )
    return LocalBackend(root=_STATIC_ROOT,
                        base_url=cfg.storage_base_url or "/api/v1/static")


def get_storage() -> StorageBackend:
    return _build()


def reset_storage() -> None:
    """写 DB 配置后调，清 lru_cache 重建（热生效）。"""
    _build.cache_clear()
```

`__init__.py` 导出 `load_effective_storage_config`。

- [ ] **Step 6: py_compile + 回归现有 storage 测试**

Run: `.venv/Scripts/python.exe -m py_compile backend/app/services/storage/effective.py backend/app/services/storage/factory.py`
Run: `.venv/Scripts/python.exe -m pytest backend/tests/services/storage/ -v`
Expected: 全过（含 test_factory 的 mock 需更新为 patch `load_effective_storage_config`，按需调整）

- [ ] **Step 7: Checkpoint**

建议提交：`refactor(storage): factory 改读 effective 配置（DB→.env→默认 三层合并）`

---

## Task 8: 后端·storage 四管理 API（GET/PUT/test/migrate）

**Files:**
- Create: `backend/app/api/storage_config.py`
- Modify: `backend/app/main.py`（注册路由）
- Test: `backend/tests/services/storage/test_storage_config_api.py`

**Context:** spec §4.5。四端点均 `get_current_admin_user`。GET 返 effective + source + 脱敏；PUT 写 DB + reset + 仅 s3 时探针；test 用待测配置建临时 backend 探针；migrate 委托 Task 9。

- [ ] **Step 1: 写失败测试**

```python
# backend/tests/services/storage/test_storage_config_api.py
from unittest.mock import patch, MagicMock


def test_get_config_masks_secret(monkeypatch):
    """GET 返脱敏 secret + source。"""
    from app.services.storage.effective import StorageEffectiveConfig
    fake = StorageEffectiveConfig(
        backend="s3", storage_base_url=None,
        s3_endpoint="https://x", s3_access_key="ak", s3_secret_key="secret-val",
        s3_bucket="b", s3_region="r", s3_url_style="path",
        sources={"backend": "db"},
    )
    from app.core import security
    monkeypatch.setattr(security, "get_current_admin_user", lambda: MagicMock(id=1, is_admin=True))
    with patch("app.api.storage_config.load_effective_storage_config", return_value=fake):
        from fastapi.testclient import TestClient
        from app.main import app
        client = TestClient(app)
        r = client.get("/api/v1/admin/storage-config")
    assert r.status_code == 200
    body = r.json()
    assert body["s3_secret_key"] == "***"
    assert body["has_secret"] is True
    assert body["sources"]["backend"] == "db"


def test_put_config_null_secret_keeps_old(monkeypatch):
    """PUT 传 s3_secret_key=null 表示不变，保留 DB 旧值。"""
    from app.core import security
    monkeypatch.setattr(security, "get_current_admin_user", lambda: MagicMock(id=1, is_admin=True))
    # mock DB：已有一行带旧 secret
    existing = MagicMock()
    existing.s3_secret_key = "old-secret"
    with patch("app.api.storage_config._get_db_row", return_value=existing), \
         patch("app.api.storage_config._upsert_db"), \
         patch("app.api.storage_config.reset_storage"), \
         patch("app.api.storage_config._probe_s3", return_value=(True, None)):  # local 不探针
        from fastapi.testclient import TestClient
        from app.main import app
        client = TestClient(app)
        r = client.put("/api/v1/admin/storage-config", json={
            "backend": "local", "s3_secret_key": None,
        })
    assert r.status_code == 200
    # upsert 收到的 secret 应是旧值
    assert r.json()["s3_secret_key"] == "***"


def test_test_endpoint_runs_probe(monkeypatch):
    """POST /test 用 body 配置探针，不落库。"""
    from app.core import security
    monkeypatch.setattr(security, "get_current_admin_user", lambda: MagicMock(id=1, is_admin=True))
    with patch("app.api.storage_config._probe_s3", return_value=(True, None)) as p:
        from fastapi.testclient import TestClient
        from app.main import app
        client = TestClient(app)
        r = client.post("/api/v1/admin/storage-config/test", json={
            "backend": "s3", "s3_endpoint": "https://x", "s3_access_key": "ak",
            "s3_secret_key": "sk", "s3_bucket": "b", "s3_region": "", "s3_url_style": "path",
        })
    assert r.status_code == 200
    assert r.json()["ok"] is True
    p.assert_called_once()
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/Scripts/python.exe -m pytest backend/tests/services/storage/test_storage_config_api.py -v`
Expected: FAIL（模块/路由不存在）

- [ ] **Step 3: 实现 storage_config.py**

```python
# backend/app/api/storage_config.py
"""图片存储配置管理 API（管理员）。spec §4.5。"""
from fastapi import APIRouter, Depends
from pydantic import BaseModel
from typing import Optional
from sqlalchemy.orm import Session

from app.core.database import get_db
from app.core.security import get_current_admin_user
from app.models.user import User
from app.models.storage_configuration import StorageConfiguration
from app.services.storage.effective import load_effective_storage_config
from app.services.storage.factory import reset_storage

router = APIRouter(prefix="/admin/storage-config", tags=["图片存储配置"])


class StorageConfigUpdate(BaseModel):
    backend: Optional[str] = None
    storage_base_url: Optional[str] = None
    s3_endpoint: Optional[str] = None
    s3_access_key: Optional[str] = None      # null/None = 不变
    s3_secret_key: Optional[str] = None      # null/None = 不变
    s3_bucket: Optional[str] = None
    s3_region: Optional[str] = None
    s3_url_style: Optional[str] = None


class StorageConfigTest(BaseModel):
    backend: str
    s3_endpoint: Optional[str] = None
    s3_access_key: Optional[str] = None
    s3_secret_key: Optional[str] = None
    s3_bucket: Optional[str] = None
    s3_region: Optional[str] = None
    s3_url_style: Optional[str] = None


def _get_db_row(db: Session):
    return db.query(StorageConfiguration).first()


def _upsert_db(db: Session, data: dict) -> StorageConfiguration:
    row = _get_db_row(db)
    if not row:
        row = StorageConfiguration()
        db.add(row)
    for k, v in data.items():
        setattr(row, k, v)
    db.commit()
    db.refresh(row)
    return row


def _probe_s3(cfg) -> tuple[bool, Optional[str]]:
    """用给定配置建临时 S3Backend，put/get/delete 探针对象。返 (ok, error)。"""
    try:
        from app.services.storage.s3 import S3Backend
        b = S3Backend(endpoint=cfg.s3_endpoint, bucket=cfg.s3_bucket,
                      access_key=cfg.s3_access_key, secret_key=cfg.s3_secret_key,
                      region=cfg.s3_region, url_style=cfg.s3_url_style or "path")
        key = "_storage_probe_test"
        b.put(key, b"ok", "application/octet-stream")
        b.get(key)
        b.delete(key)
        return True, None
    except Exception as e:
        return False, str(e)


@router.get("")
def get_config(
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_admin_user),
):
    cfg = load_effective_storage_config()
    return {
        "backend": cfg.backend,
        "storage_base_url": cfg.storage_base_url,
        "s3_endpoint": cfg.s3_endpoint,
        "s3_access_key": "***" if cfg.s3_access_key else None,
        "has_access_key": bool(cfg.s3_access_key),
        "s3_secret_key": "***" if cfg.s3_secret_key else None,
        "has_secret": bool(cfg.s3_secret_key),
        "s3_bucket": cfg.s3_bucket,
        "s3_region": cfg.s3_region,
        "s3_url_style": cfg.s3_url_style,
        "sources": cfg.sources,
    }


@router.put("")
def put_config(
    payload: StorageConfigUpdate,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_admin_user),
):
    data = payload.model_dump(exclude_unset=True)
    existing = _get_db_row(db)
    # 凭证「不变」哨兵：null/None 沿用旧值，不清空
    for cred in ("s3_access_key", "s3_secret_key"):
        if data.get(cred) in (None, "***"):
            data.pop(cred, None)
            if existing is not None and getattr(existing, cred, None):
                data[cred] = getattr(existing, cred)
    # 仅 backend=s3 时写前探针
    target_backend = data.get("backend") or (existing.backend if existing else "local")
    if target_backend == "s3":
        probe_cfg = StorageConfigTest(
            backend="s3",
            s3_endpoint=data.get("s3_endpoint") or (existing.s3_endpoint if existing else ""),
            s3_access_key=data.get("s3_access_key"),
            s3_secret_key=data.get("s3_secret_key"),
            s3_bucket=data.get("s3_bucket") or (existing.s3_bucket if existing else ""),
            s3_region=data.get("s3_region"),
            s3_url_style=data.get("s3_url_style") or (existing.s3_url_style if existing else "path"),
        )
        ok, err = _probe_s3(probe_cfg)
        if not ok:
            from fastapi import HTTPException
            raise HTTPException(status_code=400, detail=f"S3 配置校验失败：{err}")
    row = _upsert_db(db, data)
    reset_storage()
    return {"ok": True}


@router.post("/test")
def test_config(
    payload: StorageConfigTest,
    current_user: User = Depends(get_current_admin_user),
):
    if payload.backend != "s3":
        return {"ok": True, "error": None}
    ok, err = _probe_s3(payload)
    return {"ok": ok, "error": err}


@router.post("/migrate")
def migrate(
    payload: dict,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_admin_user),
):
    """触发双向异步迁移。body: {direction: 'to_s3'|'to_local', s3_config?: {...}}"""
    from app.services.storage.migrate import start_migrate_task
    direction = payload.get("direction", "to_s3")
    s3_config = payload.get("s3_config")
    task_id = start_migrate_task(db, direction, s3_config, user_id=current_user.id)
    return {"task_id": task_id}
```

- [ ] **Step 4: 注册路由**

[main.py](../../../backend/app/main.py) `include_router` 区加：
```python
from app.api.storage_config import router as storage_config_router
app.include_router(storage_config_router, prefix="/api/v1")
```

- [ ] **Step 5: 跑测试 + py_compile**

Run: `.venv/Scripts/python.exe -m pytest backend/tests/services/storage/test_storage_config_api.py -v`
Expected: PASS
Run: `.venv/Scripts/python.exe -m py_compile backend/app/api/storage_config.py`

- [ ] **Step 6: Checkpoint**

建议提交：`feat(storage): 四管理 API（GET 脱敏 / PUT 写+探针 / test / migrate）`

---

## Task 9: 后端·异步双向迁移（复用 ImportTask）

**Files:**
- Create: `backend/app/services/storage/migrate.py`
- Modify: `backend/scripts/migrate_local_to_s3.py`（`_migrate_all` 加 progress_callback + 读 bootstrap_*）
- Create: `backend/scripts/migrate_s3_to_local.py`
- Test: `backend/tests/services/storage/test_migrate.py`

**Context:** spec §4.6。复用 [`ImportTask` + `start_background_import`](../../../backend/app/services/importer/api_service.py#L22) + [`make_progress_callback`](../../../backend/app/services/importer/api_service.py#L42)。task_type=`storage_migrate`，stats 带 direction。`to_s3` 沿用 [`_migrate_all`](../../../backend/scripts/migrate_local_to_s3.py#L51)；`to_local` 新增（`list_objects_v2` 分页）。list 逻辑不扩 Protocol，内聚本模块。

- [ ] **Step 1: 写失败测试**

```python
# backend/tests/services/storage/test_migrate.py
from unittest.mock import patch, MagicMock
from pathlib import Path


def test_migrate_to_s3_uses_migrate_all(tmp_path):
    """to_s3 调 _migrate_all 逻辑，返 uploaded/skipped/failed。"""
    from app.services.storage import migrate as M
    # 造一张本地图
    (tmp_path / "images" / "recipes").mkdir(parents=True)
    (tmp_path / "images" / "recipes" / "a.jpg").write_bytes(b"x")
    fake_backend = MagicMock()
    fake_backend.exists.return_value = False
    uploaded, skipped, failed = M._migrate_to_s3_core(fake_backend, tmp_path / "images")
    assert uploaded == 1
    fake_backend.put.assert_called_once()


def test_migrate_from_s3_paginates_and_writes_local(tmp_path):
    """to_local 用 list_objects_v2 分页，get 后落本地。"""
    from app.services.storage import migrate as M
    fake_s3 = MagicMock()
    # 模拟两页：第一页 2 个 + NextContinuationToken，第二页 1 个 + 无 token
    fake_s3.client.list_objects_v2.side_effect = [
        {"Contents": [{"Key": "recipes/a.jpg"}, {"Key": "recipes/b.jpg"}],
         "NextContinuationToken": "tok"},
        {"Contents": [{"Key": "avatars/c.png"}]},
    ]
    fake_s3.get.side_effect = [b"adata", b"bdata", b"cdata"]
    images_dir = tmp_path / "images"
    uploaded, skipped, failed = M._migrate_from_s3_core(fake_s3, images_dir)
    assert uploaded == 3
    assert (images_dir / "recipes" / "a.jpg").read_bytes() == b"adata"
    assert list_objects_called_pages(fake_s3) == 2


def list_objects_called_pages(fake_s3):
    return fake_s3.client.list_objects_v2.call_count
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/Scripts/python.exe -m pytest backend/tests/services/storage/test_migrate.py -v`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现 migrate.py**

```python
# backend/app/services/storage/migrate.py
"""双向图片迁移：local↔S3。list 逻辑各自实现，不扩 StorageBackend Protocol。

- to_s3：walk 本地 images/ → S3Backend.put（沿用 _migrate_all 思路）
- to_local：S3 list_objects_v2 分页 → get → 落本地
"""
import logging
from pathlib import Path
from typing import Callable, Optional

logger = logging.getLogger(__name__)

_STATIC_ROOT = Path(__file__).resolve().parents[3] / "static"
IMAGES_DIR = _STATIC_ROOT / "images"


def _migrate_to_s3_core(s3_backend, images_dir: Path,
                        progress_callback: Optional[Callable] = None) -> tuple[int, int, int]:
    """local→S3。幂等：S3 已存在跳过。"""
    import mimetypes
    files = sorted(f for f in images_dir.rglob("*") if f.is_file())
    uploaded = skipped = failed = 0
    for i, fp in enumerate(files):
        key = fp.relative_to(images_dir).as_posix()
        try:
            if s3_backend.exists(key):
                skipped += 1
            else:
                mime, _ = mimetypes.guess_type(str(fp))
                s3_backend.put(key, fp.read_bytes(), mime or "application/octet-stream")
                uploaded += 1
        except Exception as e:
            failed += 1
            logger.warning("to_s3 失败 %s: %s", key, e)
        if progress_callback:
            progress_callback("迁移到 S3", i + 1, len(files), key)
    return uploaded, skipped, failed


def _migrate_from_s3_core(s3_backend, images_dir: Path,
                          progress_callback: Optional[Callable] = None) -> tuple[int, int, int]:
    """S3→local。分页 list_objects_v2，get 后落本地。幂等：本地已存在跳过。"""
    uploaded = skipped = failed = 0
    continuation = None
    all_keys: list[str] = []
    while True:
        kwargs = {"Bucket": s3_backend.bucket}
        if continuation:
            kwargs["ContinuationToken"] = continuation
        resp = s3_backend.client.list_objects_v2(**kwargs)
        for obj in resp.get("Contents", []):
            all_keys.append(obj["Key"])
        if resp.get("IsTruncated") and resp.get("NextContinuationToken"):
            continuation = resp["NextContinuationToken"]
        else:
            break

    for i, key in enumerate(all_keys):
        try:
            dest = images_dir / key
            if dest.exists():
                skipped += 1
            else:
                dest.parent.mkdir(parents=True, exist_ok=True)
                dest.write_bytes(s3_backend.get(key))
                uploaded += 1
        except Exception as e:
            failed += 1
            logger.warning("to_local 失败 %s: %s", key, e)
        if progress_callback:
            progress_callback("迁移到本地", i + 1, len(all_keys), key)
    return uploaded, skipped, failed


def _build_s3_from_config(s3_config: Optional[dict]):
    """从待保存配置（to_s3 用新配置）或当前 effective（to_local）建 S3Backend。"""
    from app.services.storage.s3 import S3Backend
    if s3_config:
        return S3Backend(
            endpoint=s3_config["s3_endpoint"], bucket=s3_config["s3_bucket"],
            access_key=s3_config["s3_access_key"], secret_key=s3_config["s3_secret_key"],
            region=s3_config.get("s3_region", ""), url_style=s3_config.get("s3_url_style", "path"),
        )
    # to_local：用当前 effective S3 配置
    from app.services.storage.effective import load_effective_storage_config
    cfg = load_effective_storage_config()
    return S3Backend(endpoint=cfg.s3_endpoint, bucket=cfg.s3_bucket,
                     access_key=cfg.s3_access_key, secret_key=cfg.s3_secret_key,
                     region=cfg.s3_region, url_style=cfg.s3_url_style)


def start_migrate_task(db, direction: str, s3_config: Optional[dict], user_id: int) -> int:
    """建 ImportTask + 起后台线程。返 task_id。"""
    from app.services.importer.api_service import start_background_import, make_progress_callback
    from app.models.import_task import ImportTask
    # 复用 start_background_import：传 _migrate_run 作 import_func
    task_id = start_background_import(
        db, task_type="storage_migrate", user_id=user_id,
        import_func=_run_migrate_task, direction=direction, s3_config=s3_config,
    )
    return task_id


def _run_migrate_task(db, direction: str = "to_s3", s3_config: Optional[dict] = None,
                      progress_callback: Optional[Callable] = None):
    """后台线程执行体（签名兼容 _run_import_task 的 import_func 调用约定）。"""
    from app.services.importer.models import ImportResult
    s3_backend = _build_s3_from_config(s3_config if direction == "to_s3" else None)
    if direction == "to_s3":
        uploaded, skipped, failed = _migrate_to_s3_core(s3_backend, IMAGES_DIR, progress_callback)
    else:
        uploaded, skipped, failed = _migrate_from_s3_core(s3_backend, IMAGES_DIR, progress_callback)
    result = ImportResult()
    result.stats = {"direction": direction, "uploaded": uploaded,
                    "skipped": skipped, "failed": failed,
                    "total": uploaded + skipped + failed}
    result.errors = [] if failed == 0 else [f"{failed} 张图片迁移失败"]
    return result
```

注意：[`_run_import_task`](../../../backend/app/services/importer/api_service.py#L70) 调 `import_func(*new_args, progress_callback=cb)`，故 `_run_migrate_task` 签名须接受 `db` + 关键字参 `direction`/`s3_config`/`progress_callback`。`start_background_import` 的 `*args` 会把 `direction`/`s3_config` 作位置参传——执行时核对 `_run_import_task` 的 new_args 替换逻辑，必要时把 direction/s3_config 包进 dict 或调 `_run_import_task` 签名适配（实现时按实际 API 调整，保证 task_id 返回 + 进度写入 + stats 含 direction）。

- [ ] **Step 4: migrate_local_to_s3.py 加 progress_callback + 读 bootstrap_**

[`_migrate_all`](../../../backend/scripts/migrate_local_to_s3.py#L51) 加 `progress_callback` 参数；[main](../../../backend/scripts/migrate_local_to_s3.py#L89) 读 `settings.bootstrap_s3_*`（Task 6 已改名）。

- [ ] **Step 5: migrate_s3_to_local.py CLI**

新建反向 CLI，读 effective S3 配置，调 `_migrate_from_s3_core`，输出 uploaded/skipped/failed。结构对齐 migrate_local_to_s3.py。

- [ ] **Step 6: 跑测试 + py_compile**

Run: `.venv/Scripts/python.exe -m pytest backend/tests/services/storage/test_migrate.py -v`
Expected: PASS
Run: `.venv/Scripts/python.exe -m py_compile backend/app/services/storage/migrate.py backend/scripts/migrate_local_to_s3.py backend/scripts/migrate_s3_to_local.py`

- [ ] **Step 7: Checkpoint**

建议提交：`feat(storage): 双向异步迁移（to_s3 / to_local，复用 ImportTask，list 分页）`

---

## Task 10: 前端·StorageConfigView 向导页 + 路由 + 导航

**Files:**
- Create: `frontend/src/views/admin/StorageConfigView.vue`
- Modify: `frontend/src/router/index.ts`
- Modify: 后台导航所在文件（AppLayout 或 admin 导航组件）

**Context:** spec §4.7、§4.8。后台「图片存储」页，向导五步（选 backend → 填配置 → 强制测试 → 可选迁移 → 确认切换）。参考 [MapSettingsView.vue](../../../frontend/src/views/admin/MapSettingsView.vue) 范式。

- [ ] **Step 1: 建 StorageConfigView.vue（骨架 + 当前配置展示 + 向导对话框）**

页面主体：当前 effective 配置卡片（每项 source chip）+「切换存储后端」按钮 → 打开 `v-dialog` 向导。向导用 `v-stepper`（5 步）：
1. 选 backend（radio: local/s3）
2. 填配置（S3: endpoint/bucket/access_key/secret_key/region/url_style；local: base_url）
3. 测试连接（调 `POST /admin/storage-config/test`，失败阻断）
4. 迁移（调 `POST /admin/storage-config/migrate {direction}`，轮询 `GET /import/tasks/{id}` 进度；可跳过）
5. 确认（调 `PUT /admin/storage-config`，secret 字段不改传 null）

API 调用用项目 `api` 封装（`api.get/put/post`）。凭证字段：GET 显示 `***`，保存时若未改传 `null`。

- [ ] **Step 2: 加路由**

`frontend/src/router/index.ts` 在 admin 路由组加：
```ts
{
  path: 'admin/storage',
  name: 'admin-storage',
  component: () => import('@/views/admin/StorageConfigView.vue'),
  meta: { requiresAuth: true, requiresAdmin: true, title: '图片存储' },
}
```
（meta 字段对齐现有 admin 路由）

- [ ] **Step 3: 加后台导航项**

在后台导航（`admin/map-settings` 旁）加「图片存储」项，图标 `mdi-cloud-outline`，`to="/admin/storage"`。

- [ ] **Step 4: build 通过**

Run: `npm run build`（在 `frontend/`）

- [ ] **Step 5: 手动核验**

后台进「图片存储」：① 当前配置显示，各项 source 标注（默认/env）② 点切换 → 向导 5 步 ③ 填错 S3 → 测试连接阻断 ④ local→S3 选迁移 → 进度轮询 ⑤ 确认切换 → 图从新 backend 加载。

- [ ] **Step 6: Checkpoint**

建议提交：`feat(frontend): 图片存储配置后台页（向导五步切换 + 双向迁移）`

---

## Task 11: 前端·数据维护中心任务列表 storage_migrate 分支

**Files:**
- Modify: `frontend/src/views/admin/DataMaintenanceView.vue`

**Context:** spec §4.6。`mergedTasks` 已合并 `import_tasks`，`storage_migrate` 自动收入，加展示分支映射 direction/uploaded/skipped/failed。

- [ ] **Step 1: mergedTasks 加 storage_migrate 展示**

在 `mergedTasks` 的任务渲染处加 `task_type === 'storage_migrate'` 分支：图标 `mdi-cloud-sync`、名称「图片存储迁移（→S3 / →本地）」（按 stats.direction）、stats 显示 uploaded/skipped/failed。对齐现有 import/usda 任务分支写法。

- [ ] **Step 2: build 通过**

Run: `npm run build`

- [ ] **Step 3: 手动核验**

触发一次迁移（Task 10 向导或 API），数据维护中心任务列表出现「图片存储迁移」条目，进度/结果正确。

- [ ] **Step 4: Checkpoint**

建议提交：`feat(frontend): 数据维护中心任务列表加 storage_migrate 分支`

---

# 收尾

## Task 12: 全量回归 + 最终审查

**Files:** 无（验证 only）

- [ ] **Step 1: 后端 compileall**

Run: `.venv/Scripts/python.exe -m compileall backend/app`
Expected: 无语法错误

- [ ] **Step 2: 存储相关单测全过**

Run: `.venv/Scripts/python.exe -m pytest backend/tests/services/storage/ backend/tests/services/test_region_admin.py backend/tests/services/test_region_seed_lifespan.py -v`
Expected: 全 PASS

- [ ] **Step 3: 全量 pytest 回归**

Run: `.venv/Scripts/python.exe -m pytest backend/tests/ -q`
Expected: 失败数不高于基线（失败项须确认为预存无关：auth/import/usda 等）

- [ ] **Step 4: 前端 build**

Run: `npm run build`（在 `frontend/`）
Expected: 成功

- [ ] **Step 5: 手动端到端核验**

- `/profile` 个人中心：头像/地区在「用户信息编辑」对话框内，昵称无图标，第一级「国家/地区」，各级有数据
- 后台「数据维护中心」：「行政区划」卡片 + 「图片存储迁移」任务分支
- 后台「图片存储」：向导切换 local↔S3 全流程（测试 → 迁移 → 确认）
- 地区级联、头像上传、菜谱图片（local 模式）均正常

- [ ] **Step 6: 更新 cc/ 记录**

按 [CLAUDE.md](../../../CLAUDE.md) 索引，新增 `cc/FEATURE_存储配置后台化.md`（或合并到既有索引项），记录：DB 覆盖 .env 三层优先级、BOOTSTRAP_ 改名、向导五步、双向异步迁移、地区 seed 移出条件块 + 更新模块。

- [ ] **Step 7: Checkpoint**

建议提交：`docs: 存储配置后台化 + 个人信息编辑重组 要点记录`

---

## 执行顺序说明

- **子任务一（Task 1-4）与子任务二（Task 5-11）独立，可并行。**
- Task 1→2 顺序（端点依赖 seed 修复后数据正常）；Task 3 依赖 Task 1/2（地区有数据才能核验级联）；Task 4 依赖 Task 2。
- Task 5→6→7→8→9 顺序（模型→改名→factory→API→迁移）；Task 10 依赖 Task 8/9；Task 11 依赖 Task 9。
- Task 12 收尾，全部完成后跑。
