# 头像裁剪与存储配置增强·实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: 用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 按任务逐个实现。步骤用 `- [ ]` 复选框跟踪。

**Goal:** 收尾 2026-07-20 存储配置 + 个人信息工作——头像裁剪（vue-advanced-cropper 512×512）、国家级中文化、向导按钮去重、PicGo 三字段（路径/域名/后缀）、url_style 提示、修 OSS PutObject 兼容 bug。

**Architecture:** 7 个 task：后端 OSS 兼容修复 + PicGo 三字段（数据层 + 逻辑层）+ 国家级中文映射；前端头像裁剪组件 + 向导增强（按钮/PicGo/提示）；回归。基于 2026-07-20 已实现工作增量改动。

**Tech Stack:** Python / FastAPI / SQLAlchemy / Alembic / boto3 / pytest；Vue 3 / Vuetify 3 / vue-advanced-cropper / TypeScript / Vite。

**执行规范（项目约定，必读）：**
- 后端 `backend/`，根 `.venv`（`d:\code\live_calc\.venv`）；`.venv/Scripts/python.exe -m pytest ...`。`uv` 管依赖。
- 表结构变更三件套：alembic + SQLite/MySQL/PostgreSQL 三引擎 SQL + 开发库直补（项目不走 alembic upgrade）。
- 后端 TDD：失败测试 → 确认失败 → 实现 → 确认通过。前端无单测，靠 `npm run build` + 手动核验。
- 每步 `py_compile`（后端）/ `npm run build`（前端）必过。
- 不要自行启动服务（开发者已起自动重载）。
- **不要自行 git 提交**（CLAUDE.md 规矩）。Task 末尾 **Checkpoint** 是建议提交点，等用户确认。
- 设计依据：[spec](../specs/2026-07-21-头像裁剪与存储配置增强-design.md)。
- **OSS 兼容（Task 1）需用户真实阿里云 OSS bucket 手测**（单测只能验证 Config 参数对，真机签名兼容要实测）。

---

## 文件结构

### 新建
| 文件 | 职责 |
|---|---|
| `frontend/src/components/profile/AvatarCropperDialog.vue` | 头像裁剪对话框（vue-advanced-cropper，1:1，输出 512×512） |
| `backend/alembic/versions/20260721_0001_storage_picgo_fields.py` | StorageConfiguration 加 3 列 |
| `backend/scripts/sql/20260721_storage_picgo_fields_sqlite.sql` / `_mysql.sql` / `_postgres.sql` | 三引擎 ALTER TABLE |
| `backend/tests/services/storage/test_s3_picgo.py` | S3Backend PicGo 三字段 + OSS Config 单测 |
| `backend/tests/services/test_region_country_zh.py` | 国家级中文映射单测 |

### 修改
| 文件 | 改动 |
|---|---|
| `backend/app/services/storage/s3.py` | OSS Config 修 + S3Backend 加 base_path/custom_domain/url_suffix（put/url_for/exists/get/delete 用 _full_key） |
| `backend/app/models/storage_configuration.py` | + s3_base_path / s3_custom_domain / s3_url_style 列 |
| `backend/app/services/storage/effective.py` | _DEFAULTS + _ENV_MAP + dataclass +3 字段 |
| `backend/app/config.py` | + bootstrap_s3_base_path / bootstrap_s3_custom_domain / bootstrap_s3_url_suffix |
| `backend/.env.example` | + 3 BOOTSTRAP_ 变量 |
| `backend/app/services/storage/factory.py` | _build 传 S3Backend 3 新参 |
| `backend/app/api/storage_config.py` | StorageConfigResponse/Update/Test +3 字段；_probe_s3 传 3 参 |
| `backend/app/services/region_seed.py` | + COUNTRY_NAMES_ZH 常量；_process_countries 用中文 name |
| `frontend/src/views/profile/ProfileView.vue` | 头像区接 AvatarCropperDialog |
| `frontend/src/views/admin/StorageConfigView.vue` | v-stepper hide-actions + 向导② PicGo 三字段 + url_style 提示 + 配置展示卡三字段 |
| `frontend/package.json` | + vue-advanced-cropper |

---

## Task 1: 后端·OSS PutObject 兼容修复

**Files:**
- Modify: `backend/app/services/storage/s3.py:51-66`（Config 构造）
- Test: `backend/tests/services/storage/test_s3_picgo.py`（新建，本 task 先加 OSS Config 测试）

**Context:** spec §3.6。boto3 新版默认 PutObject 用 `STREAMING-UNSIGNED-PAYLOAD-TRAILER` 签名，OSS 不支持 → NotImplemented。修法：Config 加 `payload_signing_enabled=True`（强制签名）+ `request_checksum_calculation`/`response_checksum_validation`（关默认 CRC checksum）。单测验证 Config 含对参数；真机 OSS 兼容留给用户手测（Task 7）。

- [ ] **Step 1: 写失败测试**

```python
# backend/tests/services/storage/test_s3_picgo.py
from unittest.mock import patch


def _make_backend(**kwargs):
    """构造 S3Backend 但 mock boto3.client（不发真请求）。"""
    with patch("app.services.storage.s3.boto3.client") as mock_client:
        from app.services.storage.s3 import S3Backend
        b = S3Backend(
            endpoint="https://oss.example.com",
            bucket="bkt", access_key="ak", secret_key="sk",
            url_style="virtual", **kwargs,
        )
    return b, mock_client


def test_s3_config_disables_streaming_unsigned_payload():
    """OSS 兼容：Config 必须含 payload_signing_enabled=True（强制签名，绕过 streaming-unsigned-payload-trailer）。"""
    b, mock_client = _make_backend()
    config = mock_client.call_args.kwargs["config"]
    # botocore Config 的 s3 配置（dict 形式）
    assert config.s3.get("payload_signing_enabled") is True
    # 关默认 CRC checksum（OSS 可能不支持）
    assert getattr(config, "request_checksum_calculation", None) == "when_required"
    assert getattr(config, "response_checksum_validation", None) == "when_required"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/Scripts/python.exe -m pytest backend/tests/services/storage/test_s3_picgo.py::test_s3_config_disables_streaming_unsigned_payload -v`
Expected: FAIL（当前 Config 无 payload_signing_enabled）

- [ ] **Step 3: 实现——s3.py Config 加参数**

[s3.py:51-58](../../../backend/app/services/storage/s3.py#L51-L58) 的 Config 改：
```python
        config = Config(
            signature_version="s3v4",
            s3={
                "addressing_style": "virtual" if url_style == "virtual" else "path",
                # OSS 兼容：强制签名（AWS4-HMAC-SHA256），绕过 streaming-unsigned-payload-trailer
                "payload_signing_enabled": True,
            },
            # OSS 兼容：关默认 CRC32C checksum（boto3 2.x 默认开，OSS 不支持）
            request_checksum_calculation="when_required",
            response_checksum_validation="when_required",
        )
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/Scripts/python.exe -m pytest backend/tests/services/storage/test_s3_picgo.py::test_s3_config_disables_streaming_unsigned_payload -v`
Expected: PASS

- [ ] **Step 5: py_compile + 回归现有 s3 测试**

Run: `.venv/Scripts/python.exe -m py_compile backend/app/services/storage/s3.py`
Run: `.venv/Scripts/python.exe -m pytest backend/tests/services/storage/ -v`（无回归）

- [ ] **Step 6: Checkpoint**

建议提交：`fix(storage): S3Backend Config 加 payload_signing_enabled + checksum 控制（OSS 兼容）`

**注意**：真机 OSS 兼容验证（put_object 真过）留给 Task 7 用户手测。单测只保证 Config 参数对。

---

## Task 2: 后端·PicGo 三字段·数据层（模型 + 迁移）

**Files:**
- Modify: `backend/app/models/storage_configuration.py`
- Create: `backend/alembic/versions/20260721_0001_storage_picgo_fields.py`
- Create: `backend/scripts/sql/20260721_storage_picgo_fields_sqlite.sql` / `_mysql.sql` / `_postgres.sql`
- Modify: 开发库 `backend/data/livecalc.db`

**Context:** spec §3.4。StorageConfiguration 加 3 列（S3 专用，nullable）。

- [ ] **Step 1: 模型加 3 列**

[storage_configuration.py](../../../backend/app/models/storage_configuration.py) 在 s3_url_style 后加：
```python
    s3_base_path = Column(String(255), nullable=True)       # key 前缀，如 "livecalc/"
    s3_custom_domain = Column(String(255), nullable=True)    # 自定义域名，如 "https://cdn.example.com"
    s3_url_suffix = Column(String(255), nullable=True)      # 网址后缀，如 "?imageslim"
```
`to_dict` 也加这 3 字段。

- [ ] **Step 2: alembic 迁移**

```python
# backend/alembic/versions/20260721_0001_storage_picgo_fields.py
"""add picgo fields to storage_configurations"""
from alembic import op
import sqlalchemy as sa

revision = "20260721_0001"
down_revision = "20260720_0002"   # 链至 StorageConfiguration 建表迁移；执行时核对最新
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.add_column("storage_configurations", sa.Column("s3_base_path", sa.String(255), nullable=True))
    op.add_column("storage_configurations", sa.Column("s3_custom_domain", sa.String(255), nullable=True))
    op.add_column("storage_configurations", sa.Column("s3_url_suffix", sa.String(255), nullable=True))


def downgrade() -> None:
    op.drop_column("storage_configurations", "s3_url_suffix")
    op.drop_column("storage_configurations", "s3_custom_domain")
    op.drop_column("storage_configurations", "s3_base_path")
```
（执行时 `grep -rn "^revision = " backend/alembic/versions/*.py` 核对 down_revision 链最新。）

- [ ] **Step 3: 三引擎 SQL**

`backend/scripts/sql/20260721_storage_picgo_fields_sqlite.sql`:
```sql
ALTER TABLE storage_configurations ADD COLUMN s3_base_path TEXT;
ALTER TABLE storage_configurations ADD COLUMN s3_custom_domain TEXT;
ALTER TABLE storage_configurations ADD COLUMN s3_url_suffix TEXT;
```
MySQL 版（`_mysql.sql`）：`ALTER TABLE storage_configurations ADD COLUMN s3_base_path VARCHAR(255) NULL;`（三列）。Postgres 版（`_postgres.sql`）：`ALTER TABLE storage_configurations ADD COLUMN s3_base_path VARCHAR(255);`（三列）。

- [ ] **Step 4: 开发库直补**

备份 `backend/data/livecalc.db` → `.bak_20260721_picgo`，跑 sqlite SQL（或重启后端 create_all 自动加列——但项目惯例手动补更稳）。

- [ ] **Step 5: py_compile + 验证加列**

Run: `.venv/Scripts/python.exe -m py_compile backend/app/models/storage_configuration.py`
验证:
```bash
.venv/Scripts/python.exe -c "from app.core.database import SessionLocal; from app.models.storage_configuration import StorageConfiguration; db=SessionLocal(); r=db.query(StorageConfiguration).first(); print(hasattr(r,'s3_base_path'), hasattr(r,'s3_custom_domain'), hasattr(r,'s3_url_suffix')); db.close()"
```
Expected: `True True True`

- [ ] **Step 6: Checkpoint**

建议提交：`feat(storage): StorageConfiguration 加 PicGo 三字段（base_path/custom_domain/url_suffix）+ 迁移`

---

## Task 3: 后端·PicGo 三字段·逻辑层（S3Backend + effective + config + factory + API + .env）

**Files:**
- Modify: `backend/app/services/storage/s3.py`（__init__ + put/get/delete/exists/url_for）
- Modify: `backend/app/services/storage/effective.py`（_DEFAULTS + _ENV_MAP + dataclass）
- Modify: `backend/app/config.py`（+ 3 bootstrap_ 字段）
- Modify: `backend/.env.example`（+ 3 BOOTSTRAP_ 变量）
- Modify: `backend/app/services/storage/factory.py`（_build 传 3 参）
- Modify: `backend/app/api/storage_config.py`（Response/Update/Test +3 + _probe_s3 传 3 参）
- Test: `backend/tests/services/storage/test_s3_picgo.py`（加 PicGo 三字段测试）

**Context:** spec §3.4。S3Backend 支持 base_path/custom_domain/url_suffix；DB 存逻辑 key（不含 base_path），S3Backend 内部加前缀；url_for 用 custom_domain + suffix。全链跟进。

- [ ] **Step 1: 写失败测试（PicGo 三字段）**

追加到 `backend/tests/services/storage/test_s3_picgo.py`：
```python
from unittest.mock import patch


def test_s3_put_prepends_base_path():
    """put：DB 存逻辑 key（返回值不含 base_path），但 put_object 的 Key 含前缀。"""
    with patch("app.services.storage.s3.boto3.client") as mock_client:
        from app.services.storage.s3 import S3Backend
        b = S3Backend(endpoint="https://oss.example.com", bucket="bkt",
                      access_key="ak", secret_key="sk", url_style="virtual",
                      base_path="livecalc/")
        ret = b.put("avatars/x.png", b"data", "image/png")
    assert ret == "avatars/x.png"   # 返回逻辑 key
    mock_client.return_value.put_object.assert_called_once()
    assert mock_client.return_value.put_object.call_args.kwargs["Key"] == "livecalc/avatars/x.png"


def test_s3_url_for_custom_domain_and_suffix():
    """url_for：custom_domain 优先 + url_suffix 末尾。"""
    with patch("app.services.storage.s3.boto3.client"):
        from app.services.storage.s3 import S3Backend
        b = S3Backend(endpoint="https://oss.example.com", bucket="bkt",
                      access_key="ak", secret_key="sk", url_style="virtual",
                      custom_domain="https://cdn.example.com", url_suffix="?imageslim")
        url = b.url_for("avatars/x.png")
    assert url == "https://cdn.example.com/avatars/x.png?imageslim"


def test_s3_url_for_base_path_with_endpoint():
    """url_for：无 custom_domain 时用 endpoint + base_path 前缀。"""
    with patch("app.services.storage.s3.boto3.client"):
        from app.services.storage.s3 import S3Backend
        b = S3Backend(endpoint="https://oss.example.com", bucket="bkt",
                      access_key="ak", secret_key="sk", url_style="path",
                      base_path="livecalc/")
        url = b.url_for("avatars/x.png")
    assert url == "https://oss.example.com/bkt/livecalc/avatars/x.png"


def test_s3_get_uses_full_key():
    """get/exists/delete：内部用 _full_key（含 base_path）。"""
    with patch("app.services.storage.s3.boto3.client") as mock_client:
        mock_client.return_value.get_object.return_value = {"Body": type("B", (), {"read": lambda self: b"x"})()}
        from app.services.storage.s3 import S3Backend
        b = S3Backend(endpoint="https://oss.example.com", bucket="bkt",
                      access_key="ak", secret_key="sk", url_style="path", base_path="lc/")
        b.get("avatars/x.png")
    assert mock_client.return_value.get_object.call_args.kwargs["Key"] == "lc/avatars/x.png"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/Scripts/python.exe -m pytest backend/tests/services/storage/test_s3_picgo.py -v`
Expected: 新 4 测试 FAIL（base_path 等参数不支持）

- [ ] **Step 3: 实现 S3Backend 三字段**

[s3.py __init__](../../../backend/app/services/storage/s3.py#L27) 加 3 参 + `_full_key` helper；put/get/delete/exists/url_for 用 _full_key：
```python
    def __init__(self, endpoint, bucket, access_key, secret_key, region="", url_style="path",
                 base_path="", custom_domain="", url_suffix=""):
        # ... 现有 endpoint/url_style 校验 + Config 不变（Config 已 Task 1 修）...
        self.base_path = (base_path or "").strip("/")
        self.custom_domain = (custom_domain or "").rstrip("/")
        self.url_suffix = url_suffix or ""
        # ... self.client = boto3.client(...) 不变 ...

    def _full_key(self, key: str) -> str:
        return f"{self.base_path}/{key}" if self.base_path else key

    def put(self, key, data, content_type):
        self.client.put_object(Bucket=self.bucket, Key=self._full_key(key),
                               Body=data, ContentType=content_type)
        return key   # 返回逻辑 key（DB 存）

    def get(self, key):
        try:
            return self.client.get_object(Bucket=self.bucket, Key=self._full_key(key))["Body"].read()
        except ClientError as e:
            if e.response.get("Error", {}).get("Code", "") in _NOT_FOUND_CODES:
                raise FileNotFoundError(key) from e
            raise

    def delete(self, key):
        self.client.delete_object(Bucket=self.bucket, Key=self._full_key(key))

    def exists(self, key):
        try:
            self.client.head_object(Bucket=self.bucket, Key=self._full_key(key))
            return True
        except ClientError as e:
            if e.response.get("Error", {}).get("Code", "") in _NOT_FOUND_CODES:
                return False
            raise

    def url_for(self, key):
        from urllib.parse import quote
        full_key = self._full_key(key)
        quoted = quote(full_key)
        if self.custom_domain:
            base = self.custom_domain
        elif self.url_style == "virtual":
            parsed = urlparse(self.endpoint)
            scheme = parsed.scheme or "https"
            netloc = parsed.netloc or parsed.path
            base = f"{scheme}://{self.bucket}.{netloc}"
        else:
            base = f"{self.endpoint}/{self.bucket}"
        return f"{base}/{quoted}{self.url_suffix}"
```

- [ ] **Step 4: 跑 S3 测试通过**

Run: `.venv/Scripts/python.exe -m pytest backend/tests/services/storage/test_s3_picgo.py -v`
Expected: 5 PASS（Task 1 OSS Config + Task 3 PicGo 4）

- [ ] **Step 5: effective.py 加 3 字段**

[effective.py](../../../backend/app/services/storage/effective.py)：
- `_DEFAULTS` 加：`"s3_base_path": "", "s3_custom_domain": "", "s3_url_suffix": ""`
- `_ENV_MAP` 加：`"s3_base_path": "bootstrap_s3_base_path", "s3_custom_domain": "bootstrap_s3_custom_domain", "s3_url_suffix": "bootstrap_s3_url_suffix"`
- `StorageEffectiveConfig` dataclass 加 3 字段：`s3_base_path/s3_custom_domain/s3_url_suffix: Optional[str]`
- `load_effective_storage_config` 返回构造加 3 字段

- [ ] **Step 6: config.py + .env.example 加 3 bootstrap_ 字段**

[config.py](../../../backend/app/config.py) Settings 加（在 bootstrap_s3_url_style 后）：
```python
    bootstrap_s3_base_path: str = ""
    bootstrap_s3_custom_domain: str = ""
    bootstrap_s3_url_suffix: str = ""
```
[.env.example](../../../backend/.env.example) 加（在 BOOTSTRAP_S3_URL_STYLE 后）：
```env
# PicGo 风格：S3 key 前缀（如 livecalc/）、自定义域名（如 https://cdn.example.com）、网址后缀（如 ?imageslim）
BOOTSTRAP_S3_BASE_PATH=
BOOTSTRAP_S3_CUSTOM_DOMAIN=
BOOTSTRAP_S3_URL_SUFFIX=
```

- [ ] **Step 7: factory.py _build 传 3 参**

[factory.py _build](../../../backend/app/services/storage/factory.py) 的 S3Backend 构造加：
```python
        return S3Backend(
            endpoint=cfg.s3_endpoint, bucket=cfg.s3_bucket,
            access_key=cfg.s3_access_key, secret_key=cfg.s3_secret_key,
            region=cfg.s3_region, url_style=cfg.s3_url_style,
            base_path=cfg.s3_base_path or "", custom_domain=cfg.s3_custom_domain or "",
            url_suffix=cfg.s3_url_suffix or "",
        )
```

- [ ] **Step 8: storage_config.py schema + _probe_s3 加 3 字段**

[storage_config.py](../../../backend/app/api/storage_config.py)：
- `StorageConfigUpdate` / `StorageConfigTest` / `StorageConfigResponse` 各加：
  ```python
  s3_base_path: Optional[str] = None
  s3_custom_domain: Optional[str] = None
  s3_url_suffix: Optional[str] = None
  ```
- `get_config` 返回构造加 3 字段（cfg.s3_base_path 等）
- `_probe_s3` 构造 S3Backend 传 base_path=cfg.s3_base_path / custom_domain / url_suffix
- `put_config` 的 probe_cfg 构造加 3 字段（data.get or existing）

- [ ] **Step 8b: migrate.py `_build_s3_from_config` 传 3 字段**

[migrate.py `_build_s3_from_config`](../../../backend/app/services/storage/migrate.py) 的 s3_config 分支（to_s3 用）建 S3Backend 时加 3 参：
```python
    if s3_config:
        return S3Backend(
            endpoint=s3_config["s3_endpoint"], bucket=s3_config["s3_bucket"],
            access_key=s3_config["s3_access_key"], secret_key=s3_config["s3_secret_key"],
            region=s3_config.get("s3_region", ""), url_style=s3_config.get("s3_url_style", "path"),
            base_path=s3_config.get("s3_base_path", ""),
            custom_domain=s3_config.get("s3_custom_domain", ""),
            url_suffix=s3_config.get("s3_url_suffix", ""),
        )
```
（to_local 分支用 load_effective_storage_config，effective 已含三字段，S3Backend 构造也要传——见下）

**注意 to_local 分支**：`_build_s3_from_config(s3_config=None)` 走 `load_effective_storage_config()`，effective 现含三字段（Task 3 Step 5），该分支 S3Backend 构造也要传 `base_path=cfg.s3_base_path or ""` 等。

- [ ] **Step 9: py_compile + 全 storage 测试回归**

Run: `.venv/Scripts/python.exe -m py_compile backend/app/services/storage/s3.py backend/app/services/storage/effective.py backend/app/config.py backend/app/services/storage/factory.py backend/app/api/storage_config.py`
Run: `.venv/Scripts/python.exe -m pytest backend/tests/services/storage/ -v`（全过）

- [ ] **Step 10: Checkpoint**

建议提交：`feat(storage): PicGo 三字段全链（S3Backend 前缀/域名/后缀 + effective/factory/API/.env）`

---

## Task 4: 后端·国家级中文化（region_seed COUNTRY_NAMES_ZH）

**Files:**
- Modify: `backend/app/services/region_seed.py`（加 COUNTRY_NAMES_ZH + _process_countries 改）
- Test: `backend/tests/services/test_region_country_zh.py`

**Context:** spec §3.2。全球国家级（level 0）数据源英文，内置 ISO alpha-2→中文映射，seed 时 name 用中文、英文存 name_en。re-seed 复用 POST /admin/regions/seed（2026-07-20 Task 2 端点）触发。

- [ ] **Step 1: 写失败测试**

```python
# backend/tests/services/test_region_country_zh.py
def test_country_names_zh_covers_common():
    """COUNTRY_NAMES_ZH 覆盖常见国家 + 缺失回落英文逻辑。"""
    from app.services.region_seed import COUNTRY_NAMES_ZH
    # 常见国家必须命中
    for alpha2, zh in [("CN", "中国"), ("US", "美国"), ("JP", "日本"), ("GB", "英国"), ("FR", "法国")]:
        assert COUNTRY_NAMES_ZH.get(alpha2) == zh, f"{alpha2} 缺中文映射"
    # 至少覆盖 ISO 3166-1 大部分成员（~200+，保证填全）
    assert len(COUNTRY_NAMES_ZH) >= 200


def test_process_countries_uses_zh_name(monkeypatch):
    """_process_countries：name 用中文，name_en 用英文。"""
    from app.services import region_seed
    # 喂一个中国 + 一个虚构（缺中文回落英文）
    countries = [
        {"alpha-2": "CN", "name": "China"},
        {"alpha-2": "ZZ", "name": "Zzzland"},   # 无中文映射
    ]
    captured = []
    def fake_upsert(db, code, name, level, **kw):
        captured.append((code, name, kw.get("name_en")))
        return 1, "created"
    monkeypatch.setattr(region_seed, "_upsert_region", fake_upsert)
    region_seed._process_countries(db=None, countries_data=countries, created=0, skipped=0)
    codes = {c[0]: c for c in captured}
    assert codes["CN"][1] == "中国"      # name 中文
    assert codes["CN"][2] == "China"     # name_en 英文
    assert codes["ZZ"][1] == "Zzzland"   # 缺中文回落英文
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/Scripts/python.exe -m pytest backend/tests/services/test_region_country_zh.py -v`
Expected: FAIL（COUNTRY_NAMES_ZH 不存在）

- [ ] **Step 3: 实现——加 COUNTRY_NAMES_ZH + 改 _process_countries**

[region_seed.py](../../../backend/app/services/region_seed.py) 模块顶部（import 后）加 `COUNTRY_NAMES_ZH` 常量——**完整 ISO 3166-1 ~250 成员中文译名**（数据来源：ISO 3166-1 官方国名中文译名，参考维基百科「ISO 3166-1」中文国名表 / GB/T 2659）。示例开头：
```python
# ISO 3166-1 alpha-2 → 中文国家/地区名（~250 条，内置静态）
# 数据来源：ISO 3166-1 官方中文译名（参考维基 ISO 3166-1 中文表 / GB/T 2659）
# 缺失时 _process_countries 回落英文 name
COUNTRY_NAMES_ZH: dict[str, str] = {
    "CN": "中国", "HK": "中国香港", "MO": "中国澳门", "TW": "中国台湾",
    "US": "美国", "JP": "日本", "KR": "韩国", "KP": "朝鲜",
    "GB": "英国", "FR": "法国", "DE": "德国", "IT": "意大利", "ES": "西班牙",
    "PT": "葡萄牙", "NL": "荷兰", "BE": "比利时", "CH": "瑞士", "AT": "奥地利",
    "RU": "俄罗斯", "UA": "乌克兰", "PL": "波兰", "SE": "瑞典", "NO": "挪威",
    "FI": "芬兰", "DK": "丹麦", "IS": "冰岛", "IE": "爱尔兰", "GR": "希腊",
    "CZ": "捷克", "SK": "斯洛伐克", "HU": "匈牙利", "RO": "罗马尼亚", "BG": "保加利亚",
    "RS": "塞尔维亚", "HR": "克罗地亚", "SI": "斯洛文尼亚", "BA": "波黑", "AL": "阿尔巴尼亚",
    "TR": "土耳其", "IL": "以色列", "SA": "沙特阿拉伯", "AE": "阿联酋", "EG": "埃及",
    "ZA": "南非", "NG": "尼日利亚", "KE": "肯尼亚", "MA": "摩洛哥", "GH": "加纳",
    "IN": "印度", "PK": "巴基斯坦", "BD": "孟加拉国", "LK": "斯里兰卡", "NP": "尼泊尔",
    "TH": "泰国", "VN": "越南", "MY": "马来西亚", "SG": "新加坡", "ID": "印度尼西亚",
    "PH": "菲律宾", "MM": "缅甸", "KH": "柬埔寨", "LA": "老挝",
    "AU": "澳大利亚", "NZ": "新西兰", "FJ": "斐济", "PG": "巴布亚新几内亚",
    "CA": "加拿大", "MX": "墨西哥", "BR": "巴西", "AR": "阿根廷", "CL": "智利",
    "CO": "哥伦比亚", "PE": "秘鲁", "VE": "委内瑞拉", "EC": "厄瓜多尔", "BO": "玻利维亚",
    "UY": "乌拉圭", "PY": "巴拉圭", "CU": "古巴", "DO": "多米尼加", "GT": "危地马拉",
    # ... 继续补全至 ISO 3166-1 全部 ~250 成员（用 WebSearch 或模型知识填全）
}
```
**执行时用 WebSearch 或内置知识补全至 ~250 条**（覆盖所有 ISO 3166-1 官方 alpha-2 码）。

`_process_countries` 改（[region_seed.py:154-182](../../../backend/app/services/region_seed.py#L154-L182)）：`_upsert_region` 调用的 `name` 用 `COUNTRY_NAMES_ZH.get(alpha2, name)`，加 `name_en=name`：
```python
    for entry in countries_data:
        alpha2 = entry.get("alpha-2", "") or ""
        name = entry.get("name", "") or ""
        if not alpha2 or not name:
            continue
        name_zh = COUNTRY_NAMES_ZH.get(alpha2, name)   # 缺中文回落英文
        _id, action = _upsert_region(
            db, code=alpha2, name=name_zh, level=0,
            iso_country=alpha2, path=alpha2, name_en=name,
        )
        # ... created/skipped 统计不变 ...
```

- [ ] **Step 4: 跑测试通过**

Run: `.venv/Scripts/python.exe -m pytest backend/tests/services/test_region_country_zh.py -v`
Expected: PASS

- [ ] **Step 5: py_compile + 触发 re-seed 验证（开发库）**

Run: `.venv/Scripts/python.exe -m py_compile backend/app/services/region_seed.py`
触发 re-seed（更新现有国家级 name 为中文）:
```bash
.venv/Scripts/python.exe -c "from app.core.database import SessionLocal; from app.services.region_seed import upsert_administrative_regions; db=SessionLocal(); print(upsert_administrative_regions(db)); db.close()"
```
Expected: `{created: 0, skipped: ~250, errors: 0}`（国家级已存在 skip，但 _upsert_region 对已存在 active 行是 skip——**注意**：现有国家级 name 是英文 + active，`_upsert_region` 对 active 行 skip 不更新。**需调整 `_upsert_region`**：对已存在 active 行，若 name 变了则更新（不只 skip）。

**关键**：[region_seed.py `_upsert_region`](../../../backend/app/services/region_seed.py#L96-L151) 当前对 active 行 skip（不更新字段）。要让 re-seed 更新国家级 name 为中文，`_upsert_region` 对已存在 active 行需比对并更新 name。改 `_upsert_region`：active 行若 name/name_en 不同则更新字段（reactivated 分支已有更新逻辑，active 分支加类似更新）。

调整 `_upsert_region` 的 active 分支：
```python
    if existing:
        if not existing.is_active:
            # 复活（现有逻辑）
            ...
        # active 行：若字段变化则更新（支持 re-seed 刷新，如英文 name → 中文）
        if existing.name != name:
            existing.name = name
        if name_en and existing.name_en != name_en:
            existing.name_en = name_en
        db.flush()
        return existing.id, "skipped"   # 仍计 skipped（幂等视角），但字段已刷新
```
（re-seed 后国家级 name 更新为中文，action 仍记 skipped——语义上是「行已存在」，但字段刷新了。）

重跑 re-seed 验证：开发库国家级 name 变中文：
```bash
.venv/Scripts/python.exe -c "import sqlite3; c=sqlite3.connect('backend/data/livecalc.db'); print([r for r in c.execute('SELECT code,name FROM administrative_regions WHERE level=0 LIMIT 5')])"
```
Expected: `[('CN','中国'),('US','美国'),...]`（中文）

- [ ] **Step 6: Checkpoint**

建议提交：`feat(region): 国家级 name 中文化（COUNTRY_NAMES_ZH + _upsert_region 字段刷新）`

---

## Task 5: 前端·头像裁剪（vue-advanced-cropper）

**Files:**
- Modify: `frontend/package.json`（+ vue-advanced-cropper）
- Create: `frontend/src/components/profile/AvatarCropperDialog.vue`
- Modify: `frontend/src/views/profile/ProfileView.vue`（头像区接裁剪对话框）

**Context:** spec §3.1。选图后先裁剪（1:1，512×512 输出）再上传。复用现有 uploadAvatar（接 blob）。

- [ ] **Step 1: 加依赖**

Run（在 `frontend/`）: `npm install vue-advanced-cropper`
（package.json + package-lock.json 更新）

- [ ] **Step 2: 建 AvatarCropperDialog.vue**

```vue
<!-- frontend/src/components/profile/AvatarCropperDialog.vue -->
<template>
  <v-dialog :model-value="modelValue" @update:model-value="$emit('update:modelValue', $event)" max-width="560" persistent>
    <v-card>
      <v-card-title class="d-flex align-center">
        裁剪头像
        <v-spacer />
        <v-btn icon="mdi-close" variant="text" size="small" @click="$emit('update:modelValue', false)" />
      </v-card-title>
      <v-card-text>
        <div class="cropper-wrap" style="width: 100%; height: 400px; background: #f5f5f5;">
          <cropper
            v-if="src"
            :src="src"
            :stencil-props="{ aspectRatio: 1 }"
            :resize="{ image: true }"
            image-restriction="stencil"
            @change="onChange"
          />
        </div>
        <div class="text-caption text-medium-emphasis mt-2">拖动调整位置，滚轮缩放，输出 512×512</div>
      </v-card-text>
      <v-card-actions>
        <v-spacer />
        <v-btn variant="text" @click="$emit('update:modelValue', false)">取消</v-btn>
        <v-btn color="primary" :loading="processing" @click="confirm">确认裁剪</v-btn>
      </v-card-actions>
    </v-card>
  </v-dialog>
</template>

<script setup lang="ts">
import { ref } from 'vue'
import { Cropper } from 'vue-advanced-cropper'
import 'vue-advanced-cropper/dist/style.css'

const props = defineProps<{ modelValue: boolean; src: string }>()
const emit = defineEmits<{ 'update:modelValue': [boolean]; cropped: [Blob] }>()

const canvas = ref<HTMLCanvasElement | null>(null)
const processing = ref(false)

function onChange({ canvas: c }: { canvas: HTMLCanvasElement }) {
  canvas.value = c
}

async function confirm() {
  processing.value = true
  try {
    const src = canvas.value
    if (!src) return
    // resize 到 512×512
    const out = document.createElement('canvas')
    out.width = 512
    out.height = 512
    const ctx = out.getContext('2d')
    if (!ctx) return
    ctx.drawImage(src, 0, 0, 512, 512)
    const blob: Blob = await new Promise((resolve) =>
      out.toBlob((b) => resolve(b as Blob), 'image/jpeg', 0.9)
    )
    emit('cropped', blob)
    emit('update:modelValue', false)
  } finally {
    processing.value = false
  }
}
</script>
```

- [ ] **Step 3: ProfileView 接入**

[ProfileView.vue](../../../frontend/src/views/profile/ProfileView.vue) 头像区改：选文件后不直接 uploadAvatar，先打开裁剪对话框。

- 加 ref/state：
```ts
import AvatarCropperDialog from '@/components/profile/AvatarCropperDialog.vue'
const cropperDialog = ref(false)
const cropperSrc = ref('')
```
- 改 `uploadAvatar`（原直接上传）→ 拆成「选文件 → 打开裁剪」+「裁剪后上传」：
```ts
async function uploadAvatar(file: File | null) {
  // v-file-input @update:model-value 收到 File → 打开裁剪对话框（不上传）
  if (!file) return
  cropperSrc.value = URL.createObjectURL(file)
  cropperDialog.value = true
}

async function uploadCroppedAvatar(blob: Blob) {
  // 裁剪后上传（复用原上传逻辑，blob 转 File）
  avatarUploading.value = true
  try {
    const file = new File([blob], 'avatar.jpg', { type: 'image/jpeg' })
    const formData = new FormData()
    formData.append('file', file)
    await api.post('/auth/me/avatar', formData, { headers: { 'Content-Type': 'multipart/form-data' } })
    await userStore.fetchUser()
    notify('头像已更新', 'success')
  } catch (e: any) {
    notify('头像上传失败：' + (e?.userMessage || e?.message || '未知错误'), 'error')
  } finally {
    avatarUploading.value = false
    URL.revokeObjectURL(cropperSrc.value)
    cropperSrc.value = ''
  }
}
```
- 模板加裁剪对话框（accountDialog 外，根模板区）：
```vue
<AvatarCropperDialog v-model="cropperDialog" :src="cropperSrc" @cropped="uploadCroppedAvatar" />
```

注意：原 `uploadAvatar` 的上传部分搬到 `uploadCroppedAvatar`；原 `uploadAvatar` 改成只打开裁剪对话框。确认 `api` / `notify` / `avatarUploading` / `userStore` 引用对（读 ProfileView 现有）。

- [ ] **Step 4: build**

Run（在 `frontend/`）: `npm run build`
Expected: 成功（vue-advanced-cropper 引入 + 样式）

- [ ] **Step 5: Checkpoint**

建议提交：`feat(frontend): 头像裁剪（vue-advanced-cropper，512×512 输出）`

---

## Task 6: 前端·向导增强（按钮去重 + PicGo 字段 + url_style 提示）

**Files:**
- Modify: `frontend/src/views/admin/StorageConfigView.vue`

**Context:** spec §3.3、§3.4、§3.5。v-stepper hide-actions；向导②加 PicGo 三字段（base_path/custom_domain/url_suffix）+ url_style 提示；配置展示卡显示三字段。

- [ ] **Step 1: v-stepper 关默认按钮**

[StorageConfigView.vue](../../../frontend/src/views/admin/StorageConfigView.vue) 的 `<v-stepper>` 加 `:hide-actions="true"`（关默认 PREVIOUS/NEXT），只留自定义中文「上一步/下一步」按钮组。

- [ ] **Step 2: 向导②加 PicGo 三字段 + url_style 提示**

向导②填配置区（S3 字段后）加：
```vue
<v-text-field v-model="wizardConfig.s3_base_path" label="存储路径前缀（可选）"
  placeholder="如 livecalc/（留空则存 bucket 根目录）" variant="outlined" density="compact" class="mb-2" />
<v-text-field v-model="wizardConfig.s3_custom_domain" label="自定义域名（可选）"
  placeholder="如 https://cdn.example.com（留空用 endpoint 拼）" variant="outlined" density="compact" class="mb-2" />
<v-text-field v-model="wizardConfig.s3_url_suffix" label="网址后缀（可选）"
  placeholder="如 ?imageslim（图床处理参数）" variant="outlined" density="compact" class="mb-2" />
```
url_style 字段加 hint（或下方说明）：
```vue
<v-radio-group v-model="wizardConfig.s3_url_style" inline>
  <template #label><span class="text-body-2">URL 风格</span></template>
  <v-radio value="path" label="路径风格（MinIO/自建）" />
  <v-radio value="virtual" label="虚拟主机（OSS/AWS S3）" />
</v-radio-group>
<div class="text-caption text-medium-emphasis mb-2">
  OSS / AWS S3 选「虚拟主机」（&lt;bucket&gt;.&lt;host&gt;）；MinIO / 自建选「路径风格」（&lt;host&gt;/&lt;bucket&gt;）。不确定时 OSS/AWS 选 virtual，自建选 path。
</div>
```

`wizardConfig` reactive 加 `s3_base_path/s3_custom_domain/s3_url_suffix`（默认空串）。

- [ ] **Step 3: test/migrate/PUT payload 加 PicGo 三字段**

- 向导③ `POST /admin/storage-config/test` body：加 s3_base_path/s3_custom_domain/s3_url_suffix（wizardConfig 值）
- 向导④ `POST /admin/storage-config/migrate` s3_config：加三字段（to_s3 时传）
- 向导⑤ `PUT /admin/storage-config` payload：加三字段

- [ ] **Step 4: 配置展示卡显示三字段**

GET 展示卡（S3 配置区）加 base_path/custom_domain/url_suffix 显示（读 config.s3_base_path 等，空则「未设置」）。

- [ ] **Step 5: build + 手动核验要点**

Run: `npm run build`（在 frontend/）

- [ ] **Step 6: Checkpoint**

建议提交：`feat(frontend): 向导按钮去重 + PicGo 三字段 + url_style 提示`

---

## Task 7: 回归 + cc 记录

**Files:** 无代码改动（回归 + 文档 + OSS 真机手测）

- [ ] **Step 1: 后端 compileall + 存储单测**

Run: `.venv/Scripts/python.exe -m compileall backend/app`
Run: `.venv/Scripts/python.exe -m pytest backend/tests/services/storage/ backend/tests/services/test_region_admin.py backend/tests/services/test_region_country_zh.py -v`
Expected: 全过（含 Task 1 OSS Config + Task 3 PicGo + Task 4 国家级中文）

- [ ] **Step 2: 全量 pytest 回归**

Run: `.venv/Scripts/python.exe -m pytest backend/tests/ -q --no-header 2>&1 | tail -30`
Expected: 失败数不高于基线（2026-07-20 Task 12 记录的 23 预存失败）。无本次引入的新失败。

- [ ] **Step 3: 前端 build**

Run: `npm run build`（在 frontend/）

- [ ] **Step 4: 【关键】OSS 真机手测（需用户阿里云 OSS bucket）**

**单测只能验证 Config 参数对，OSS 兼容真机验证必须用户手测**。清单交用户：
1. 后台「图片存储」向导：填 OSS 配置（endpoint/bucket/ak/sk/region=oss-cn-xxx/url_style=**virtual**）
2. 点「测试连接」（向导③）—— 若仍报 `STREAMING-UNSIGNED-PAYLOAD-TRAILER`，说明 Task 1 的 Config 参数不够，需加更多（如 `s3={"payload_signing_enabled": True, "addressing_style": "virtual"}` 单独不够时，试 `botocore.config.Config` 的 `signature_version="s3"`（旧 v2）或降级 boto3 版本）
3. 测试通过 → 迁移 → 确认切换 → 前端图从 OSS 加载（验证 custom_domain/base_path/url_suffix 生效）
4. 国家级下拉：re-seed 后显示中文（点数据维护中心「更新行政区划数据」按钮触发）

**若 OSS 仍报错**：反馈具体错误，迭代 Config 参数（可能要 `payload_signing_enabled` + `content_sha256` 计算，或换 `aws-request-signing` 适配）。

- [ ] **Step 5: cc 记录**

新建 `cc/FEATURE_头像裁剪与存储配置增强.md`，按 [CLAUDE.md](../../../CLAUDE.md) 索引记录：
- 头像裁剪（vue-advanced-cropper 512×512）
- 国家级中文化（COUNTRY_NAMES_ZH + _upsert_region 字段刷新）
- 向导按钮去重（v-stepper hide-actions）
- PicGo 三字段（base_path/custom_domain/url_suffix，DB 存逻辑 key）
- url_style 提示
- OSS 兼容修复（payload_signing_enabled + checksum 控制，真机验证结果）
- CLAUDE.md 加索引行（一行指向新 cc）

- [ ] **Step 6: Checkpoint**

建议提交：`docs: 头像裁剪与存储配置增强 要点记录`

---

## 执行顺序说明

- Task 1（OSS 修复）先做——用户实测阻塞点，且 Task 3 改 s3.py 要基于 Task 1 的 Config。
- Task 2（PicGo 数据层）→ Task 3（PicGo 逻辑层）顺序（Task 3 依赖 Task 2 字段）。
- Task 4（国家级中文）独立，可与 Task 2/3 并行。
- Task 5（头像裁剪）/ Task 6（向导增强）独立，可并行。
- Task 7（回归 + cc）收尾，含 OSS 真机手测（用户）。
