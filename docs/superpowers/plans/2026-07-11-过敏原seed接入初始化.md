# 过敏原 seed 接入初始化流程 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 13 个过敏原黑名单分组种子数据接入后端 lifespan 启动流程，使空库首次启动自动创建、有数据则跳过（幂等自愈）。

**Architecture:** 把种子数据与创建逻辑从独立脚本抽到 `app/services/allergen_seed.py` 作为唯一数据源；lifespan 调幂等入口 `ensure_allergen_groups`；原 `scripts/seed_allergen_groups.py` 瘦身为壳，复用同一份逻辑、保留命令行清空重建行为。

**Tech Stack:** Python 3 + FastAPI + SQLAlchemy ORM（PG 生产 / 内存 SQLite 测试）；pytest + conftest 的 `db_session` fixture。

**项目约束（来自 CLAUDE.md，覆盖 skill 默认行为）：**
- **不自动 git commit / 建分支**——用户未要求时绝不执行 git 操作。本计划用「✋ Checkpoint」步骤替代 commit 步骤，每完成一个 Task 向用户汇报，由用户决定是否提交。
- **不在对话中启动服务**——后端已开启自动重载（uvicorn reload），改 `main.py` 后会自动重跑 lifespan；验证靠查 DB + `py_compile`，不手动起服务。
- 后端修改须确保无语法错误；虚拟环境在根目录 `.venv`，命令前缀 `.venv/Scripts/python.exe`。

---

## 文件结构

| 文件 | 动作 | 职责 |
|---|---|---|
| `backend/app/services/allergen_seed.py` | **新建** | `ALLERGEN_GROUPS` 常量（唯一数据源）+ `_create_groups(db)` 纯创建逻辑 + `ensure_allergen_groups(db)` 幂等入口 |
| `backend/tests/services/test_allergen_seed.py` | **新建** | 单测：空库创建 / 已有跳过 / 双调幂等 / 缺失原料容错 |
| `backend/app/main.py` | **修改** :286 后插入 | lifespan 调 `ensure_allergen_groups(db)`，try/except 兜底 |
| `backend/scripts/seed_allergen_groups.py` | **修改** 整文件 | 瘦身为壳，import services 的常量与逻辑，保留命令行 destructive 行为 |

---

## Task 1: 新建 `app/services/allergen_seed.py` + 单测（TDD）

**Files:**
- Create: `backend/tests/services/test_allergen_seed.py`
- Create: `backend/app/services/allergen_seed.py`

- [ ] **Step 1: 写失败测试**

创建 `backend/tests/services/test_allergen_seed.py`：

```python
"""过敏原 seed 服务测试。"""
import pytest

from app.models.blacklist_group import BlacklistGroup, BlacklistGroupIngredient
from app.models.nutrition import Ingredient
from app.services.allergen_seed import (
    ALLERGEN_GROUPS,
    _create_groups,
    ensure_allergen_groups,
)


def _clean_blacklist(db):
    """清空 BlacklistGroup 及映射，隔离测试。"""
    db.query(BlacklistGroupIngredient).delete()
    db.query(BlacklistGroup).delete()
    db.commit()


@pytest.fixture()
def clean_db(db_session):
    """每个测试前后清空黑名单分组表（db_session 是共享内存库，须自隔离）。"""
    _clean_blacklist(db_session)
    yield db_session
    _clean_blacklist(db_session)


def _add_ingredients(db):
    """预置若干原料供名称匹配（名字取自 ALLERGEN_GROUPS 真实成员）。"""
    for name in ["鸡蛋", "花生", "牛奶"]:
        db.add(Ingredient(name=name, is_active=True))
    db.commit()


def test_ensure_creates_groups_when_empty(clean_db):
    db = clean_db
    _add_ingredients(db)

    ensure_allergen_groups(db)

    assert db.query(BlacklistGroup).count() == len(ALLERGEN_GROUPS)
    assert db.query(BlacklistGroupIngredient).count() >= 3


def test_ensure_skips_when_already_populated(clean_db):
    db = clean_db
    db.add(BlacklistGroup(name="占位组", display_order=99, is_active=True))
    db.commit()

    ensure_allergen_groups(db)

    assert db.query(BlacklistGroup).count() == 1


def test_ensure_idempotent_on_double_call(clean_db):
    db = clean_db
    _add_ingredients(db)

    ensure_allergen_groups(db)
    first_groups = db.query(BlacklistGroup).count()
    first_maps = db.query(BlacklistGroupIngredient).count()

    ensure_allergen_groups(db)

    assert db.query(BlacklistGroup).count() == first_groups
    assert db.query(BlacklistGroupIngredient).count() == first_maps


def test_create_groups_handles_missing_ingredient(clean_db):
    """_create_groups 对缺失原料容错：计入 not_found、不抛错。

    共享内存库可能有残留原料，但 ALLERGEN_GROUPS 名字远多于测试预置，
    必有未命中项；只断言容错行为，不断言具体匹配数。
    """
    db = clean_db

    created, mappings, not_found = _create_groups(db)

    assert created == len(ALLERGEN_GROUPS)
    assert len(not_found) >= 1
```

- [ ] **Step 2: 跑测试验证失败（模块不存在）**

Run（在 `backend/` 下）:
```bash
.venv/Scripts/python.exe -m pytest tests/services/test_allergen_seed.py -v
```
Expected: FAIL，`ModuleNotFoundError: No module named 'app.services.allergen_seed'`

- [ ] **Step 3: 实现 `app/services/allergen_seed.py`**

创建 `backend/app/services/allergen_seed.py`。`ALLERGEN_GROUPS` 的 13 组数据**从 `scripts/seed_allergen_groups.py:22-159` 原样搬入**（即该文件现有 `ALLERGEN_GROUPS = [ ... ]` 整块），下面给出完整文件框架（数据块用注释标明边界，执行时贴入真实 13 组）：

```python
"""过敏原黑名单分组种子数据与服务。

将 GB 7718-2025 的 13 类过敏原分组接入数据库初始化流程：
- lifespan 启动时调 ``ensure_allergen_groups`` 幂等创建；
- 命令行脚本 ``scripts/seed_allergen_groups.py`` 复用本模块数据与逻辑，
  保留 destructive 清空重建行为。
"""
import logging

from sqlalchemy.orm import Session

from app.models.blacklist_group import BlacklistGroup, BlacklistGroupIngredient
from app.models.nutrition import Ingredient

logger = logging.getLogger(__name__)


# ── 13 个过敏原分组及其原料名称映射（GB 7718-2025）──────────────
# 以下 ALLERGEN_GROUPS 列表原样复制自 scripts/seed_allergen_groups.py 第 22-159 行，
# 包含 13 个分组（含麸质谷物/甲壳纲/鱼类/蛋类/花生/大豆/乳类/坚果/
# 芝麻/芹菜/芥末/软体动物/椰子），每组含 name、display_order、ingredient_names。
ALLERGEN_GROUPS = [
    # >>> 在此贴入 scripts/seed_allergen_groups.py:23-158 的完整 13 个字典 <<<
]


def _create_groups(db: Session):
    """创建全部过敏原分组及原料映射（纯创建，不清理、不自开 session）。

    返回 ``(created_count, mapping_count, not_found)``。
    读取 ingredient_names 采用非破坏性 ``.get``，保证 ALLERGEN_GROUPS
    作为模块级常量可被反复调用（原脚本的 ``.pop`` 会破坏常量，已弃用）。
    """
    # 1) 构建 name → id 查找表（含别名）
    all_ings = db.query(Ingredient).filter(Ingredient.is_active == True).all()
    name_to_id = {}
    for ing in all_ings:
        name_to_id.setdefault(ing.name, ing.id)
        if ing.aliases:
            aliases = ing.aliases if isinstance(ing.aliases, list) else []
            for alias in aliases:
                if alias and alias not in name_to_id:
                    name_to_id[alias] = ing.id

    # 2) 逐组创建 + 映射
    created_count = 0
    mapping_count = 0
    not_found = []

    for group_data in ALLERGEN_GROUPS:
        ingredient_names = group_data.get("ingredient_names", [])  # 非破坏性读取
        found_ids = []
        for name in ingredient_names:
            ing_id = name_to_id.get(name)
            if ing_id:
                found_ids.append(ing_id)
            else:
                not_found.append(f"[{group_data['name']}] 未找到: {name}")
        found_ids = list(set(found_ids))  # 去重

        group = BlacklistGroup(
            name=group_data["name"],
            display_order=group_data["display_order"],
            is_active=True,
        )
        db.add(group)
        db.flush()

        for ing_id in found_ids:
            db.add(BlacklistGroupIngredient(
                group_id=group.id,
                ingredient_id=ing_id,
                is_ai_matched=False,
                is_active=True,
            ))
            mapping_count += 1
        created_count += 1

    db.commit()
    return created_count, mapping_count, not_found


def ensure_allergen_groups(db: Session) -> None:
    """幂等确保过敏原分组存在：空表才创建，有数据则跳过。"""
    existing = db.query(BlacklistGroup).count()
    if existing > 0:
        logger.info("过敏原分组已存在（%s 个），跳过初始化", existing)
        return
    created, mappings, not_found = _create_groups(db)
    logger.info(
        "过敏原分组初始化完成：%s 组，%s 条映射，未匹配 %s 条",
        created, mappings, len(not_found),
    )
```

- [ ] **Step 4: 跑测试验证通过**

Run:
```bash
.venv/Scripts/python.exe -m pytest tests/services/test_allergen_seed.py -v
```
Expected: 4 passed

- [ ] **Step 5: ✋ Checkpoint**

向用户汇报：services 模块 + 单测完成、4 passed。等待用户确认是否提交。

---

## Task 2: 接入 `main.py` lifespan

**Files:**
- Modify: `backend/app/main.py`（在 :286「批量建商品」收尾之后、:288「USDA 索引」之前插入）

- [ ] **Step 1: 插入 ensure 调用**

在 `main.py` 的 lifespan 中，定位到批量建商品 `elif product_count > 0:` 分支结束后（约 :286 行）、`# 加载 USDA 搜索索引` 注释（:288）之前，插入：

```python
        # 确保过敏原黑名单分组存在（空表才创建，幂等，失败不阻断启动）
        try:
            from app.services.allergen_seed import ensure_allergen_groups
            ensure_allergen_groups(db)
        except Exception as e:
            logger.warning(f"过敏原分组初始化失败: {e}")
```

位置理由：必须排在批量建商品之后（seed 按原料名查 ID，此时 ingredients/products 已就绪）；不放进 `first_run_init_recipes` 分支（该开关默认 False 会致永不执行）。

- [ ] **Step 2: 语法校验**

Run:
```bash
.venv/Scripts/python.exe -m py_compile app/main.py app/services/allergen_seed.py
```
Expected: 无输出（语法正确）

- [ ] **Step 3: 验证 lifespan 实际生效（查 DB）**

后端自动重载会重跑 lifespan。等待重载完成后查 PG（`.venv` 在根目录）:
```bash
cd backend && ../.venv/Scripts/python.exe -X utf8 -c "from app.core.database import SessionLocal; from app.models.blacklist_group import BlacklistGroup, BlacklistGroupIngredient; db=SessionLocal(); print('groups=', db.query(BlacklistGroup).count()); print('mappings=', db.query(BlacklistGroupIngredient).count()); db.close()"
```
Expected: `groups= 13`，`mappings= ` 某正数（具体取决于库内现有原料命中数）

- [ ] **Step 4: ✋ Checkpoint**

向用户汇报：lifespan 接入完成、PG 已出现 13 组。等待确认。

---

## Task 3: 改造 `scripts/seed_allergen_groups.py` 为壳

**Files:**
- Modify: `backend/scripts/seed_allergen_groups.py`（整文件替换）

- [ ] **Step 1: 整文件替换为壳**

将 `backend/scripts/seed_allergen_groups.py` 整体替换为：

```python
"""过敏原分组种子脚本（命令行：清空重建）。

复用 ``app.services.allergen_seed`` 的 ALLERGEN_GROUPS 数据与 _create_groups 逻辑。
lifespan 启动时走幂等 ``ensure_allergen_groups``；本脚本走 destructive
清空重建，供开发者手动重跑（如调整分组定义后强制刷新）。

执行方式: python -X utf8 scripts/seed_allergen_groups.py
"""
import sys
import os

sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..'))
os.environ.setdefault('DATABASE_URL', 'sqlite:///data/livecalc.db')

from app.core.database import SessionLocal
from app.models.blacklist_group import BlacklistGroup, BlacklistGroupIngredient
from app.services.allergen_seed import ALLERGEN_GROUPS, _create_groups


def seed_allergen_groups():
    """清空重建过敏原分组（destructive，命令行专用）。"""
    db = SessionLocal()
    try:
        # 1) 清理旧数据
        existing_count = db.query(BlacklistGroup).count()
        if existing_count > 0:
            print(f"🧹 清理 {existing_count} 个旧分组及关联映射...")
            db.query(BlacklistGroupIngredient).delete()
            db.query(BlacklistGroup).delete()
            db.flush()
            print("   已清理完成")

        # 2) 复用 services 的创建逻辑
        created, mappings, not_found = _create_groups(db)

        print(f"\n🎉 完成！共创建 {created} 个分组，关联 {mappings} 条原料映射。")
        if not_found:
            print(f"\n⚠️  以下原料未找到（共 {len(not_found)} 条）：")
            for msg in not_found:
                print("  " + msg)

    except Exception as e:
        db.rollback()
        print(f"❌ 发生错误：{e}")
        raise
    finally:
        db.close()


if __name__ == "__main__":
    print("=" * 60)
    print("🌰  过敏原分组种子脚本 - GB 7718-2025（清空重建）")
    print("=" * 60)
    seed_allergen_groups()
```

- [ ] **Step 2: 语法校验**

Run:
```bash
.venv/Scripts/python.exe -m py_compile scripts/seed_allergen_groups.py
```
Expected: 无输出

- [ ] **Step 3: ✋ Checkpoint（脚本不强制运行）**

向用户汇报：脚本已改为壳、复用 services 逻辑。**不主动运行**（destructive 会清空现有分组；如需验证由用户决定，命令：`python -X utf8 backend/scripts/seed_allergen_groups.py`）。

---

## Task 4: 整体验证

- [ ] **Step 1: 全量语法校验**

Run:
```bash
.venv/Scripts/python.exe -m compileall app/main.py app/services/allergen_seed.py scripts/seed_allergen_groups.py
```
Expected: `Listing ...` 无 Error

- [ ] **Step 2: 重跑新单测确认无回归**

Run:
```bash
.venv/Scripts/python.exe -m pytest tests/services/test_allergen_seed.py -v
```
Expected: 4 passed

- [ ] **Step 3: 最终 DB 现状核对**

Run（查 PG）:
```bash
cd backend && ../.venv/Scripts/python.exe -X utf8 -c "from app.core.database import SessionLocal; from app.models.blacklist_group import BlacklistGroup; db=SessionLocal(); rows=db.query(BlacklistGroup).order_by(BlacklistGroup.display_order).all(); print('total=', len(rows)); [print(' ', g.display_order, g.name) for g in rows]; db.close()"
```
Expected: `total= 13`，列出 13 个分组名（含麸质谷物…椰子）

- [ ] **Step 4: ✋ Checkpoint（最终）**

向用户汇报全部完成：services 模块 + lifespan 接入 + 脚本改造 + 单测 + DB 核对均通过。由用户决定是否记录要点到 `cc/`、是否提交。

---

## Self-Review（plan 作者自查）

**1. Spec 覆盖：**
- 组件拆分（ALLERGEN_GROUPS / _create_groups / ensure）→ Task 1 ✓
- scripts 改壳 → Task 3 ✓
- lifespan 接入点（批量建商品后、USDA 前）→ Task 2 ✓
- pop 破坏性修复 → Task 1 Step 3 用 `.get` 替代 `.pop`，并有注释说明 ✓
- 幂等（count>0 跳过）→ ensure 实现 + test_ensure_skips + test_idempotent ✓
- 测试四例 → Task 1 ✓
- 验证标准（compileall / 13 组 / 幂等重启）→ Task 2/4 ✓
- 无表结构变更 → 全程无 alembic/SQL ✓

**2. 占位符扫描：** Task 1 Step 3 的 `ALLERGEN_GROUPS` 用了「贴入 scripts:22-159 真实数据」的明确引用（源真实存在、行号确定），非 TBD/TODO。其余步骤代码完整。

**3. 类型/签名一致性：**
- `_create_groups(db) -> (created_count, mapping_count, not_found)`：Task 1 定义、Task 1 测试、Task 3 调用一致 ✓
- `ensure_allergen_groups(db) -> None`：Task 1 定义、Task 2 调用一致 ✓
- `BlacklistGroup(name, display_order, is_active)` / `BlacklistGroupIngredient(group_id, ingredient_id, is_ai_matched, is_active)`：与 [blacklist_group.py](backend/app/models/blacklist_group.py) + AuditMixin 字段一致 ✓

无问题，可执行。
