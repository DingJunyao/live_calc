# 未使用图片清理增强 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让「未使用图片清理」功能覆盖头像目录/`users.avatar`/`Product.image_url` 引用，并在 S3 后端下正常工作。

**Architecture:** StorageBackend protocol 加 `list` 方法（Local/S3 各自实现）→ GET 端点用 `storage.list()` 扫全目录 + 收集三源 used keys → DELETE 端点接受完整 key → 前端分组展示。

**Tech Stack:** FastAPI / SQLAlchemy / boto3（后端），Vue 3 + Vuetify（前端），pytest（测试）。

**项目约定：**
- 不主动 git commit / 建分支；每个 Task 以验证通过收尾。
- 后端虚拟环境在 `d:\code\live_calc\.venv`，测试/编译用 `.venv\Scripts\python.exe`，从 backend 目录跑 pytest。
- 前端用 `npm --prefix frontend run build`。
- 不要在对话中启动服务（已自动重载）。

**Spec:** `docs/superpowers/specs/2026-07-22-未使用图片清理增强-design.md`

---

## File Structure

- `backend/app/services/storage/base.py` — StorageBackend protocol 加 `list` 方法
- `backend/app/services/storage/local.py` — LocalBackend.list 实现
- `backend/app/services/storage/s3.py` — S3Backend.list 实现（复用 migrate.py 的分页模式）
- `backend/app/api/admin.py` — GET/DELETE 端点重构；抽取 `_collect_used_image_keys` 辅助
- `frontend/src/views/admin/UnusedImagesView.vue` — 按子目录分组、传完整 key
- `backend/tests/services/test_storage_list.py`（新建）— list 方法 + 端点逻辑测试

---

## Task 1：StorageBackend protocol 加 `list` + LocalBackend 实现

**Files:**
- Modify: `backend/app/services/storage/base.py`（protocol 加 `list`）
- Modify: `backend/app/services/storage/local.py`（LocalBackend.list）
- Test: `backend/tests/services/test_storage_list.py`（新建）

- [ ] **Step 1：写失败测试**

创建 `backend/tests/services/test_storage_list.py`：

```python
"""StorageBackend list 方法测试。"""
from pathlib import Path
from app.services.storage.local import LocalBackend


def test_local_list_returns_all_keys(tmp_path):
    images = tmp_path / "images"
    (images / "recipes").mkdir(parents=True)
    (images / "recipes" / "a.jpg").write_bytes(b"a")
    (images / "recipes" / "sub" / "b.jpg").mkdir(parents=True)
    (images / "recipes" / "sub" / "b.jpg").write_bytes(b"b")
    (images / "avatars" / "c.png").mkdir(parents=True)
    (images / "avatars" / "c.png").write_bytes(b"c")

    lb = LocalBackend(root=tmp_path)
    keys = lb.list("")
    assert "recipes/a.jpg" in keys
    assert "recipes/sub/b.jpg" in keys
    assert "avatars/c.png" in keys
    assert len(keys) == 3


def test_local_list_prefix_filters(tmp_path):
    images = tmp_path / "images"
    (images / "recipes" / "a.jpg").mkdir(parents=True)
    (images / "recipes" / "a.jpg").write_bytes(b"a")
    (images / "avatars" / "b.png").mkdir(parents=True)
    (images / "avatars" / "b.png").write_bytes(b"b")

    lb = LocalBackend(root=tmp_path)
    keys = lb.list("recipes/")
    assert keys == ["recipes/a.jpg"]


def test_local_list_empty_dir(tmp_path):
    lb = LocalBackend(root=tmp_path)
    assert lb.list("") == []
```

- [ ] **Step 2：跑测试，确认失败**

```
Push-Location d:\code\live_calc\backend; & 'd:\code\live_calc\.venv\Scripts\python.exe' -m pytest tests/services/test_storage_list.py -v; Pop-Location
```
Expected: FAIL（`list` 不存在 / LocalBackend 未实现）。

- [ ] **Step 3：在 `base.py` 的 `StorageBackend` protocol 加 `list`**

在 `base.py` 的 `url_for` 之后追加：

```python
    def list(self, prefix: str = "") -> list[str]:
        """列出给定 prefix 下的所有 key（递归）。prefix 为空时列全部。"""
        ...
```

- [ ] **Step 4：在 `local.py` 的 `LocalBackend` 添加 `list`**

在 `LocalBackend.url_for` 之后（或 `exists` 与 `url_for` 之间）追加：

```python
    def list(self, prefix: str = "") -> list[str]:
        """列出给定 prefix 下的所有 key（递归）。prefix 为空时从 images 根目录列起。"""
        scan_dir = self.images_dir / prefix if prefix else self.images_dir
        if not scan_dir.exists():
            return []
        keys = []
        for fp in sorted(scan_dir.rglob("*")):
            if fp.is_file():
                rel = fp.relative_to(self.images_dir).as_posix()
                keys.append(rel)
        return keys
```

- [ ] **Step 5：跑测试，确认通过**

```
Push-Location d:\code\live_calc\backend; & 'd:\code\live_calc\.venv\Scripts\python.exe' -m pytest tests/services/test_storage_list.py::test_local_list_returns_all_keys test_storage_list.py::test_local_list_prefix_filters test_storage_list.py::test_local_list_empty_dir -v; Pop-Location
```
Expected: PASS (3/3).

---

## Task 2：S3Backend 实现 list

**Files:**
- Modify: `backend/app/services/storage/s3.py`
- Test: `backend/tests/services/test_storage_list.py`（追加）

- [ ] **Step 1：写失败测试（追加到 test_storage_list.py）**

```python
from unittest.mock import MagicMock, patch


def test_s3_list_returns_all_keys():
    """模拟 list_objects_v2 分页，验证 S3Backend.list 返回全部 key。"""
    from app.services.storage.s3 import S3Backend

    fake_client = MagicMock()
    # 第一页（IsTruncated），第二页（不截断）
    fake_client.list_objects_v2.side_effect = [
        {"Contents": [{"Key": "recipes/a.jpg"}], "IsTruncated": True,
         "NextContinuationToken": "tok1"},
        {"Contents": [{"Key": "avatars/b.png"}], "IsTruncated": False},
    ]
    backend = S3Backend(
        endpoint="https://s3.example.com", bucket="test",
        access_key="ak", secret_key="sk",
    )
    backend.client = fake_client  # 替换为 mock
    keys = backend.list("")
    assert "recipes/a.jpg" in keys
    assert "avatars/b.png" in keys
    assert len(keys) == 2
    # 验证分页传参
    assert fake_client.list_objects_v2.call_count == 2
    _, kwargs = fake_client.list_objects_v2.call_args
    assert kwargs.get("ContinuationToken") == "tok1"


def test_s3_list_prefix_passed():
    from app.services.storage.s3 import S3Backend

    fake_client = MagicMock()
    fake_client.list_objects_v2.return_value = {"Contents": [], "IsTruncated": False}
    backend = S3Backend(
        endpoint="https://s3.example.com", bucket="test",
        access_key="ak", secret_key="sk",
    )
    backend.client = fake_client
    backend.list("recipes/")
    _, kwargs = fake_client.list_objects_v2.call_args
    assert kwargs.get("Prefix") == "recipes/"
```

- [ ] **Step 2：跑测试，确认失败**

```
Push-Location d:\code\live_calc\backend; & 'd:\code\live_calc\.venv\Scripts\python.exe' -m pytest tests/services/test_storage_list.py::test_s3_list_returns_all_keys test_storage_list.py::test_s3_list_prefix_passed -v; Pop-Location
```
Expected: FAIL（S3Backend 无 list 方法）。

- [ ] **Step 3：在 `s3.py` 的 `S3Backend` 添加 `list`**

在 `S3Backend.url_for` 之后（建议放在 `delete` 与 `exists` 之间）追加：

```python
    def list(self, prefix: str = "") -> list[str]:
        """列出给定 prefix 下的所有 key（递归）。分页自动遍历。"""
        keys: list[str] = []
        continuation = None
        kwargs = {"Bucket": self.bucket}
        if prefix:
            kwargs["Prefix"] = prefix
        while True:
            if continuation:
                kwargs["ContinuationToken"] = continuation
            resp = self.client.list_objects_v2(**kwargs)
            for obj in resp.get("Contents", []):
                keys.append(obj["Key"])
            if resp.get("IsTruncated") and resp.get("NextContinuationToken"):
                continuation = resp["NextContinuationToken"]
            else:
                break
        return keys
```

- [ ] **Step 4：跑测试，确认通过**

```
Push-Location d:\code\live_calc\backend; & 'd:\code\live_calc\.venv\Scripts\python.exe' -m pytest tests/services/test_storage_list.py::test_s3_list_returns_all_keys test_storage_list.py::test_s3_list_prefix_passed -v; Pop-Location
```
Expected: PASS (2/2).

---

## Task 3：后端 GET/DELETE 端点重构

**Files:**
- Modify: `backend/app/api/admin.py`（GET + DELETE 端点，抽 `_collect_used_image_keys`）
- Test: `backend/tests/services/test_storage_list.py`（追加端点逻辑测试）

- [ ] **Step 1：写失败测试**

追加到 `test_storage_list.py`：

```python
from unittest.mock import MagicMock


def _mock_db():
    """造一个 mock db，query().all() 返回指定数据。"""
    class FakeQuery:
        def __init__(self, rows):
            self.rows = rows
        def filter(self, *a, **kw):
            return self
        def all(self):
            return self.rows

    class FakeSession:
        def __init__(self):
            self.recipes = [([],), (["recipes/a.jpg", "recipes/b.jpg"],)]
            self.users = [("avatars/old.png",), (None,)]
            self.products = [("http://example.com/img.png",), ("recipes/c.jpg",), (None,)]

        def query(self, model_name):
            """模拟 query() 返回链式调用的 FakeQuery。"""
            name = getattr(model_name, "__name__", "")
            if "Recipe" in name:
                return FakeQuery(self.recipes)
            if "User" in name:
                return FakeQuery(self.users)
            if "Product" in name or "ProductEntity" in name:
                return FakeQuery(self.products)
            return FakeQuery([])

    return FakeSession()


def test_collect_used_keys_aggregates_all_sources(monkeypatch):
    """验证 _collect_used_image_keys 收集 Recipe.images + User.avatar + Product.image_url。"""
    import app.api.admin as admin_mod

    db = _mock_db()
    used = admin_mod._collect_used_image_keys(db)
    assert "recipes/a.jpg" in used
    assert "recipes/b.jpg" in used
    assert "avatars/old.png" in used
    assert "recipes/c.jpg" in used           # 内部 key
    assert "http://example.com/img.png" not in used  # 外部 URL 排除
    assert len(used) == 4
```

- [ ] **Step 2：跑测试，确认失败**

```
Push-Location d:\code\live_calc\backend; & 'd:\code\live_calc\.venv\Scripts\python.exe' -m pytest tests/services/test_storage_list.py::test_collect_used_keys_aggregates_all_sources -v; Pop-Location
```
Expected: FAIL（`_collect_used_image_keys` 不存在）。

- [ ] **Step 3：在 `admin.py` 中抽取 `_collect_used_image_keys`**

在文件顶部（`get_storage` 导入之后）插入：

```python
def _collect_used_image_keys(db) -> set[str]:
    """收集所有图片字段引用的 storage key，供未使用图片清理使用。

    来源：Recipe.images（JSON list）、users.avatar（单 key）、
    ProductEntity.image_url（仅不以 http 开头的内部 key）。
    """
    used: set[str] = set()

    for (images_list,) in db.query(Recipe.images).all():
        if images_list:
            for img in images_list:
                if isinstance(img, str) and img:
                    used.add(img)

    for (avatar,) in db.query(User.avatar).filter(User.avatar.isnot(None)).all():
        if avatar:
            used.add(avatar)

    for (url,) in db.query(ProductEntity.image_url).filter(ProductEntity.image_url.isnot(None)).all():
        if url and not url.startswith("http"):
            used.add(url)

    return used
```

并在文件顶部补充 import：查看现有 import 确认是否需要加 `User`、`ProductEntity`。如果已有则不用。

- [ ] **Step 4：重构 `get_unused_images`**

替换当前 `get_unused_images`（~line 247-283）为：

```python
@router.get("/images/unused")
async def get_unused_images(
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_admin_user),
):
    """获取服务器上未被任何菜谱/用户/商品引用的图片列表 - 仅限管理员。
    
    扫描 storage 后端中的所有图片文件，排除 Recipe.images、users.avatar、
    ProductEntity.image_url 中已引用的 key，返回未使用的图片列表。
    支持本地和 S3 存储后端。
    """
    used = _collect_used_image_keys(db)
    storage = get_storage()
    all_keys = storage.list("")

    unused = []
    for key in sorted(all_keys):
        if key not in used:
            unused.append({
                "key": key,
                "filename": key.split("/")[-1],
                "url": storage.url_for(key),
            })

    return {"images": unused}
```

- [ ] **Step 5：重构 `delete_unused_images`**

替换当前 DELETE 端点（~line 286-308）为：

```python
@router.post("/images/unused/delete")
async def delete_unused_images(
    body: dict,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_admin_user),
):
    """删除指定的未使用图片 - 仅限管理员
    
    接受 keys（完整 storage key 列表），直接调 storage.delete(key) 删除。
    不再硬编码 `recipes/` 前缀，支持所有子目录。
    """
    keys = body.get("keys", [])
    if not keys:
        raise HTTPException(status_code=400, detail="缺少 keys")

    storage = get_storage()
    deleted: list = []
    errors: list = []

    for key in keys:
        try:
            storage.delete(key)
            deleted.append(key)
        except Exception as e:
            errors.append(f"{key}: {str(e)}")

    return {"deleted": deleted, "errors": errors}
```

- [ ] **Step 6：更新 `_mock_db` 和测试，运行验证**

Step 1 的测试应该通过了。再验证 GET 端点的全流程：

```
Push-Location d:\code\live_calc\backend; & 'd:\code\live_calc\.venv\Scripts\python.exe' -m pytest tests/services/test_storage_list.py::test_collect_used_keys_aggregates_all_sources -v; Pop-Location
```
Expected: PASS.

也运行前面所有测试，确认无回归：

```
Push-Location d:\code\live_calc\backend; & 'd:\code\live_calc\.venv\Scripts\python.exe' -m pytest tests/services/test_storage_list.py -v; Pop-Location
```
Expected: 6 passed（local 3 + s3 2 + used_keys 1）。

---

## Task 4：前端 UnusedImagesView 分组 + 完整 key

**Files:**
- Modify: `frontend/src/views/admin/UnusedImagesView.vue`

- [ ] **Step 1：后端返回数据结构已变 —— 前端适配**

后端现在返回 `{key, filename, url}`（完整 key，不再只返 filename）。前端要：
- 按子目录（`key.split('/')[0]`）分组
- 删选时传完整 key
- 显示完整 key 代替纯 filename

- [ ] **Step 2：实现 — 替换 UnusedImagesView.vue 的 script 和 template**

**Script 改动：**

```ts
const images = ref<any[]>([])
const selected = ref<string[]>([])  // 存完整 key
const loading = ref(false)
const deleting = ref(false)
const errorMsg = ref('')
const successMsg = ref('')

// 计算分组
interface ImageGroup {
  dir: string
  label: string
  items: any[]
}
const groups = computed<ImageGroup[]>(() => {
  const map = new Map<string, any[]>()
  for (const img of images.value) {
    const dir = (img.key as string).split('/')[0] || 'other'
    if (!map.has(dir)) map.set(dir, [])
    map.get(dir)!.push(img)
  }
  const labels: Record<string, string> = {
    'recipes': '菜谱图片',
    'avatars': '头像',
  }
  return Array.from(map.entries())
    .map(([dir, items]) => ({ dir, label: labels[dir] || dir, items }))
})
```

**Template 改动：**

替换原来的 `<v-row>` 循环区域为分组渲染：

```html
<template v-for="group in groups" :key="group.dir">
  <v-divider class="my-2" />
  <div class="text-subtitle-2 font-weight-medium mb-2 mt-2">
    📁 {{ group.label }}
    <span class="text-caption text-medium-emphasis">（{{ group.items.length }} 张）</span>
  </div>
  <v-row>
    <v-col
      v-for="img in group.items"
      :key="img.key"
      cols="6" sm="4" md="3" lg="2"
    >
      <v-card
        elevation="1"
        :color="selected.includes(img.key) ? 'primary' : undefined"
        @click="toggleSelect(img.key)"
        style="cursor: pointer"
      >
        <v-img :src="img.url" height="100" cover class="bg-grey-lighten-3">
          <template #placeholder>
            <div class="d-flex align-center justify-center fill-height">
              <v-icon color="grey">mdi-image-off</v-icon>
            </div>
          </template>
        </v-img>
        <v-card-text class="pa-1 text-caption text-center text-truncate">
          {{ img.filename }}
        </v-card-text>
      </v-card>
    </v-col>
  </v-row>
</template>
```

**删除请求体改为 keys：**

```ts
const resp = await api.post('/admin/images/unused/delete', {
  keys: selected.value,
})
```

- [ ] **Step 3：构建验证**

```
npm --prefix frontend run build
```
Expected: 构建成功。

---

## Task 5：全量验证

- [ ] **Step 1：后端 py_compile**

```
d:\code\live_calc\.venv\Scripts\python.exe -m py_compile backend/app/services/storage/base.py backend/app/services/storage/local.py backend/app/services/storage/s3.py backend/app/api/admin.py
```
Expected: 无输出。

- [ ] **Step 2：跑全部新测试**

```
Push-Location d:\code\live_calc\backend; & 'd:\code\live_calc\.venv\Scripts\python.exe' -m pytest tests/services/test_storage_list.py -v; Pop-Location
```
Expected: 6 passed。

- [ ] **Step 3：前端 build**

```
npm --prefix frontend run build
```
Expected: 构建成功。

---

## Self-Review

- **Spec 覆盖**：storage.list（Task 1/2）、LocalBackend.list（Task 1）、S3Backend.list（Task 2）、GET 端点三源 used keys + storage.list 扫描（Task 3）、DELETE 端点接受 keys（Task 3）、前端分组 + 完整 key（Task 4）——全覆盖。
- **占位符**：无 TBD/TODO，每个 step 有完整代码或命令。
- **类型/签名一致性**：`list(prefix: str) -> list[str]` 在 Task 1 protocol 定义、Task 1/2 实现、Task 3 调用一致。`_collect_used_image_keys(db) -> set[str]` 在 Task 3 定义与调用一致。DELETE 接受 `{"keys": [...]}` 与前端传递一致。
