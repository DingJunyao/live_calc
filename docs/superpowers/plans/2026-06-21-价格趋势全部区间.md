# 价格趋势「全部」区间标签 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在菜谱、商品、原料三个详情页的价格趋势图「周/月/季/年」之后追加「全部」标签，可查看完整历史区间；并修正商品/原料图表原先基于「分页 10 条」聚合的问题，改为按时间区间取数。

**Architecture:** 纯前端改动，后端零改动（`/products` 已支持 `start_date/end_date`，菜谱 `cost-history-range` 已支持 `offset_days` 分批）。公共组件 `PriceTrendChart` 增加「全部」标签并在该区间不做时间切片；菜谱「全部」走前端循环分批（每批 365 天，空批止）规避 10s 超时；商品/原料引入图表专用累积池 `chartPriceRecords`，按时间区间累积加载，「全部」单请求放宽超时到 30s。

**Tech Stack:** Vue 3 + TypeScript + Vuetify + ECharts（前端）；后端无关。

---

## 全局约定（执行前必读）

- ⚠️ **Git 操作需用户明确授权**：本项目约定「用户未主动要求时，绝不擅自 `git commit/branch/push`」。每个任务末尾的「检查点」含建议的提交命令，但**仅在用户明确同意后才执行**；未获授权时，到检查点即暂停汇报。
- **前端验证**：项目无前端单元测试框架，每个任务以 `npm run build`（在 `frontend/` 下）通过为准，辅以手动验证。
- **不要在对话中启动服务**：前端 `npm run dev`、后端服务由用户已启动的自动重载实例承担，本计划不启动。
- **代码注释语言**：与现有代码一致，使用中文。
- **后端**：本计划不改动任何后端文件。

## File Structure

| 文件 | 职责 | 本次改动 |
|------|------|---------|
| `frontend/src/components/charts/PriceTrendChart.vue` | 三处共用的价格趋势图组件 | 加「全部」标签、`all` 不切片、清理重复 `watch` |
| `frontend/src/views/recipes/RecipeDetail.vue` | 菜谱详情 | `onCostTrendFilterChange` 加 `all` 分支 + 新增 `loadAllCostHistory` 分批循环 |
| `frontend/src/views/products/ProductDetail.vue` | 商品详情 | 新增图表累积池、`loadChartPriceRecords`、`onPriceTrendFilterChange`；`chartData` 换数据源；组件绑定 `@filter-change` |
| `frontend/src/views/ingredients/IngredientDetail.vue` | 原料详情 | 同商品（`product_id`→`ingredient_id`） |

---

## Task 1: 公共组件 `PriceTrendChart.vue` 增加「全部」标签

**Files:**
- Modify: `frontend/src/components/charts/PriceTrendChart.vue`

- [ ] **Step 1: 扩展 `emit` 的 `filter-change` 类型**

定位（约 L129-L131）：
```typescript
const emit = defineEmits<{
  'filter-change': [filter: 'week' | 'month' | 'quarter' | 'year']
}>()
```
改为：
```typescript
const emit = defineEmits<{
  'filter-change': [filter: 'week' | 'month' | 'quarter' | 'year' | 'all']
}>()
```

- [ ] **Step 2: 扩展 `selectedFilter` 类型**

定位（约 L139）：
```typescript
const selectedFilter = ref<'week' | 'month' | 'quarter' | 'year'>('month')
```
改为：
```typescript
const selectedFilter = ref<'week' | 'month' | 'quarter' | 'year' | 'all'>('month')
```

- [ ] **Step 3: `filters` 末尾追加「全部」**

定位（约 L141-L146）：
```typescript
const filters = [
  { label: '周', value: 'week' as const },
  { label: '月', value: 'month' as const },
  { label: '季', value: 'quarter' as const },
  { label: '年', value: 'year' as const }
]
```
改为：
```typescript
const filters = [
  { label: '周', value: 'week' as const },
  { label: '月', value: 'month' as const },
  { label: '季', value: 'quarter' as const },
  { label: '年', value: 'year' as const },
  { label: '全部', value: 'all' as const }
]
```

- [ ] **Step 4: `chartData` 在 `all` 区间不做时间切片**

定位（约 L152-L176，整个 `chartData` computed）：
```typescript
const chartData = computed(() => {
  if (!props.data || props.data.length === 0) return []

  const now = new Date()
  const startDate = new Date(now.getTime())

  switch (selectedFilter.value) {
    case 'week':
      startDate.setDate(startDate.getDate() - 7)
      break
    case 'month':
      startDate.setMonth(startDate.getMonth() - 1)
      break
    case 'quarter':
      startDate.setMonth(startDate.getMonth() - 3)
      break
    case 'year':
      startDate.setFullYear(startDate.getFullYear() - 1)
      break
  }

  return props.data
    .filter(d => new Date(d.date) >= startDate)
    .sort((a, b) => new Date(a.date).getTime() - new Date(b.date).getTime())
})
```
改为：
```typescript
const chartData = computed(() => {
  if (!props.data || props.data.length === 0) return []

  // 「全部」区间：不按时间窗口切片，返回全部数据（仅排序）
  if (selectedFilter.value === 'all') {
    return [...props.data].sort((a, b) => new Date(a.date).getTime() - new Date(b.date).getTime())
  }

  const now = new Date()
  const startDate = new Date(now.getTime())

  switch (selectedFilter.value) {
    case 'week':
      startDate.setDate(startDate.getDate() - 7)
      break
    case 'month':
      startDate.setMonth(startDate.getMonth() - 1)
      break
    case 'quarter':
      startDate.setMonth(startDate.getMonth() - 3)
      break
    case 'year':
      startDate.setFullYear(startDate.getFullYear() - 1)
      break
  }

  return props.data
    .filter(d => new Date(d.date) >= startDate)
    .sort((a, b) => new Date(a.date).getTime() - new Date(b.date).getTime())
})
```

- [ ] **Step 5: 删除重复的 `watch(selectedFilter)`**

文件中存在两个完全相同的 `watch(selectedFilter, ...)`（约 L193-L202 与 L402-L411）。删除**第二个**（约 L402-L411 这一段），保留第一个（L193-L202）。被删除的片段如下：
```typescript
// 筛选变化时更新图表
watch(selectedFilter, (newFilter) => {
  emit('filter-change', newFilter)
  nextTick(() => {
    if (chartData.value.length > 0 && !chart) {
      initChart()
    } else {
      updateChart()
    }
  })
})
```
（连同其上方注释 `// 筛选变化时更新图表` 一并删除。）

- [ ] **Step 6: 构建验证**

Run（在 `frontend/` 下）:
```bash
npm run build
```
Expected: 构建成功，无 TypeScript / 模板错误。

- [ ] **Step 7: 检查点**

构建通过。⚠️ 如需提交，需用户明确授权；获授权后：
```bash
git add frontend/src/components/charts/PriceTrendChart.vue
git commit -m "feat(chart): 价格趋势组件增加「全部」区间标签"
```

---

## Task 2: 菜谱 `RecipeDetail.vue` 「全部」分批加载

**Files:**
- Modify: `frontend/src/views/recipes/RecipeDetail.vue`

**背景：** 菜谱组件已绑定 `@filter-change="onCostTrendFilterChange"`。`loadCostHistory(days, offsetDays)`（L784）内部对空结果 `return`（不更新 `maxDaysLoaded`、不合并），且成功时 `maxDaysLoaded = max(old, offsetDays + days)`（L803）。菜谱成本趋势按天前向填充，每日有值，故「某批返回空」即「已早于最早记录」，可作终止条件。

- [ ] **Step 1: 扩展 `onCostTrendFilterChange` 并新增 `loadAllCostHistory`**

定位（约 L840-L855，`onCostTrendFilterChange` 整个函数）：
```typescript
// 成本趋势筛选切换时，按需加载更多数据
const onCostTrendFilterChange = async (filter: 'week' | 'month' | 'quarter' | 'year') => {
  const targetDays: Record<string, number> = {
    week: 7,
    month: 30,
    quarter: 90,
    year: 365,
  }
  const days = targetDays[filter] || 90
  if (days > maxDaysLoaded.value) {
    loadingCostHistory.value = true  // 立即显示转圈
    await enqueueCostHistory(days, true)
  }
  // 请求完成或已在队列中等候完成，停止转圈
  loadingCostHistory.value = false
}
```
改为：
```typescript
// 成本趋势筛选切换时，按需加载更多数据
const onCostTrendFilterChange = async (filter: 'week' | 'month' | 'quarter' | 'year' | 'all') => {
  // 「全部」：分批加载所有历史，规避单次请求超过 10s 超时
  if (filter === 'all') {
    await loadAllCostHistory()
    return
  }

  const targetDays: Record<string, number> = {
    week: 7,
    month: 30,
    quarter: 90,
    year: 365,
  }
  const days = targetDays[filter] || 90
  if (days > maxDaysLoaded.value) {
    loadingCostHistory.value = true  // 立即显示转圈
    await enqueueCostHistory(days, true)
  }
  // 请求完成或已在队列中等候完成，停止转圈
  loadingCostHistory.value = false
}

// 「全部」区间：循环分批加载所有历史成本数据
// 每批 days=365（后端上限 le=365），offset_days 随 maxDaysLoaded 自然递增；
// 菜谱成本按天前向填充（每日有值），某批未带来新数据即表示已早于最早记录，终止。
const loadAllCostHistory = async () => {
  loadingCostHistory.value = true
  try {
    while (true) {
      const before = costHistoryRecords.value.length
      await loadCostHistory(365, maxDaysLoaded.value)
      // 本批未带来新数据 → 已加载到最早记录之前，终止
      if (costHistoryRecords.value.length === before) break
    }
  } catch (e) {
    console.error('加载全部成本历史失败', e)
  } finally {
    loadingCostHistory.value = false
  }
}
```

> 说明：组件层 `selectedFilter === 'all'` 时已在 Task 1 处理为不切片，故此处只需把全部历史累积进 `costHistoryRecords`，`chartData` computed（L858）与渲染自动生效。

- [ ] **Step 2: 构建验证**

Run（在 `frontend/` 下）:
```bash
npm run build
```
Expected: 构建成功。

- [ ] **Step 3: 检查点**

构建通过。⚠️ 如需提交，需用户明确授权；获授权后：
```bash
git add frontend/src/views/recipes/RecipeDetail.vue
git commit -m "feat(recipe): 菜谱成本趋势「全部」区间分批加载"
```

---

## Task 3: 商品 `ProductDetail.vue` 按时间区间取数 + 「全部」

**Files:**
- Modify: `frontend/src/views/products/ProductDetail.vue`

- [ ] **Step 1: 新增图表专用状态变量**

定位（约 L1179-L1184）：
```typescript
const priceRecords = ref<PriceRecord[]>([])
const pricePage = ref(1)
const pricePageSize = ref(10)
const priceTotal = ref(0)
const priceTotalPages = computed(() => Math.ceil(priceTotal.value / pricePageSize.value))
```
在其后追加：
```typescript
// 图表专用数据源（按时间区间累积加载，独立于下方分页列表的 priceRecords）
const chartPriceRecords = ref<PriceRecord[]>([])
// 已请求覆盖到的最早日期：undefined=未请求, null=已全量, Date=已覆盖到该日
const chartEarliestDate = ref<Date | null | undefined>(undefined)
const loadingChartPrices = ref(false)
```

- [ ] **Step 2: 新增 `loadChartPriceRecords` 与 `onPriceTrendFilterChange`**

在 `loadPriceRecords` 函数（约 L2013-L2032）之后追加：
```typescript
// 今天往前 n 天的日期
const daysAgo = (n: number): Date => {
  const d = new Date()
  d.setDate(d.getDate() - n)
  return d
}

// 加载图表价格记录（按时间区间累积）
// startDate 为空表示「全部」；「全部」请求放宽超时到 30s，规避前端 10s 上限
const loadChartPriceRecords = async (startDate?: Date) => {
  // 判断是否需要发请求（已覆盖则跳过）
  let needFetch = false
  if (startDate) {
    if (chartEarliestDate.value === undefined) {
      needFetch = true
    } else if (chartEarliestDate.value === null) {
      needFetch = false  // 已全量，已覆盖
    } else {
      // startDate 早于已覆盖日期时才需要补拉更早数据
      needFetch = startDate < chartEarliestDate.value
    }
  } else {
    // 「全部」：仅当未全量时请求
    needFetch = chartEarliestDate.value !== null
  }
  if (!needFetch) return

  loadingChartPrices.value = true
  try {
    const params: Record<string, any> = {
      product_id: productId.value,
      limit: 1000,
    }
    if (startDate) {
      params.start_date = startDate.toISOString().split('T')[0]
    }
    const response = await api.get('/products', {
      params,
      timeout: startDate ? 10000 : 30000,  // 「全部」放宽到 30s
    })
    const items = response.items || []
    // 按 id 去重合并
    const existing = new Set(chartPriceRecords.value.map(r => r.id))
    for (const r of items) {
      if (!existing.has(r.id)) {
        chartPriceRecords.value.push(r)
      }
    }
    // 更新已覆盖范围（取更早）
    if (startDate) {
      chartEarliestDate.value = (chartEarliestDate.value instanceof Date && chartEarliestDate.value < startDate)
        ? chartEarliestDate.value
        : startDate
    } else {
      chartEarliestDate.value = null
    }
  } catch (e) {
    console.error('加载图表价格记录失败', e)
  } finally {
    loadingChartPrices.value = false
  }
}

// 图表区间切换：按时间区间发起请求
const onPriceTrendFilterChange = (filter: 'week' | 'month' | 'quarter' | 'year' | 'all') => {
  const startDateMap: Record<string, Date | undefined> = {
    week: daysAgo(7),
    month: daysAgo(30),
    quarter: daysAgo(90),
    year: daysAgo(365),
    all: undefined,
  }
  loadChartPriceRecords(startDateMap[filter])
}
```

- [ ] **Step 3: `chartData` 数据源改用 `chartPriceRecords`**

定位（约 L1844-L1885）聚合函数中两处对 `priceRecords.value` 的引用。第一处（约 L1845）：
```typescript
  if (!priceRecords.value || priceRecords.value.length === 0) return []
```
改为：
```typescript
  if (!chartPriceRecords.value || chartPriceRecords.value.length === 0) return []
```
第二处（约 L1850 的循环）：
```typescript
  for (const record of priceRecords.value) {
```
改为：
```typescript
  for (const record of chartPriceRecords.value) {
```
（聚合逻辑：按日分组、归一化到 ¥/斤、min/max/avg/count 一律不动。）

- [ ] **Step 4: 组件绑定 `@filter-change` 并改用图表加载态**

定位（约 L433-L444）：
```vue
          <PriceTrendChart
            v-if="product"
            title="价格趋势"
            icon="mdi-chart-line"
            icon-color="tertiary"
            :unit="chartUnit"
            empty-text="暂无价格历史数据"
            :data="chartData"
            :loading="loadingPrices"
            color="#ff9800"
            class="grid-item item-price-trend"
          />
```
改为：
```vue
          <PriceTrendChart
            v-if="product"
            title="价格趋势"
            icon="mdi-chart-line"
            icon-color="tertiary"
            :unit="chartUnit"
            empty-text="暂无价格历史数据"
            :data="chartData"
            :loading="loadingChartPrices"
            color="#ff9800"
            class="grid-item item-price-trend"
            @filter-change="onPriceTrendFilterChange"
          />
```

- [ ] **Step 5: 首次加载默认拉近 30 天（月）**

定位 `loadData`（约 L1985-L2010）中：
```typescript
    // 后台分别加载其他数据，互不影响
    loadMerchantPrices()
    loadPriceRecords()
    loadNutritionData()
```
改为（在 `loadPriceRecords()` 后追加一行）：
```typescript
    // 后台分别加载其他数据，互不影响
    loadMerchantPrices()
    loadPriceRecords()
    loadChartPriceRecords(daysAgo(30))  // 图表默认加载近 30 天（月）
    loadNutritionData()
```

- [ ] **Step 6: 构建验证**

Run（在 `frontend/` 下）:
```bash
npm run build
```
Expected: 构建成功。

- [ ] **Step 7: 检查点**

构建通过。⚠️ 如需提交，需用户明确授权；获授权后：
```bash
git add frontend/src/views/products/ProductDetail.vue
git commit -m "feat(product): 商品价格趋势按时间区间取数并支持「全部」"
```

---

## Task 4: 原料 `IngredientDetail.vue` 按时间区间取数 + 「全部」

**Files:**
- Modify: `frontend/src/views/ingredients/IngredientDetail.vue`

> 与 Task 3 对称，唯一差异：请求参数用 `ingredient_id: ingredientId.value`（替代 `product_id`）。

- [ ] **Step 1: 新增图表专用状态变量**

定位（约 L1772-L1777）：
```typescript
const priceRecords = ref<PriceRecord[]>([])
const pricePage = ref(1)
const pricePageSize = ref(10)
const priceTotal = ref(0)
const priceTotalPages = computed(() => Math.ceil(priceTotal.value / pricePageSize.value))
```
在其后追加：
```typescript
// 图表专用数据源（按时间区间累积加载，独立于下方分页列表的 priceRecords）
const chartPriceRecords = ref<PriceRecord[]>([])
// 已请求覆盖到的最早日期：undefined=未请求, null=已全量, Date=已覆盖到该日
const chartEarliestDate = ref<Date | null | undefined>(undefined)
const loadingChartPrices = ref(false)
```

- [ ] **Step 2: 新增 `loadChartPriceRecords` 与 `onPriceTrendFilterChange`**

在 `loadPriceRecords` 函数（约 L2747-L2767）之后追加（注意这里是 `ingredient_id`）：
```typescript
// 今天往前 n 天的日期
const daysAgo = (n: number): Date => {
  const d = new Date()
  d.setDate(d.getDate() - n)
  return d
}

// 加载图表价格记录（按时间区间累积）
// startDate 为空表示「全部」；「全部」请求放宽超时到 30s，规避前端 10s 上限
const loadChartPriceRecords = async (startDate?: Date) => {
  // 判断是否需要发请求（已覆盖则跳过）
  let needFetch = false
  if (startDate) {
    if (chartEarliestDate.value === undefined) {
      needFetch = true
    } else if (chartEarliestDate.value === null) {
      needFetch = false  // 已全量，已覆盖
    } else {
      needFetch = startDate < chartEarliestDate.value
    }
  } else {
    needFetch = chartEarliestDate.value !== null
  }
  if (!needFetch) return

  loadingChartPrices.value = true
  try {
    const params: Record<string, any> = {
      ingredient_id: ingredientId.value,
      limit: 1000,
    }
    if (startDate) {
      params.start_date = startDate.toISOString().split('T')[0]
    }
    const response = await api.get('/products', {
      params,
      timeout: startDate ? 10000 : 30000,  // 「全部」放宽到 30s
    })
    const items = response.items || []
    // 按 id 去重合并
    const existing = new Set(chartPriceRecords.value.map(r => r.id))
    for (const r of items) {
      if (!existing.has(r.id)) {
        chartPriceRecords.value.push(r)
      }
    }
    // 更新已覆盖范围（取更早）
    if (startDate) {
      chartEarliestDate.value = (chartEarliestDate.value instanceof Date && chartEarliestDate.value < startDate)
        ? chartEarliestDate.value
        : startDate
    } else {
      chartEarliestDate.value = null
    }
  } catch (e) {
    console.error('加载图表价格记录失败', e)
  } finally {
    loadingChartPrices.value = false
  }
}

// 图表区间切换：按时间区间发起请求
const onPriceTrendFilterChange = (filter: 'week' | 'month' | 'quarter' | 'year' | 'all') => {
  const startDateMap: Record<string, Date | undefined> = {
    week: daysAgo(7),
    month: daysAgo(30),
    quarter: daysAgo(90),
    year: daysAgo(365),
    all: undefined,
  }
  loadChartPriceRecords(startDateMap[filter])
}
```

- [ ] **Step 3: `chartData` 数据源改用 `chartPriceRecords`**

定位聚合函数（约 L2583+）。第一处（约 L2584）：
```typescript
  if (!priceRecords.value || priceRecords.value.length === 0) return []
```
改为：
```typescript
  if (!chartPriceRecords.value || chartPriceRecords.value.length === 0) return []
```
第二处（约 L2589 的循环）：
```typescript
  for (const record of priceRecords.value) {
```
改为：
```typescript
  for (const record of chartPriceRecords.value) {
```
（单位转换到原料默认单位的整段逻辑不动，仅替换数据源变量名。）

- [ ] **Step 4: 组件绑定 `@filter-change` 并改用图表加载态**

定位（约 L281-L292）：
```vue
      <!-- 价格趋势图表 -->
      <PriceTrendChart
        v-if="ingredient"
        title="价格趋势"
        icon="mdi-chart-line"
        icon-color="tertiary"
        :unit="chartUnit"
        empty-text="暂无价格历史数据"
        :data="chartData"
        :loading="loadingPrices"
        color="#ff9800"
        class="grid-item item-price-trend"
      />
```
改为：
```vue
      <!-- 价格趋势图表 -->
      <PriceTrendChart
        v-if="ingredient"
        title="价格趋势"
        icon="mdi-chart-line"
        icon-color="tertiary"
        :unit="chartUnit"
        empty-text="暂无价格历史数据"
        :data="chartData"
        :loading="loadingChartPrices"
        color="#ff9800"
        class="grid-item item-price-trend"
        @filter-change="onPriceTrendFilterChange"
      />
```

- [ ] **Step 5: 首次加载默认拉近 30 天（月）**

定位 `loadData`（约 L2683-L2693）中：
```typescript
    // 后台分别加载其他数据，互不影响
    loadLatestPrice()
    loadMerchantPrices()
    loadProducts()
    loadPriceRecords()
    loadNutritionData()
```
改为（在 `loadPriceRecords()` 后追加一行）：
```typescript
    // 后台分别加载其他数据，互不影响
    loadLatestPrice()
    loadMerchantPrices()
    loadProducts()
    loadPriceRecords()
    loadChartPriceRecords(daysAgo(30))  // 图表默认加载近 30 天（月）
    loadNutritionData()
```

> 注：约 L3238 编辑基本信息后的 `await loadPriceRecords()` 刷新**不需要**追加图表加载（编辑基本信息不改变价格记录范围）。

- [ ] **Step 6: 构建验证**

Run（在 `frontend/` 下）:
```bash
npm run build
```
Expected: 构建成功。

- [ ] **Step 7: 检查点**

构建通过。⚠️ 如需提交，需用户明确授权；获授权后：
```bash
git add frontend/src/views/ingredients/IngredientDetail.vue
git commit -m "feat(ingredient): 原料价格趋势按时间区间取数并支持「全部」"
```

---

## Task 5: 整体构建验证与手动验证清单

**Files:**
- 无代码改动，仅验证。

- [ ] **Step 1: 整体构建**

Run（在 `frontend/` 下）:
```bash
npm run build
```
Expected: 构建成功，无错误、无 TypeScript 报错。

- [ ] **Step 2: 手动验证 — 菜谱详情**

在浏览器（已由用户启动的开发发服务）打开一个有成本历史数据的菜谱详情页：
1. 价格趋势图标签栏出现「周 / 月 / 季 / 年 / 全部」五项，「全部」在最末。
2. 点「全部」：成本趋势分批渐进刷新（每批合并后图表更新），最终显示完整历史；不出现「请求超时」。
3. 切回「年 / 季 / 月 / 周」：显示对应区间，正常。
4. 默认进页面为「月」。

- [ ] **Step 3: 手动验证 — 商品详情**

打开一个有较多价格记录（>10 条）的商品详情页：
1. 标签栏含「全部」。
2. 默认「月」：图表显示近 30 天全部记录聚合（不再只有当前分页 10 条）。
3. 点「全部」：显示该商品完整价格历史（不超时）。
4. 下方「价格记录列表」翻页时，图表**不再**跟着翻页变化（图表已独立于分页列表）。
5. 切「全部」后再切回「年/季/月/周」：不发额外请求，直接切片显示。

- [ ] **Step 4: 手动验证 — 原料详情**

打开一个有关联商品且有价格记录的原料详情页，重复 Step 3 的 1-5 项检查。

- [ ] **Step 5: 最终检查点**

全部验证通过。⚠️ 如需提交（整功能收尾），需用户明确授权；获授权后：
```bash
git add docs/superpowers/specs/2026-06-21-价格趋势全部区间-design.md docs/superpowers/plans/2026-06-21-价格趋势全部区间.md
git commit -m "docs: 价格趋势「全部」区间标签 设计与实现计划"
```

---

## 自审记录（Self-Review）

- **Spec 覆盖**：spec 的 3.1（组件）→ Task 1；3.2（菜谱分批）→ Task 2；3.3（商品/原料按时间 + 放宽超时）→ Task 3/4；3.4 超时规避 → Task 2（分批）/Task 3-4（timeout=30s）；验证 → Task 5。全覆盖。
- **占位符扫描**：无 TBD/TODO；每个代码步骤均给出完整可粘贴代码。
- **类型一致性**：`filter` 联合类型 `'week'|'month'|'quarter'|'year'|'all'` 在组件 emit、菜谱 `onCostTrendFilterChange`、商品/原料 `onPriceTrendFilterChange` 三处一致；`chartEarliestDate` 状态机（undefined/null/Date）在商品、原料两处定义与判断一致；`loadChartPriceRecords(startDate?: Date)` 签名一致。
- **后端零改动**：已在全局约定与各 Task 中明确，未引入任何后端步骤。
