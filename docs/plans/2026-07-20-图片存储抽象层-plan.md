# 图片存储抽象层（阶段一）实施计划

> **For Claude:** 本计划可用 superpowers:executing-plans 或 superpowers:subagent-driven-development 执行。执行前必读 design 稿第四章「阶段一」：[2026-07-20-用户体系扩展与图片存储抽象-design.md](./2026-07-20-用户体系扩展与图片存储抽象-design.md)

**Goal:** 引入 `StorageBackend` 抽象层，把全项目散落的本地图片 I/O 全部收口，支持 local 与 S3 兼容 backend 按配置切换；上传 / 初始化 / 清理三环节全部接入；顺手治三个 importer 的 fallback bug 与两个清理缺口。

**Architecture:** `Protocol` 接口 + `LocalBackend` + `S3Backend` + 工厂按 `settings.storage_backend` 切换；DB 统一存 key（相对路径），`url_for` 动态拼 URL，切 backend 时 DB 零改动。

**Tech Stack:** Python / FastAPI / SQLAlchemy / Alembic / boto3（S3）/ pytest；前端仅 `utils/image.ts` 微调。

**项目约定（执行者必读）：**
- 后端在 `backend/`，用根目录 `.venv`（非 `backend/.venv`）；`uv` 管理依赖
- 数据库迁移走「alembic 迁移 + SQLite/MySQL/PostgreSQL/PostGIS 四引擎 SQL + 开发库直接补」三件套（项目不走 alembic upgrade，开发库 create_all 建表后靠手动补 ALTER）
- 所有改动确保 `py_compile` 通过 + 前端 `npm run build` 通过
- 不要自行启动服务（开发者已起自动重载）
- TDD：先写失败测试 → 实现 → 测试通过 → 提交

---

## Task 1: StorageBackend 接口与配置项

**Files:**
- Create: `backend/app/services/storage/__init__.py`
- Create: `backend/app/services/storage/base.py`
- Modify: `backend/app/config.py`（Settings 加存储配置项）
- Modify: `backend/.env.example`（文档化配置项）
- Test: `backend/tests/services/storage/test_base.py`

**Context:** design §4.1、§4.7。现有 `backend/app/config.py:6-71` 的 `Settings` 类。`pydantic_settings.BaseSettings` 驱动。

**Step 1: 写失败测试**

```python
# backend/tests/services/storage/test_base.py
from app.services.storage.base import StorageBackend
from app.config import settings

def test_storage_backend_protocol_methods():
    """Protocol 必须声明 put/get/delete/exists/url_for 五个方法签名"""
    methods = [m for m in dir(StorageBackend) if not m.startswith("_")]
    for required in ("put", "get", "delete", "exists", "url_for"):
        assert required in methods, f"StorageBackend 缺方法 {required}"

def test_settings_has_storage_config():
    assert hasattr(settings, "storage_backend")
    assert settings.storage_backend in ("local", "s3")
    assert hasattr(settings, "s3_endpoint")
    assert hasattr(settings, "s3_bucket")
```

**Step 2: 跑测试确认失败**
Run: `python -m pytest backend/tests/services/storage/test_base.py -v`
Expected: FAIL（模块不存在）

**Step 3: 实现**

```python
# backend/app/services/storage/base.py
from typing import Protocol


class StorageBackend(Protocol):
    """图片存储后端统一契约。DB 只存 key（相对路径），url_for 动态拼 URL。"""

    def put(self, key: str, data: bytes, content_type: str) -> str:
        """存储，返回 key。"""
        ...

    def get(self, key: str) -> bytes:
        """读取。"""
        ...

    def delete(self, key: str) -> None:
        """删除。"""
        ...

    def exists(self, key: str) -> bool:
        """存在性。"""
        ...

    def url_for(self, key: str) -> str:
        """对外可访问 URL。"""
        ...
```

```python
# backend/app/services/storage/__init__.py
from app.services.storage.base import StorageBackend

__all__ = ["StorageBackend"]
```

`config.py` Settings 加（默认值给 local）：
```python
    # 图片存储
    storage_backend: str = "local"  # 'local' | 's3'
    storage_base_url: str = ""  # local 端对外基址，空则走 /api/v1/static
    s3_endpoint: str = ""  # OSS/COS/MinIO 兼容端点
    s3_access_key: str = ""
    s3_secret_key: str = ""
    s3_bucket: str = ""
    s3_region: str = ""
```

`.env.example` 加同名项 + 注释说明。

**Step 4: 跑测试通过**
Run: `python -m pytest backend/tests/services/storage/test_base.py -v`
Expected: PASS

**Step 5: py_compile + 提交**
Run: `python -m py_compile backend/app/services/storage/base.py backend/app/config.py`
Commit: `feat(storage): 加 StorageBackend 接口与存储配置项`

---

## Task 2: LocalBackend 实现

**Files:**
- Create: `backend/app/services/storage/local.py`
- Test: `backend/tests/services/storage/test_local.py`

**Context:** design §4.2。落盘根沿用现有 `backend/app/main.py:551` 的 `Path(__file__).parent.parent / "static"`，但子目录按 key 前缀分（`recipes/`、`avatars/`）。`url_for` 返 `/api/v1/static/images/<key>`（对齐现有 StaticFiles 挂载 `main.py:559` + 前端 `resolveImageUrl` 的 `/static/images/` 分支）。

**Step 1: 写失败测试**（用 tmp_path 覆盖根目录，不污染真实 static）

```python
# backend/tests/services/storage/test_local.py
from pathlib import Path
from app.services.storage.local import LocalBackend

def test_put_get_roundtrip(tmp_path):
    backend = LocalBackend(root=tmp_path, base_url="/api/v1/static")
    key = backend.put("recipes/abc.png", b"\x89PNG data", "image/png")
    assert key == "recipes/abc.png"
    assert backend.get(key) == b"\x89PNG data"
    assert backend.exists(key) is True
    assert (tmp_path / "images" / "recipes" / "abc.png").exists()

def test_url_for_local(tmp_path):
    backend = LocalBackend(root=tmp_path, base_url="/api/v1/static")
    assert backend.url_for("recipes/abc.png") == "/api/v1/static/images/recipes/abc.png"

def test_delete(tmp_path):
    backend = LocalBackend(root=tmp_path, base_url="/api/v1/static")
    backend.put("avatars/x.jpg", b"data", "image/jpeg")
    backend.delete("avatars/x.jpg")
    assert backend.exists("avatars/x.jpg") is False

def test_put_creates_subdirs(tmp_path):
    backend = LocalBackend(root=tmp_path, base_url="/api/v1/static")
    backend.put("recipes/sub/deep.png", b"x", "image/png")
    assert backend.exists("recipes/sub/deep.png")
```

**Step 2: 跑测试失败**（LocalBackend 不存在）

**Step 3: 实现**

```python
# backend/app/services/storage/local.py
from pathlib import Path


class LocalBackend:
    """本地文件系统后端。落 <root>/images/<key>，url_for 返 /api/v1/static/images/<key>。"""

    def __init__(self, root: Path, base_url: str = "/api/v1/static"):
        self.root = Path(root)
        self.images_dir = self.root / "images"
        self.images_dir.mkdir(parents=True, exist_ok=True)
        self.base_url = base_url.rstrip("/")

    def _path(self, key: str) -> Path:
        return self.images_dir / key

    def put(self, key: str, data: bytes, content_type: str) -> str:
        path = self._path(key)
        path.parent.mkdir(parents=True, exist_ok=True)
        path.write_bytes(data)
        return key

    def get(self, key: str) -> bytes:
        return self._path(key).read_bytes()

    def delete(self, key: str) -> None:
        path = self._path(key)
        if path.exists():
            path.unlink()

    def exists(self, key: str) -> bool:
        return self._path(key).exists()

    def url_for(self, key: str) -> str:
        return f"{self.base_url}/images/{key}"
```

**Step 4: 测试通过 + py_compile + 提交**
Commit: `feat(storage): LocalBackend 实现`

---

## Task 3: S3Backend 实现（boto3 + 公共读）

**Files:**
- Modify: `backend/requirements.txt` 或 `pyproject.toml`（加 `boto3`）
- Create: `backend/app/services/storage/s3.py`
- Test: `backend/tests/services/storage/test_s3.py`

**Context:** design §4.2、§4.7。S3 兼容协议覆盖 OSS/COS/MinIO/AWS。公共读 bucket，`url_for` 返永久 URL（`https://<bucket>.<endpoint>/<key>` 或按 endpoint 风格）。测试用 `moto` 或 `unittest.mock` mock boto3 client。

**Step 1: 加依赖**
`boto3`（若用 moto 测试则加 `moto` 到 dev 依赖）。

**Step 2: 写失败测试**（mock boto3）

```python
# backend/tests/services/storage/test_s3.py
from unittest.mock import MagicMock
from app.services.storage.s3 import S3Backend

def _backend():
    b = S3Backend(endpoint="https://oss.example.com", bucket="live",
                  access_key="ak", secret_key="sk", region="cn")
    b.client = MagicMock()
    return b

def test_put_calls_s3():
    b = _backend()
    key = b.put("recipes/a.png", b"data", "image/png")
    assert key == "recipes/a.png"
    b.client.put_object.assert_called_once()
    kwargs = b.client.put_object.call_args.kwargs
    assert kwargs["Bucket"] == "live"
    assert kwargs["Key"] == "recipes/a.png"
    assert kwargs["Body"] == b"data"
    assert kwargs["ContentType"] == "image/png"

def test_get_delete_exists():
    b = _backend()
    b.client.get_object.return_value = {"Body": MagicMock(read=lambda: b"x")}
    assert b.get("k") == b"x"
    b.delete("k")
    b.client.delete_object.assert_called_once()
    b.client.head_object.return_value = {}
    assert b.exists("k") is True
    from botocore.exceptions import ClientError
    b.client.head_object.side_effect = ClientError({"Error": {"Code": "404"}}, "head")
    assert b.exists("k") is False

def test_url_for_public():
    b = _backend()
    url = b.url_for("recipes/a.png")
    assert "live" in url and "recipes/a.png" in url
```

**Step 3: 实现**

```python
# backend/app/services/storage/s3.py
import boto3
from botocore.exceptions import ClientError


class S3Backend:
    """S3 兼容后端（OSS/COS/MinIO/AWS）。公共读 bucket，url_for 返永久 URL。"""

    def __init__(self, endpoint: str, bucket: str, access_key: str,
                 secret_key: str, region: str = ""):
        self.endpoint = endpoint.rstrip("/")
        self.bucket = bucket
        self.client = boto3.client(
            "s3",
            endpoint_url=endpoint or None,
            aws_access_key_id=access_key,
            aws_secret_access_key=secret_key,
            region_name=region or None,
        )

    def put(self, key: str, data: bytes, content_type: str) -> str:
        self.client.put_object(Bucket=self.bucket, Key=key,
                               Body=data, ContentType=content_type)
        return key

    def get(self, key: str) -> bytes:
        return self.client.get_object(Bucket=self.bucket, Key=key)["Body"].read()

    def delete(self, key: str) -> None:
        self.client.delete_object(Bucket=self.bucket, Key=key)

    def exists(self, key: str) -> bool:
        try:
            self.client.head_object(Bucket=self.bucket, Key=key)
            return True
        except ClientError:
            return False

    def url_for(self, key: str) -> str:
        # 公共读：永久 URL。endpoint 含 bucket 则直接拼，否则用 <bucket>.<endpoint> 风格
        return f"{self.endpoint}/{self.bucket}/{key}"
```

注：`url_for` 的 URL 风格按真实 OSS bucket 域名规则调整（OSS 默认 `<bucket>.<endpoint>`，MinIO 默认 `<endpoint>/<bucket>`）。实现者据 endpoint 字符串判断；测试覆盖两种。

**Step 4: 测试通过 + py_compile + 提交**
Commit: `feat(storage): S3Backend 实现（boto3 公共读）`

---

## Task 4: Storage 工厂（按 config 切换 + 单例）

**Files:**
- Create: `backend/app/services/storage/factory.py`
- Modify: `backend/app/services/storage/__init__.py`（导出 get_storage）
- Test: `backend/tests/services/storage/test_factory.py`

**Context:** design §4.1、§4.2。工厂按 `settings.storage_backend` 返回 backend 实例，单例（避免每请求新建 boto3 client）。LocalBackend 的 root = `backend/app/main.py:551` 的 static_dir。

**Step 1: 写失败测试**

```python
# backend/tests/services/storage/test_factory.py
from unittest.mock import patch
from app.services.storage import get_storage, reset_storage
from app.services.storage.local import LocalBackend
from app.services.storage.s3 import S3Backend

def test_factory_local_by_default():
    reset_storage()
    with patch("app.services.storage.factory.settings") as s:
        s.storage_backend = "local"
        s.storage_base_url = "/api/v1/static"
        b = get_storage()
        assert isinstance(b, LocalBackend)

def test_factory_singleton():
    reset_storage()
    with patch("app.services.storage.factory.settings") as s:
        s.storage_backend = "local"
        s.storage_base_url = "/api/v1/static"
        assert get_storage() is get_storage()

def test_factory_s3():
    reset_storage()
    with patch("app.services.storage.factory.settings") as s:
        s.storage_backend = "s3"
        s.s3_endpoint = "https://oss.example.com"
        s.s3_bucket = "live"
        s.s3_access_key = "ak"
        s.s3_secret_key = "sk"
        s.s3_region = "cn"
        assert isinstance(get_storage(), S3Backend)
```

**Step 2-4: 实现 + 通过**

```python
# backend/app/services/storage/factory.py
from pathlib import Path
from functools import lru_cache
from app.config import settings
from app.services.storage.base import StorageBackend
from app.services.storage.local import LocalBackend
from app.services.storage.s3 import S3Backend

_STATIC_ROOT = Path(__file__).resolve().parents[3] / "static"


@lru_cache(maxsize=1)
def _build() -> StorageBackend:
    if settings.storage_backend == "s3":
        return S3Backend(
            endpoint=settings.s3_endpoint,
            bucket=settings.s3_bucket,
            access_key=settings.s3_access_key,
            secret_key=settings.s3_secret_key,
            region=settings.s3_region,
        )
    return LocalBackend(root=_STATIC_ROOT, base_url=settings.storage_base_url or "/api/v1/static")


def get_storage() -> StorageBackend:
    return _build()


def reset_storage() -> None:
    """测试用：重置单例。"""
    _build.cache_clear()
```

`__init__.py` 导出 `get_storage` / `reset_storage`。

**Step 5: 提交**
Commit: `feat(storage): 工厂按 config 切换 backend（单例）`

---

## Task 5: DB 数据迁移——图片路径剥前缀成 key

**Files:**
- Create: `backend/alembic/versions/20260720_0001_storage_key_strip_prefix.py`
- Create: `backend/scripts/sql/20260720_storage_key_strip_prefix_sqlite.sql`
- Create: `backend/scripts/sql/20260720_storage_key_strip_prefix_mysql.sql`
- Create: `backend/scripts/sql/20260720_storage_key_strip_prefix_postgres.sql`
- （PostGIS 无关，不发 PostGIS 变体）
- Modify: 开发库 `backend/data/livecalc.db` 直接补（手动执行 sqlite 脚本）

**Context:** design §4.3、§8.1。现 DB 存 `/static/images/recipes/xxx.jpg`，需剥成 key `recipes/xxx.jpg`。规则：剥 `/static/images/` 前缀。外链 `http(s)://` 与已是 key 格式的不动。涉及 `recipes.images`（JSON 数组）与 `products.image_url`（String）。

**实现要点：**
- **alembic 迁移**：用 `op.execute` 跑引擎对应 SQL。SQLite JSON 用 `replace()`（recipes.images 是 JSON 数组文本，整体 replace 字符串前缀安全）；MySQL/PG 同理用 `REPLACE()`。
  - `UPDATE recipes SET images = REPLACE(images, '/static/images/', '')`
  - `UPDATE products SET image_url = REPLACE(image_url, '/static/images/', '') WHERE image_url LIKE '/static/images/%'`
- **三引擎 SQL**：sqlite/mysql/postgres 各一份（语句几乎相同，差异在 JSON 函数；本项目 recipes.images 在 SQLite 存为 TEXT，直接 REPLACE 即可）。
- **开发库直补**：跑 sqlite 脚本，迁移前先备份 `livecalc.db.bak_20260720`。

**验证：**
- 迁移后 `SELECT images FROM recipes WHERE images LIKE '%/static/%'` 应空
- `SELECT image_url FROM products WHERE image_url LIKE '/static/%'` 应空
- 抽查若干条：images 形如 `["recipes/xxx.jpg"]`，image_url 形如 `recipes/yyy.jpg`

**提交：** `refactor(storage): 图片路径剥前缀成 key`

---

## Task 6: 上传端点收口（recipes/images 走 storage）

**Files:**
- Modify: `backend/app/api/recipes.py:650-718`（upload_image）
- Modify: `backend/app/api/recipes.py:721-765`（delete_image）

**Context:** design §4.4。现 upload 直接 `open(file_path,'wb').write(content)`，改为 `storage.put(key, content, mime)`。文件名生成（uuid4 hex + 扩展名，recipes.py:687-689）保留。key = `recipes/<filename>`。返回 `image_path`（= key）与 `image_url`（= `storage.url_for(key)`）。

**实现要点：**
- `upload_image`：
  ```python
  from app.services.storage import get_storage
  storage = get_storage()
  ext = os.path.splitext(file.filename or "image.jpg")[1] or ".jpg"
  key = f"recipes/{uuid.uuid4().hex}{ext}"
  storage.put(key, content, file.content_type or "image/jpeg")
  # DB 存 key
  recipe.images.append(key)  # direct_write 分支
  return {"image_path": key, "image_url": storage.url_for(key)}
  ```
- `delete_image`：`storage.delete(key)` 替换 `file_path.unlink()`。DB `recipe.images` 移除 key（注意现 739-744 按 filename 匹配，改为按 key 匹配）。
- 前端消费：`image_path` 现在是 key（`recipes/xxx.jpg`），前端 `resolveImageUrl` 的 `/static/images/` 分支需兼容 key——见 Task 9。

**验证：**
- 现有 recipes 图片相关单测（如有）跑通；无则补一个：上传一张图 → DB 存 key → 再次 GET recipe 返 key → `url_for` 拼出可访问 URL
- 手动验证：API 上传 → 文件落 `backend/static/images/recipes/`（local 模式）→ 返 URL 可访问

**提交：** `refactor(storage): 菜谱图片上传/删除走 StorageBackend`

---

## Task 7: 初始化收口（三个 importer 走 storage + 统一 fallback）

**Files:**
- Modify: `backend/app/services/json_recipe_import_service.py:72-106`（`_download_image`）
- Modify: `backend/app/services/enhanced_recipe_import_service.py:189-205`（`_download_image` + 666-667 fallback）
- Modify: `backend/app/services/importer/importers/howtocook.py:284-296`（`_download_image` + 217-218 fallback）

**Context:** design §4.4、§4.5。三个 importer 的 `_download_image` 现各自 `shutil.copy2` 到 local，fallback 不一致（json 跳过，enhanced/howtocook 写残缺 `images/xxx.jpg`）。统一改为：成功 → `storage.put(key='recipes/<filename>', data, content_type)` 返 key；失败 → 返 None（跳过，不写残缺路径）。

**实现要点：**
- 抽公共逻辑或各自改造（三处逻辑相近，DRY 考虑可抽 `backend/app/services/storage/import_helpers.py`，但三个 importer 的临时目录来源不同，保持各自薄封装也可——实现者判断）。
- `key` 生成：用源文件 basename（现 howtocook.py:291 已是 `os.path.basename`）。
- fallback 统一为「失败返 None」，删除 enhanced:667 / howtocook:218 的 `or normalized_path` / `or img` 兜底。
- 导入后 DB 存 key（`recipes/xxx.jpg`），不再是 `/static/images/recipes/xxx.jpg`。

**验证：**
- 单测：mock 临时图存在 → `_download_image` 调 `storage.put` 返 key；临时图不存在 → 返 None
- 端到端：清空开发库菜谱后重跑启动导入（`FIRST_RUN_INIT_RECIPES=true`），抽查 recipe.images 全为 key 格式、无残缺路径

**提交：** `refactor(storage): 三个 importer 图片下载走 StorageBackend，统一 fallback`

---

## Task 8: 清理收口 + 补清理缺口 + 修商品 image_url 导入

**Files:**
- Modify: `backend/app/api/admin.py:246-309`（孤儿清理 list + delete）
- Modify: `backend/app/api/recipes.py:457-541`（update_recipe，补移出图清理）
- Modify: `backend/app/services/importer/importers/export.py:523`（商品 image_url 过 `_restore_image_paths`）

**Context:** design §4.4、§4.5。三个动作：
1. **孤儿清理**（admin.py）：`unlink()` 改 `storage.delete(key)`；list 的 `url` 走 `storage.url_for(key)`（顺带治 admin.py:277 缺 `/api/v1` 前缀的 bug——走抽象层后自动正确）。
2. **菜谱 update 缺口**：`update_recipe` 收到 `images` 字段时，diff 旧列表与新列表，对「旧有新无」的 key 调 `storage.delete`。注意：走审核提议的菜谱不应直接删图（图随提议生效后删）——direct_write 分支才删。
3. **商品 image_url 导入 bug**：`export.py:523` 的 `image_url=item.get("image_url")` 改为过 `self._restore_image_paths([image_url])[0]`（或等价还原），保证导入后是 key 格式。

**验证：**
- 孤儿清理：mock 几个文件 → list 返正确 url → delete 后 `exists` 为 False
- update diff：菜谱原 images=[a,b]，PUT images=[a] → b 被 `storage.delete`
- 商品导入：zip 里 image_url=`images/products/x.jpg` → 导入后 DB 存 `products/x.jpg`（key）

**提交：** `fix(storage): 清理走抽象层，补 update 移出图缺口，修商品 image_url 导入`

---

## Task 9: 前端 image.ts 兜底对齐 + resolveImageUrl 适配 key

**Files:**
- Modify: `frontend/src/utils/image.ts:16-25`

**Context:** design §4.5。现 `resolveImageUrl` 三分支：http 外链 / `/static/images/` 补前缀 / 仓库相对路径兜底 GitHub。DB 改存 key（`recipes/xxx.jpg`）后，需新增/调整分支识别 key 格式。

**实现要点：**
- key 识别：不以 `http` / `/` 开头的路径视为 key，拼 `${VITE_API_URL||'/api/v1'}/static/images/<key>`（local 模式）。
- 保留 `/static/images/...` 旧格式分支（向后兼容迁移期数据）。
- 兜底 GitHub raw 分支：保留但默认 base 与后端 `data_repo_branch`（main）对齐——改 `DEFAULT_REPO_BASE`（image.ts:14）的 `corr` → `main`，或改为读 `VITE_DATA_REPO_BRANCH`。
- 五个消费组件（RecipesView/RecipeDetail/ImageManager/MealCard/RecipeEditDiff）因复用 `resolveImageUrl`，自动受益，无需逐个改。

**验证：**
- `npm run build` 通过
- 手动：DB 存 key `recipes/xxx.jpg` → 前端图正常显示；存外链 → 正常；存旧 `/static/images/...` → 仍正常

**提交：** `refactor(frontend): resolveImageUrl 适配 storage key，兜底分支对齐 main`

---

## Task 10: 迁移脚本 migrate_local_to_s3

**Files:**
- Create: `backend/scripts/migrate_local_to_s3.py`
- Test: `backend/tests/services/storage/test_migrate.py`

**Context:** design §4.6。用户拿真实 OSS bucket 自测。脚本读 local `backend/static/images/` 全部文件 → 逐个 `S3Backend.put(key, data, mime)` → 校验 `exists` → 统计成功/失败。

**实现要点：**
- CLI 入口：`python -m scripts.migrate_local_to_s3`（或 `uv run`），读 `settings` 的 s3_* 配置。
- key 规则：相对 `images/` 的路径即 key（如 `images/recipes/xxx.jpg` → key `recipes/xxx.jpg`）。
- 幂等：`S3Backend.exists(key)` 为真则跳过（避免重复上传）。
- content_type：按扩展名推断（`.jpg→image/jpeg` 等）。
- 输出：成功 N / 跳过 M / 失败 K + 失败明细。

**验证：**
- 单测：mock LocalBackend 读 + S3Backend 写，断言 put 调用次数 = 文件数 - 已存在数
- 用户自测：配 `.env` 的 s3_* → 跑脚本 → 查 OSS bucket 文件齐全 → 切 `storage_backend=s3` → 前端图从 OSS 加载

**提交：** `feat(storage): local→S3 迁移脚本`

---

## Task 11: StaticFiles 挂载适配 + 配置文档 + 整体回归

**Files:**
- Modify: `backend/app/main.py:550-559`（StaticFiles 挂载，local 模式保留）
- Modify: `backend/.env.example`
- Modify: `cc/`（记录要点，按项目索引）

**Context:** design §4.7、§8.2。local 模式仍需 StaticFiles 挂载（LocalBackend 落盘到 static，StaticFiles 对外暴露）；s3 模式可保留挂载（无害）也可不挂。`.env.example` 文档化全部存储配置项。

**实现要点：**
- `main.py` StaticFiles 挂载保留（local 必须）；可选：s3 模式下打印日志提示「图片走对象存储」。
- `.env.example` 加 `STORAGE_BACKEND` / `STORAGE_BASE_URL` / `S3_*` 注释样例。
- 回归：
  - `python -m pytest backend/tests/services/storage/ -v` 全过
  - 后端 `python -m compileall backend/app` 通过
  - 前端 `npm run build` 通过
  - 全量 `pytest` 回归（失败数应不高于基线，失败项需确认为预存无关）

**提交：** `docs(storage): 配置文档化 + StaticFiles 适配`

---

## Task 12: 最终整体审查

**Files:** 无（审查 only）

**动作：**
- 派 final code reviewer subagent 审整个阶段一实现（spec 符合 + 代码质量 + 跨 task 一致性）
- 核对 design §4 全部要点落地：接口/两 backend/工厂/key 范式/三环节收口/三 bug 修复/迁移脚本/配置项
- 核对「顺手治的 bug」清单全清：enhanced/howtocook fallback 统一、商品 image_url 还原、admin url 前缀、前端兜底对齐
- 确认无表结构变更外的 alembic 误加、无 boto3 在 local 模式下的强依赖（懒加载 client）

**完成标志：** reviewer approve + 全测试绿 + build 通过。

---

## 执行顺序说明

Task 1→2→3→4 为顺序依赖（接口→local→s3→工厂）。
Task 5（DB 迁移）可与 1-4 并行，但须在 6-8（收口）前完成（收口后 DB 写 key，迁移要把旧数据对齐）。
Task 6→7→8 三环节收口，建议顺序（上传最独立→初始化→清理）。
Task 9（前端）在 6 完成后做（消费 key 格式）。
Task 10（迁移脚本）独立，任何时候可做。
Task 11 收尾文档化 + 回归。
Task 12 最终审查。
