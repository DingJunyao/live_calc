# 快速填写 - 粘贴文本导入价格 实现计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 在快速填写页面导航栏新增「粘贴导入」按钮，弹出对话框，用户粘贴文本批量解析并创建价格记录（不计入支出），不存在的商品由用户手动关联/创建。

**Architecture:** 以前端为主：解析逻辑抽成纯函数 `pastePriceParser.ts`；对话框组件 `PasteImportDialog.vue` 负责「解析→预览→匹配→并发导入→结果展示」。后端仅一处小改动：`POST /products` 支持可选 `ingredient_id`，用于「挂靠已有原料创建商品」场景。商品匹配为纯手动模式（第一版不含 AI）。

**Tech Stack:** FastAPI + SQLAlchemy（后端）、Vue 3 + TypeScript + Vuetify（前端）、Axios（HTTP）。

---

## 项目适配说明（务必遵守）

1. **无前端测试框架**：`frontend/package.json` 未配置 vitest/jest。解析逻辑写成纯函数便于将来测试，但本计划用「手动验证（浏览器控制台/构建通过）」代替正式单测，不引入新测试依赖。
2. **不主动 git 提交**：遵循项目规范（CLAUDE.md），本计划不包含 `git commit` 步骤。每个任务以「构建/编译/语法通过 + 手动验证」为完成标准，是否提交由开发者决定。
3. **后端改动需无语法错误**：所有 Python 改动确保可导入、无语法报错。
4. **前端改动需构建通过**：每个涉及前端的任务结束前运行 `npm run build`（在 `frontend` 目录）确保通过。
5. **不要在对话中启动服务**：开发者已启动自动重载的前后端服务，无需手动启动。
6. **数据库**：本功能不涉及表结构变更，无需 alembic 迁移或 SQL 脚本。

### 关键 API 速查（已核对源码）

| 用途 | 方法 | 端点 | 备注 |
|------|------|------|------|
| 商品搜索 | GET | `/products/autocomplete?q=&limit=` | 返回数组 `{id, name, ...}` |
| 原料搜索 | GET | `/ingredients?q=&limit=` | 返回 `{items:[{id,name,aliases}], total}` |
| 价格创建 | POST | `/products` | Body 见下 |

`POST /products` Body 组合（三种商品匹配场景）：
- 关联已有商品：`{product_id, merchant_id, price, original_quantity, original_unit, record_type, recorded_at?}`
- 创建同名原料+商品：`{product_name, merchant_id, price, ...}`
- 挂靠已有原料：`{product_name, ingredient_id, merchant_id, price, ...}`（需 Task 1 后端改动）

`record_type` 取值：`'price'`（不计入支出）/ `'purchase'`（计入支出）。本功能统一用 `'price'`。

---

## Task 1: 后端支持「挂靠已有原料创建商品」

**Files:**
- Modify: `backend/app/schemas/product.py`
- Modify: `backend/app/api/products.py`

**Step 1: 给 `ProductRecordCreate` 增加可选 `ingredient_id`**

在 `backend/app/schemas/product.py` 的 `ProductRecordCreate` 中，在 `product_name` 字段后新增：

```python
    ingredient_id: Optional[int] = None  # 可选，自动创建商品时挂靠到指定原料（不提供则创建同名原料）
```

（即该类第 9-10 行附近，`product_name` 之后。）

**Step 2: 改造 `_get_or_create_product` 支持挂靠**

在 `backend/app/api/products.py` 中，将 `_get_or_create_product`（约 64 行起）整体替换为：

```python
def _get_or_create_product(
    db: Session, product_name: str, current_user, ingredient_id: Optional[int] = None
) -> Product:
    """获取或创建商品

    指定 ingredient_id 时，在该原料下创建商品（用于挂靠已有原料场景）；
    否则获取或创建同名原料并在其下创建同名商品。
    """
    # 首先尝试查找已存在的同名商品（处于激活状态）
    product = db.query(Product).filter(
        Product.name == product_name,
        Product.is_active == True
    ).first()

    if product:
        return product

    # 确定挂靠的原料
    if ingredient_id:
        ingredient = db.query(Ingredient).filter(
            Ingredient.id == ingredient_id,
            Ingredient.is_active == True
        ).first()
        if not ingredient:
            raise HTTPException(
                status_code=404,
                detail=f"原料ID {ingredient_id} 不存在"
            )
    else:
        ingredient = _get_or_create_ingredient(db, product_name, current_user)

    # 创建商品
    product = Product(
        name=product_name,
        ingredient_id=ingredient.id,
        created_by=current_user.id,
        updated_by=current_user.id,
        is_active=True
    )
    db.add(product)
    db.commit()
    db.refresh(product)

    return product
```

**Step 3: 在 `create_product_record` 调用处传入 `ingredient_id`**

`backend/app/api/products.py` 的 `create_product_record` 中，找到这段（约 153-157 行）：

```python
        else:
            # 如果没有提供 product_id，自动创建或获取商品
            product = _get_or_create_product(db, record.product_name, current_user)
            product_id = product.id
            product_name = product.name
```

改为：

```python
        else:
            # 如果没有提供 product_id，自动创建或获取商品
            product = _get_or_create_product(db, record.product_name, current_user, record.ingredient_id)
            product_id = product.id
            product_name = product.name
```

**Step 4: 验证后端**

- 确认 `Optional` 已在文件顶部导入（products.py 顶部已有 `from typing import List, Optional`，✓）。
- 确认 `HTTPException` 已导入（✓）。
- 运行（在 `backend` 目录，使用项目虚拟环境）：

```bash
python -c "from app.api.products import _get_or_create_product; from app.schemas.product import ProductRecordCreate; print('OK')"
```

Expected: 输出 `OK`，无导入/语法错误。

- （可选，若服务已运行）用 Swagger/手动验证：`POST /api/v1/products` 不带 `product_id`，带 `product_name="测试挂靠商品"`、`ingredient_id=<某已有原料id>`、其余必填项，应在该原料下创建商品。

**完成标准**：导入无报错，既有「同名原料+商品」逻辑不受影响（不传 ingredient_id 行为不变）。

---

## Task 2: 前端解析纯函数 `pastePriceParser.ts`

**Files:**
- Create: `frontend/src/utils/pastePriceParser.ts`

**Step 1: 创建解析模块**

写入 `frontend/src/utils/pastePriceParser.ts`：

```typescript
/**
 * 粘贴文本价格解析器
 *
 * 支持格式（名称与价格之间用空格或 tab 分隔）：
 *   名称 价格            -> 芹菜 1.88            (默认 1 斤)
 *   名称 价格/单位       -> 芽菇 4/袋            (1 袋)
 *   名称 价格/缩写单位   -> 嫩豆腐 5.18/kg       (1 kg)
 *   名称 价格/数量+单位  -> 土豆粉 2.5/200g      (200 g)
 */

export interface ParsedPriceLine {
  raw: string                 // 原始行
  name: string                // 商品名
  price: number | null        // 价格
  quantity: number            // 数量
  unit: string                // 单位缩写
  ok: boolean                 // 是否解析成功
  error?: string              // 失败原因
}

// 中文单位别名 -> 系统 abbreviation（克=g、千克=kg、斤=斤）
const UNIT_ALIASES: Record<string, string> = {
  '克': 'g',
  '公斤': 'kg',
  '千克': 'kg',
}

// 名称(非贪婪) 价格 [/[数量]单位]
// 组1 名称 | 组2 价格 | 组3 数量(可空) | 组4 单位(可空)
const LINE_RE = /^(.+?)\s+(\d+(?:\.\d+)?)(?:\/(\d*\.?\d*)\s*([A-Za-z一-龥]+))?\s*$/

function normalizeUnit(raw: string): string {
  return UNIT_ALIASES[raw] ?? raw
}

export function parsePasteLine(line: string): ParsedPriceLine {
  const trimmed = line.trim()

  if (trimmed === '') {
    return { raw: line, name: '', price: null, quantity: 0, unit: '', ok: false, error: '空行' }
  }
  if (trimmed.startsWith('#')) {
    return { raw: line, name: '', price: null, quantity: 0, unit: '', ok: false, error: '注释行' }
  }

  const m = trimmed.match(LINE_RE)
  if (!m) {
    return { raw: line, name: '', price: null, quantity: 0, unit: '', ok: false, error: '格式无法识别' }
  }

  const name = m[1].trim()
  const price = parseFloat(m[2])
  const qtyStr = m[3] ?? ''     // 可能为空串
  const unitStr = m[4]          // 可能为 undefined

  const quantity = qtyStr !== '' ? parseFloat(qtyStr) : 1
  const unit = unitStr ? normalizeUnit(unitStr) : '斤'

  if (!name) {
    return { raw: line, name: '', price, quantity, unit, ok: false, error: '商品名为空' }
  }
  if (!Number.isFinite(price) || price <= 0) {
    return { raw: line, name, price, quantity, unit, ok: false, error: '价格无效' }
  }

  return { raw: line, name, price, quantity, unit, ok: true }
}

export function parsePasteText(text: string): ParsedPriceLine[] {
  return text.split(/\r?\n/).map(parsePasteLine)
}
```

**Step 2: 手动验证解析逻辑**

在浏览器控制台（或临时把模块内容粘进控制台）验证四种格式 + 错误格式。期望：

```text
parsePasteLine('芹菜 1.88')        -> {name:'芹菜', price:1.88, quantity:1, unit:'斤', ok:true}
parsePasteLine('芽菇 4/袋')        -> {name:'芽菇', price:4,    quantity:1, unit:'袋', ok:true}
parsePasteLine('嫩豆腐 5.18/kg')   -> {name:'嫩豆腐', price:5.18, quantity:1, unit:'kg', ok:true}
parsePasteLine('土豆粉 2.5/200g')  -> {name:'土豆粉', price:2.5, quantity:200, unit:'g', ok:true}
parsePasteLine('老豆腐 5.96/公斤') -> {name:'老豆腐', price:5.96, quantity:1, unit:'kg', ok:true}  // 公斤->kg
parsePasteLine('   ')              -> {ok:false, error:'空行'}
parsePasteLine('# 备注')           -> {ok:false, error:'注释行'}
parsePasteLine('随便写写')         -> {ok:false, error:'格式无法识别'}
```

**完成标准**：所有用例符合期望。

---

## Task 3: 对话框组件 `PasteImportDialog.vue`

本任务拆为 3a/3b/3c 三个小步，逐层构建并验证。最终文件：`frontend/src/components/prices/PasteImportDialog.vue`。

### 设计要点（三步通用）

- Props：`modelValue: boolean`、`merchantId: number | null`
- Emits：`update:modelValue`、`imported`（通知父组件刷新）
- 行状态机：解析后每行得到 `status`：`'matched'`（已自动/手动关联到商品）、`'unmatched'`（需处理，默认创建同名）、`'invalid'`（解析失败，导入时跳过）
- 未匹配行的处理模式 `mode`：`'existing'`（关联已有商品，带 `productId`）、`'new_same'`（创建同名原料+商品）、`'new_attach'`（挂靠已有原料，带 `ingredientId`）

### 3a: 组件骨架 + 解析 + 预览表格（只读）

**Files:**
- Create: `frontend/src/components/prices/PasteImportDialog.vue`

**Step 1: 创建组件，写入骨架与预览（暂不含匹配交互与导入执行）**

写入以下内容（本步只实现：日期时间、文本框、解析按钮、预览表格展示 `parsePasteText` 结果）：

```vue
<template>
  <v-dialog :model-value="modelValue" max-width="720" persistent @update:model-value="$emit('update:modelValue', $event)">
    <v-card>
      <v-card-title class="d-flex align-center">
        粘贴导入价格
        <v-spacer />
        <v-btn icon="mdi-close" variant="text" size="small" @click="$emit('update:modelValue', false)" />
      </v-card-title>

      <v-card-text>
        <!-- 日期时间 -->
        <v-text-field
          v-model="dateTime"
          label="记录时间"
          type="datetime-local"
          variant="outlined"
          density="compact"
          class="mb-3"
          hide-details
        />

        <!-- 粘贴区 -->
        <v-textarea
          v-model="rawText"
          label="粘贴价格文本（每行一条，格式：名称 价格[/单位]）"
          variant="outlined"
          rows="6"
          :placeholder="`芹菜 1.88\n芽菇 4/袋\n嫩豆腐 5.18/kg\n土豆粉 2.5/200g`"
          hide-details
          class="mb-3"
        />

        <div class="d-flex align-center mb-3">
          <v-btn color="primary" variant="tonal" prepend-icon="mdi-text-search" :loading="parsing" @click="doParse">
            解析并匹配
          </v-btn>
          <span v-if="summaryText" class="text-caption text-medium-emphasis ml-3">{{ summaryText }}</span>
        </div>

        <!-- 预览表格 -->
        <v-table v-if="rows.length > 0" density="compact" class="paste-preview-table">
          <thead>
            <tr>
              <th style="width: 36px"></th>
              <th>商品</th>
              <th style="width: 90px">价格</th>
              <th style="width: 70px">数量</th>
              <th style="width: 70px">单位</th>
            </tr>
          </thead>
          <tbody>
            <tr v-for="(row, i) in rows" :key="i">
              <td class="text-center">
                <v-icon v-if="row.status === 'matched'" color="success" size="small">mdi-check-circle</v-icon>
                <v-icon v-else-if="row.status === 'unmatched'" color="warning" size="small">mdi-alert-circle</v-icon>
                <v-icon v-else color="error" size="small">mdi-close-circle</v-icon>
              </td>
              <td>{{ row.name || '—' }}</td>
              <td>{{ row.price ?? '—' }}</td>
              <td>{{ row.quantity }}</td>
              <td>{{ row.unit }}</td>
            </tr>
          </tbody>
        </v-table>
      </v-card-text>

      <v-alert v-if="result" :type="result.fail === 0 ? 'success' : 'warning'" variant="tonal" class="mx-4 mb-2">
        导入完成：成功 {{ result.success }} 条，失败 {{ result.fail }} 条
        <div v-if="result.failures.length" class="text-caption mt-1">
          失败项目：{{ result.failures.map(f => `${f.name}（${f.reason}）`).join('、') }}
        </div>
      </v-alert>

      <v-card-actions>
        <v-spacer />
        <v-btn variant="text" @click="$emit('update:modelValue', false)">关闭</v-btn>
      </v-card-actions>
    </v-card>
  </v-dialog>
</template>

<script setup lang="ts">
import { ref, computed, watch } from 'vue'
import { api } from '@/api/client'
import { getLocalDateTimeString } from '@/utils/timezone'
import { parsePasteText, type ParsedPriceLine } from '@/utils/pastePriceParser'

interface ImportRow extends ParsedPriceLine {
  status: 'matched' | 'unmatched' | 'invalid'
  productId: number | null
  ingredientId: number | null
  mode: 'existing' | 'new_same' | 'new_attach'
}

const props = defineProps<{
  modelValue: boolean
  merchantId: number | null
}>()

defineEmits<{
  (e: 'update:modelValue', value: boolean): void
  (e: 'imported'): void
}>()

const dateTime = ref(getLocalDateTimeString())
const rawText = ref('')
const parsing = ref(false)
const rows = ref<ImportRow[]>([])
const result = ref<{ success: number; fail: number; failures: { name: string; reason: string }[] } | null>(null)

const summaryText = computed(() => {
  if (rows.value.length === 0) return ''
  const matched = rows.value.filter(r => r.status === 'matched').length
  const unmatched = rows.value.filter(r => r.status === 'unmatched').length
  const invalid = rows.value.filter(r => r.status === 'invalid').length
  return `已匹配 ${matched} · 待处理 ${unmatched} · 无法识别 ${invalid}`
})

// 打开时重置
watch(() => props.modelValue, (val) => {
  if (val) {
    dateTime.value = getLocalDateTimeString()
    rawText.value = ''
    rows.value = []
    result.value = null
  }
})

// 3a：仅解析，不匹配。匹配逻辑在 3b 接入
async function doParse() {
  result.value = null
  parsing.value = true
  try {
    const parsed = parsePasteText(rawText.value)
    rows.value = parsed.map(p => ({
      ...p,
      status: p.ok ? 'unmatched' : 'invalid',
      productId: null,
      ingredientId: null,
      mode: 'new_same',
    }))
  } finally {
    parsing.value = false
  }
}
</script>

<style scoped>
.paste-preview-table :deep(th),
.paste-preview-table :deep(td) {
  padding: 4px 8px !important;
  font-size: 13px;
}
</style>
```

**Step 2: 构建验证**

```bash
cd frontend && npm run build
```

Expected: 构建通过（组件尚未被引用，但需保证自身语法/类型正确）。

**完成标准**：构建通过；组件可被父组件挂载（下个任务接入后能在浏览器粘贴文本看到预览表）。

---

### 3b: 商品匹配交互（关联已有 / 创建同名 / 挂靠原料）

**Files:**
- Modify: `frontend/src/components/prices/PasteImportDialog.vue`

**Step 1: 在 `doParse` 中接入「自动精确匹配」**

解析后，对每个 `ok` 的行调商品搜索；若返回项中存在 `item.name === row.name`，则自动标记 matched。把 3a 的 `doParse` 替换为：

```typescript
async function doParse() {
  result.value = null
  parsing.value = true
  try {
    const parsed = parsePasteText(rawText.value)
    rows.value = parsed.map(p => ({
      ...p,
      status: p.ok ? 'unmatched' : 'invalid',
      productId: null,
      ingredientId: null,
      mode: 'new_same',
    }))
    // 自动精确匹配（并发，限量）
    const okRows = rows.value.filter(r => r.ok)
    await Promise.all(okRows.map(r => tryAutoMatch(r)))
  } finally {
    parsing.value = false
  }
}

async function tryAutoMatch(row: ImportRow) {
  try {
    const list: any[] = await api.get('/products/autocomplete', { params: { q: row.name, limit: 10 } })
    const exact = (list || []).find((it: any) => it.name === row.name)
    if (exact) {
      row.productId = exact.id
      row.status = 'matched'
      row.mode = 'existing'
    }
  } catch {
    /* 匹配失败保持 unmatched，由用户手动处理 */
  }
}
```

**Step 2: 预览表格中，未匹配行增加可点击的商品/原料选择**

把预览表格中「商品」那一列单元格替换为条件渲染（未匹配行可操作）。在模板的 `<tbody>` 内，将该 `<td>{{ row.name || '—' }}</td>` 替换为：

```html
              <td>
                <template v-if="row.status === 'invalid'">{{ row.name || '（' + row.error + '）' }}</template>
                <template v-else-if="row.status === 'matched'">{{ row.name }}</template>
                <!-- 未匹配：可关联已有商品 -->
                <v-menu v-else :close-on-content-click="true" location="bottom">
                  <template #activator="{ props: menuProps }">
                    <span class="paste-editable" v-bind="menuProps">{{ row.name }}<v-icon size="x-small" class="ml-1">mdi-chevron-down</v-icon></span>
                  </template>
                  <v-list density="compact" max-height="320" width="280">
                    <v-list-subheader>关联已有商品</v-list-subheader>
                    <v-list-item
                      v-for="opt in row.suggestions"
                      :key="opt.id"
                      :title="opt.name"
                      @click="chooseExisting(row, opt)"
                    />
                    <v-list-item v-if="!row.suggestions || row.suggestions.length === 0">
                      <v-list-item-title class="text-medium-emphasis text-caption">无匹配商品</v-list-item-title>
                    </v-list-item>
                    <v-divider />
                    <v-list-subheader>创建</v-list-subheader>
                    <v-list-item title="创建同名原料 + 商品" :active="row.mode === 'new_same'" @click="chooseNewSame(row)" />
                    <v-list-item>
                      <v-list-item-title class="text-medium-emphasis text-caption">
                        <v-menu :close-on-content-click="true" location="bottom end">
                          <template #activator="{ props: sub }">
                            <span v-bind="sub">挂靠已有原料…</span>
                          </template>
                          <v-list density="compact" max-height="300" width="240">
                            <v-list-item
                              v-for="ing in row.ingredientSuggestions"
                              :key="ing.id"
                              :title="ing.name"
                              @click="chooseAttach(row, ing)"
                            />
                            <v-list-item v-if="!row.ingredientSuggestions || row.ingredientSuggestions.length === 0">
                              <v-list-item-title class="text-medium-emphasis text-caption">输入关键词搜索原料</v-list-item-title>
                            </v-list-item>
                          </v-list>
                        </v-menu>
                      </v-list-item-title>
                    </v-list-item>
                  </v-list>
                </v-menu>
              </td>
```

> 注意：挂靠原料的二级菜单采用嵌套 `v-menu`。若嵌套菜单在某些 Vuetify 版本下定位异常，可改为「未匹配行直接展开内联搜索框」的折中方案（见 Step 4 备选）。

**Step 3: 扩展 `ImportRow` 类型与交互函数**

把 `interface ImportRow` 改为：

```typescript
interface ImportRow extends ParsedPriceLine {
  status: 'matched' | 'unmatched' | 'invalid'
  productId: number | null
  ingredientId: number | null
  mode: 'existing' | 'new_same' | 'new_attach'
  suggestions: { id: number; name: string }[]
  ingredientSuggestions: { id: number; name: string }[]
}
```

`doParse` 初始化时补两个字段（在 map 里加 `suggestions: [], ingredientSuggestions: []`）。

新增交互函数与菜单展开时的搜索加载（加入 `<script setup>`）：

```typescript
// 展开未匹配菜单时加载商品建议
watch(rows, async (list) => {
  for (const r of list) {
    if (r.status === 'unmatched' && r.suggestions.length === 0) {
      try {
        const list2: any[] = await api.get('/products/autocomplete', { params: { q: r.name, limit: 10 } })
        r.suggestions = (list2 || []).map((it: any) => ({ id: it.id, name: it.name }))
      } catch {
        r.suggestions = []
      }
    }
  }
}, { deep: true })

function chooseExisting(row: ImportRow, opt: { id: number; name: string }) {
  row.productId = opt.id
  row.ingredientId = null
  row.mode = 'existing'
  row.status = 'matched'
}

function chooseNewSame(row: ImportRow) {
  row.productId = null
  row.ingredientId = null
  row.mode = 'new_same'
  row.status = 'matched'
}

// 挂靠原料：首次展开加载原料建议（懒加载）
async function ensureIngredientSuggestions(row: ImportRow) {
  if (row.ingredientSuggestions.length > 0) return
  try {
    const res: any = await api.get('/ingredients', { params: { q: row.name, limit: 10 } })
    row.ingredientSuggestions = ((res.items || []) as any[]).map((it: any) => ({ id: it.id, name: it.name }))
  } catch {
    row.ingredientSuggestions = []
  }
}

function chooseAttach(row: ImportRow, ing: { id: number; name: string }) {
  row.productId = null
  row.ingredientId = ing.id
  row.mode = 'new_attach'
  row.status = 'matched'
}
```

并在模板挂靠原料菜单的 activator 上加上 `@click="ensureIngredientSuggestions(row)"`（让展开时触发懒加载）。

**Step 4（备选，仅当嵌套菜单体验不佳）：内联搜索折中方案**

若嵌套 `v-menu` 在移动端定位不稳定，未匹配行改为：点击后在该行下方展开一个 `v-autocomplete`（商品）和一个「挂靠原料」`v-autocomplete`（原料），选中后写入 `productId`/`ingredientId` 并置 matched。逻辑函数不变，仅模板呈现方式不同。

**Step 5: 构建验证**

```bash
cd frontend && npm run build
```

Expected: 构建通过。

**完成标准**：构建通过；接入父组件后，粘贴示例文本 → 已存在商品自动标绿，未存在行可手动关联/创建同名/挂靠原料。

---

### 3c: 导入执行（并发 + 进度 + 结果）

**Files:**
- Modify: `frontend/src/components/prices/PasteImportDialog.vue`

**Step 1: 加入导入按钮与进度条**

在 `<!-- 预览表格 -->` 之后、`</v-card-text>` 之前，加入：

```html
        <div v-if="rows.some(r => r.status === 'matched')" class="mt-3">
          <v-progress-linear
            v-if="importing"
            :model-value="progressPct"
            height="8"
            color="primary"
            class="mb-2"
          />
          <div v-if="importing && progressText" class="text-caption text-medium-emphasis mb-2">{{ progressText }}</div>
          <v-btn color="primary" :loading="importing" :disabled="importing" prepend-icon="mdi-database-import" @click="doImport">
            全部导入（{{ importableCount }} 条）
          </v-btn>
        </div>
```

**Step 2: 新增导入逻辑**

在 `<script setup>` 中新增：

```typescript
import { getErrorMessage } from '@/utils/errorHandler'

const emit = defineEmits<{
  (e: 'update:modelValue', value: boolean): void
  (e: 'imported'): void
}>()

const importing = ref(false)
const progress = ref({ current: 0, total: 0 })

const importableRows = computed(() => rows.value.filter(r => r.status === 'matched' && r.ok))
const importableCount = computed(() => importableRows.value.length)
const progressPct = computed(() => {
  if (progress.value.total === 0) return 0
  return Math.round((progress.value.current / progress.value.total) * 100)
})
const progressText = computed(() =>
  progress.value.total > 0 ? `正在导入 ${progress.value.current}/${progress.value.total}…` : ''
)

async function doImport() {
  if (!props.merchantId) return
  if (importableRows.value.length === 0) return

  importing.value = true
  result.value = null
  const targets = importableRows.value
  progress.value = { current: 0, total: targets.length }

  const recordedAt = dateTime.value ? new Date(dateTime.value).toISOString() : undefined

  // 构造 payload
  const payloads = targets.map(row => {
    const payload: Record<string, any> = {
      price: row.price,
      original_quantity: row.quantity,
      original_unit: row.unit,
      merchant_id: props.merchantId,
      record_type: 'price',  // 不计入支出
    }
    if (recordedAt) payload.recorded_at = recordedAt
    if (row.mode === 'existing' && row.productId) {
      payload.product_id = row.productId
    } else {
      payload.product_name = row.name
      if (row.mode === 'new_attach' && row.ingredientId) {
        payload.ingredient_id = row.ingredientId
      }
    }
    return { row, payload }
  })

  // 并发导入，并发数 5（沿用快速填写模式）
  const CONCURRENCY = 5
  let success = 0
  const failures: { name: string; reason: string }[] = []

  for (let i = 0; i < payloads.length; i += CONCURRENCY) {
    const batch = payloads.slice(i, i + CONCURRENCY)
    const settled = await Promise.allSettled(
      batch.map(p => api.post('/products', p.payload).then(() => p))
    )
    settled.forEach((s, j) => {
      if (s.status === 'fulfilled') {
        success++
      } else {
        const reason = getErrorMessage((s as PromiseRejectedResult).reason, '导入失败')
        failures.push({ name: batch[j].row.name, reason })
      }
      progress.value.current++
    })
  }

  importing.value = false
  progress.value = { current: 0, total: 0 }
  result.value = { success, fail: failures.length, failures }

  // 全部成功时通知父组件刷新并关闭
  if (failures.length === 0) {
    emit('imported')
    emit('update:modelValue', false)
  }
}
```

> 注意：本步把原来 3a/3b 中临时的 `defineEmits` 合并为此处的带 `imported` 版本，删除之前的重复声明，避免重复声明报错。

**Step 3: 构建验证**

```bash
cd frontend && npm run build
```

Expected: 构建通过。

**完成标准**：构建通过；点「全部导入」后进度条推进，完成后展示成功/失败数量与失败项目（在浏览器联调阶段端到端验证，见 Task 5）。

---

## Task 4: QuickFillView 集成按钮与对话框

**Files:**
- Modify: `frontend/src/views/prices/QuickFillView.vue`

**Step 1: 导航栏新增「粘贴导入」按钮**

在 `frontend/src/views/prices/QuickFillView.vue` 的 `<template #append>` 内（约 5-12 行），把：

```html
    <template #append>
      <div class="d-flex align-center">
        <span v-if="saveProgress" class="text-caption text-medium-emphasis mr-2">
          {{ saveProgress.current }}/{{ saveProgress.total }}
        </span>
        <v-btn icon="mdi-check-all" variant="text" :loading="saving" @click="saveAll" />
      </div>
    </template>
```

改为（在保存按钮前加一个粘贴导入按钮，商家未选时禁用）：

```html
    <template #append>
      <div class="d-flex align-center">
        <span v-if="saveProgress" class="text-caption text-medium-emphasis mr-2">
          {{ saveProgress.current }}/{{ saveProgress.total }}
        </span>
        <v-btn
          icon="mdi-content-paste"
          variant="text"
          :disabled="!selectedMerchantId"
          @click="pasteDialog = true"
        />
        <v-btn icon="mdi-check-all" variant="text" :loading="saving" @click="saveAll" />
      </div>
    </template>
```

**Step 2: 在模板末尾（`<v-snackbar>` 之后、`</v-container>` 之前或之后均可，建议 snackbar 之后）加入对话框**

在 `<v-snackbar ...>...</v-snackbar>` 之后插入：

```html
    <PasteImportDialog
      v-model="pasteDialog"
      :merchant-id="selectedMerchantId"
      @imported="onPasteImported"
    />
```

**Step 3: 引入组件并新增状态与回调**

在 `<script setup>` 顶部 import 区（`import { api } from '@/api/client'` 之后）加入：

```typescript
import PasteImportDialog from '@/components/prices/PasteImportDialog.vue'
```

在状态区（`const snackbar = ...` 附近）加入：

```typescript
const pasteDialog = ref(false)
```

在 `saveAll` 之后、`onMounted` 之前加入回调（导入成功后刷新历史商品）：

```typescript
async function onPasteImported() {
  if (selectedMerchantId.value) {
    await onMerchantChange(selectedMerchantId.value)
  }
}
```

**Step 4: 构建验证**

```bash
cd frontend && npm run build
```

Expected: 构建通过。

**完成标准**：构建通过；快速填写页导航栏出现粘贴图标，选好商家后可点击打开对话框。

---

## Task 5: 端到端验证与记录要点

**Step 1: 端到端手动验证（开发者已运行前后端服务）**

在浏览器（`http://localhost:5173`）进入「快速填写」页面：

1. 选择一个商家 → 粘贴图标变为可用。
2. 点粘贴图标 → 弹出对话框，记录时间默认为当前。
3. 粘贴示例文本：
   ```
   芹菜 1.88
   水芹 3.99
   芽菇 4/袋
   嫩豆腐 5.18/kg
   土豆粉 2.5/200g
   ```
4. 点「解析并匹配」→ 已存在商品标绿，未存在的标黄。
5. 对未匹配行分别尝试：关联已有商品、创建同名、挂靠原料。
6. 点「全部导入」→ 进度条推进 → 完成提示成功数。
7. 关闭后回到快速填写，已导入商品按「近 1h 隐藏」逻辑不再显示；前往价格记录页确认新记录存在、且 `record_type` 为 price（不计入支出）。

**Step 2: 失败路径验证**

- 故意让某行匹配一个会被后端拒绝的输入（如把价格改空、或断网），确认失败数与失败项目正确展示，且成功项不受影响。
- 未选商家时粘贴图标禁用；对话框内若 `merchantId` 为空不应触发导入（已有 `if (!props.merchantId) return` 保护）。

**Step 3: 记录要点（遵循 CLAUDE.md 索引规范）**

在 `cc/` 目录新建 `FEATURE_快速填写粘贴导入.md`，记录：导航栏新增「粘贴导入」入口；对话框解析（`名称 价格[/[数量]单位]` 四种格式）→ 预览匹配 → 并发导入（record_type=price 不计入支出）；不存在的商品支持「关联已有 / 创建同名原料+商品 / 挂靠已有原料」三种处理；后端 `ProductRecordCreate` 增 `ingredient_id`、`_get_or_create_product` 支持挂靠。并在 `CLAUDE.md` 的「功能实现记录」与索引补一行链接。

**完成标准**：端到端流程跑通；要点文档已记录并登记到 `CLAUDE.md` 索引。

---

## 验收清单

- [ ] Task 1：后端 `ingredient_id` 改动可导入，向后兼容。
- [ ] Task 2：解析函数四种格式 + 错误格式全部符合期望。
- [ ] Task 3：组件解析→匹配→并发导入→结果展示完整，构建通过。
- [ ] Task 4：快速填写页按钮接入，构建通过。
- [ ] Task 5：端到端验证通过，记录要点。
- [ ] 全程无语法错误、构建通过；未自行 git 提交（遵循项目规范）。
