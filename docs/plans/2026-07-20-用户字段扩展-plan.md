# 用户字段扩展（阶段二）实施计划

> 执行 design 稿 §5（阶段二·A）和 §6（阶段二·B）。阶段一（图片存储抽象层）已完成，`get_storage()` 就绪。

**依赖**：avatar 上传依赖阶段一（已完成）；nickname / 类型修复 / 行政区划不依赖阶段一，可与 avatar 并行。

---

## 阶段二·A：用户字段（avatar / nickname）

### Task A1: User 模型加列 + 迁移
- Modify: `backend/app/models/user.py` — 加 `avatar`（String nullable）、`nickname`（String nullable）
- 新建 alembic 迁移 + SQLite/MySQL/PostgreSQL 三引擎 SQL

### Task A2: UserResponse 更新 + _user_to_response 收口
- Modify: `backend/app/schemas/auth.py` — UserResponse 加 avatar/nickname
- Modify: `backend/app/api/auth.py` — 7 处手工构造收进 `_user_to_response`

### Task A3: PUT /auth/me/account 支持 nickname
- Modify: `backend/app/schemas/auth.py` — UserAccountUpdate 加 nickname
- Modify: `backend/app/api/auth.py` — update_my_account 处理 nickname

### Task A4: POST /auth/me/avatar（上传头像走 storage）
- Modify: `backend/app/api/auth.py` — 新增端点：multipart → `storage.put('avatars/...')` → 更新 `user.avatar` → 返 `url_for`；换头像删旧

### Task A5: 前端 User 类型大扫除
- Modify: `frontend/src/types/index.ts` — 删 `is_superuser`，补 phone/is_active/email_verified/nutrition_goals/daily_budget/unit_preferences/avatar/nickname

### Task A6: 前端 ProfileView 加 nickname 编辑 + 头像上传
- Modify: `frontend/src/views/profile/ProfileView.vue` — 编辑对话框加 nickname + avatar 上传组件

### Task A7: 前端 AppLayout 加用户卡片
- Modify: `frontend/src/components/layout/AppLayout.vue` — 抽屉头部加 v-avatar + 昵称（空 fallback username）

---

## 阶段二·B：行政区划模块

### Task B1: AdministrativeRegion 模型 + 迁移
- Create: `backend/app/models/administrative_region.py`
- alembic 迁移 + 三引擎 SQL

### Task B2: 行政区划数据 seed
- Create: `backend/app/services/region_seed.py` — 类似 allergen_seed.py
- 中国省/市/县（~3500）+ 全球国家级（~250），静态 JSON 打包
- lifespan `ensure_administrative_regions`（幂等）

### Task B3: User 加 region_id FK
- Modify: `backend/app/models/user.py` — 加 `region_id`（FK nullable）
- alembic 迁移 + 三引擎 SQL

### Task B4: GET /regions API（懒加载子节点）
- Create: `backend/app/api/regions.py`
- `GET /regions?parent_id=&level=` — 子节点列表

### Task B5: 前端级联选择器
- 在 ProfileView 编辑对话框加入级联选择器（国→省→市→县，按 parent 懒加载）
